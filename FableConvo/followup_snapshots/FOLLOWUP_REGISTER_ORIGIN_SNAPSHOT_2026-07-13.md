# ARA follow-up register — geometry drill with Codex/Sol

**Captured:** 12 July 2026  
**Purpose:** preserve promising threads without requiring Dylan to test them immediately.  
**Orientation:** up = slower/larger; down = faster/smaller unless a test declares otherwise.  
**Protocol status:** this is a planning register, not `MASTER_PREDICTION_LEDGER` registration. Every item remains `PARKED` until Dylan explicitly activates it and signs a Step-0 header under `TEST_PROTOCOL.md`.

## Living-register governance — amended 13 July 2026

This file is a **living index**, not a global preregistration freeze. Its dated origin snapshot is preserved at
`FableConvo/followup_snapshots/FOLLOWUP_REGISTER_ORIGIN_SNAPSHOT_2026-07-13.md`.

- A parked thread may be clarified, decompressed and updated as ARA develops. Append a dated amendment; do not erase
  the earlier wording or pretend the newer wording was known earlier.
- Hard freeze occurs only when Dylan activates a thread. Create a separate dated/versioned protocol, settle the
  target, coordinate, rivals, controls and kill conditions, then hash it **before inspecting target outcomes**.
- If relevant outcomes or plots were already seen, disclose that exposure. The highest valid ceiling is then
  replication/extension, not blind discovery.
- Never repair a frozen protocol on its scored data. A changed rule becomes a new version and needs new target data.
- Priority and energy estimates may change through dated amendments because they are planning metadata, not outcomes.

**13 July amendment:** H1 is explicitly non-blind and capped at replication/extension because the BP/heart evidence
already shaped it. H1, T1 and R1 were raised to high human-energy estimates. A1 and B1 received sharper kill
conditions. MX1 simulator-enforced identity correlations were demoted to instrument checks. MX1–MX3g are disclosed
as one adaptive exploratory chain on one inspected archive.

## How to use this file

- Continue discussing geometry without opening a test merely because it is interesting.
- When Dylan says “activate `<ID>`,” write one short preregistration with one primary claim, controls, and falsifier.
- Close or pause the active test before activating another in the same thread.
- Preserve failed tests; do not repair a frozen hypothesis on its scored dataset.
- Energy estimates are human-work estimates, not compute estimates.

## Priority map

| ID | Thread | Status | Priority | Dylan energy | Why it matters |
|---|---|---|---:|---:|---|
| H1 | Resolved two-sided heart + BP observability | PARKED / NON-BLIND | 1 | High | Replication/extension test of the new “RR is a mixed ridge” explanation. |
| H2 | Raw-data coupled-pair recovery | PARKED | 2 | Medium | Tests whether preprocessing hides the relation channel. |
| T1 | Temporal tomography and conditional ARA value | PARKED | 3 | High | Tests whether geometry compresses/adds to a proper delay reconstruction. |
| C1 | Connection↔transfer time-slice axis | PARKED | 4 | Low–medium | Deborah/storage-loss physics directly measures the proposed axis. |
| W1 | Multi-liquid capillary–gravity handover | PROTOCOL WRITTEN, DATA PENDING | 5 | Low–medium | Cleanest measured water/interface test; fixed math already written. |
| R1 | One canonical coarse-graining operator | PARKED | 6 | High | Addresses the main missing aggregation/rung law. |
| A1 | Atmosphere–space exobase handover | PARKED | 7 | Medium | Better match to Dylan's “atmospheric boundary tension” intuition. |
| P1 | Phi interfacial-weave test | PARKED | 8 | High | Discriminates a new phi role from generic boundaries. |
| P2 | Phi identity-preserving repeated exchange | PARKED | 9 | Medium | Turns non-recurrence intuition into a signed optimization test. |
| S1 | Self-localized sphere vs deformation/collapse | PARKED | 10 | High | Defines when external pressure confines, remodels, or destroys identity. |
| B1 | Body–Earth coupling graph | PARKED | 11 | Low | Repairs the old muscle→plate category mismatch without number translation. |
| X1 | Hidden pairs behind apparent singletons | PARKED | 12 | High | Important ontology; requires strict anti-unfalsifiability rules. |
| L1 | Lightning–neuron empirical waveform replication | PARKED/LOW | 13 | Medium | Existing canonical-shape test was mixed and synthetic. |
| D1 | Dark-sector geometric derivation | PARKED | 14 | Very high | Provenance is valid; physics derivation remains parameter-supplied. |
| G1 | Triangle→hexagon→pentagon slice mechanism | PARKED | 15 | Medium | Separates exact geometry from the proposed reality-slice ontology. |
| Q1 | Quantum/atomic observable-specific ARA cycles | PARKED | 16 | High | Prevents classical-orbit category slips while retaining real quantum cycles. |
| M1 | LLM Information³ pair/closure instrument | PARKED + SAFEGUARDED | 17 | High | Tests hidden relations in raw activations without equating closure with truth. |
| U1 | Fusion/engineering prospective application | PARKED | 18 | Very high | Strong prospective test class, but requires expert domain and safety review. |
| O1 | Orbital scale covariance and physical-law crosswalk audit | PARKED | 19 | Medium | Separates real relation-born modes from “every equation has three symbols.” |
| EM1 | Bound-field↔radiative-light decomposition | PARKED | 20 | Medium | Directly instruments Matter/Information versus Information/Time using antenna physics. |
| MX1 | Gauss↔ARA/TE-ARA plasma crosswalk | DRAFT; CONFIRMATION SEALED | 21 | Low | Best current aggregation bridge; development data verified and independent holdout reserved. |
| F1 | Return to deliberate prediction modelling | PARKED | Later | High | Dylan wants deeper geometry and system knowledge first. |

---

## H1 — Resolved two-sided heart and BP observability

**Status:** `PARKED / NON-BLIND HYPOTHESIS / REPLICATION-EXTENSION CEILING`, highest priority for later cardiac work.

The existing heart/BP results and this conversation shaped the hypothesis. It must not be represented as an unseen
discovery prediction. A later activated protocol can still provide a strong held-out replication/extension test on
new subjects and more resolved channels.

### Dylan's hypothesis

The RR/ECG signal may be a `1.0`-ridge projection of two coupled heart parts. BP helped because it observed across the next coupling/rung. Directly resolving the two heart sections and their interaction should make the heart easier to map.

### Refined system decomposition

- right pump ↔ left pump;
- atrial filling ↔ ventricular ejection inside each pump;
- pulmonary circulation transfers right output into left preload;
- systemic arterial pressure records left mechanical output;
- baroreflex returns BP information to cardiac control.

### Minimum useful data

Simultaneous:

1. multi-lead ECG or intracardiac electrograms;
2. systemic arterial BP;
3. pulmonary arterial pressure or central venous pressure;
4. respiration;
5. preferably ventricular volume/flow or impedance cardiography.

### Pre-activation discriminating prediction candidate

If BP helps mainly because RR hides the internal pair, then a resolved right/left state model should:

- beat RR-only under held-out subjects; and
- materially reduce the incremental lift obtained by adding BP.

If BP retains its full lift after internal state resolution, BP is carrying external vascular/baroreflex information rather than merely unmixing the heart.

### Mandatory models

1. persistence and train-mean;
2. autoregressive RR;
3. full-waveform ECG morphology;
4. resolved right/left state;
5. RR+BP;
6. resolved state+BP;
7. standard multivariate state-space/VAR baseline with the same channels.

### Failure conditions

- no stable pair is recovered across held-out subjects;
- resolved channels do not improve state prediction;
- BP lift is unchanged and no predicted coupling mechanism explains it;
- only one selectively chosen patient benefits.

### Existing evidence/artifacts

- `TheFormula/12.../heart_bp_eeg_thirdleg_result.json` — BP lift on four polysomnographic records.
- `TheFormula/12.../heart_mimic_lock_result.json` — BP lift on four MIMIC records.
- `TheFormula/02.../POST_LEAK_CARDIAC_REPLICATION_2026-07-11.md` — earlier general ARA forecast did not replicate.

### Restart phrase

> “Activate H1: find a public simultaneous right/left cardiac + BP dataset and write the observability preregistration before inspecting outcomes.”

---

## H2 — Raw-data coupled-pair recovery

**Status:** `PARKED`.

### Question

Does ARA appear more useful on raw signals because filtering/aggregation projects phase and anti-phase modes onto a middle/ridge?

### Controlled representations of the same record

1. raw waveform;
2. phase-preserving denoising;
3. low-pass smoothing that progressively removes morphology;
4. narrowband version — expected by CANON to destroy duty asymmetry;
5. phase-randomized surrogate preserving the power spectrum;
6. interval or summary-only representation.

### Prediction candidate

Pair-recovery and future-state skill should degrade with destroyed relation information, not merely with reduced sample count. Phase randomization should remove the pair even when the spectrum is preserved.

### Controls

- known two-mode synthetic referee with fixed coupling;
- known one-mode synthetic negative control;
- matched noise/SNR;
- Fourier, state-space, delay-embedding, and nonlinear-coupling baselines;
- held-out subjects.

### Falsifier

ARA reports the same pair after phase randomization or invents a pair in the one-mode control.

### Relationship to H1

H2 can be an instrument gate before H1. It should not be used to tune the cardiac prediction after seeing H1 outcomes.

---

## T1 — Temporal tomography and conditional ARA value

**Status:** `PARKED`.

### Dylan's hypothesis

Multiple wave cycles reveal the overall sphere/fractal nature. Lag/home is hard to beat because it is the previous ARA slices and therefore already gives the general state unless a major fluctuation or large coupling enters.

### Exact anchor

Delay-coordinate embedding can reconstruct a low-dimensional deterministic attractor from a sufficiently rich scalar history under stated smoothness/genericity conditions. For fractal attractors, `Embedology` gives a generic embedding condition tied to box-counting dimension.

### Primary scientific question

Does an ARA geometry state:

1. compactly reconstruct the same delay manifold with fewer samples/parameters; or
2. add conditional future information after a strong causal delay/lag model?

### Model ladder

1. persistence, seasonal/cycle-ago where appropriate;
2. causal AR/lag+harmonic;
3. nonparametric delay-embedding neighbour predictor;
4. ARA geometry alone;
5. lag+ARA;
6. lag+measured physical feeders;
7. lag+feeders+ARA.

Match training windows, parameter budgets where possible, and held-out origins.

### Primary score

Predict the residual left by the strongest lag/delay baseline. Estimate conditional information only with bias controls and surrogates; out-of-sample residual skill is the safer primary result.

### Prediction candidate

ARA should add most near crossings, curvature changes, regime transitions, and low-concentration multi-rung states. It should add little to a clean concentrated oscillator already reconstructed by one clock/lag.

### ENSO instantiation

- home: NINO3.4 history;
- independent state: WWV, SOI/winds, possibly spatial thermocline/current features;
- external/innovation audit: westerly wind bursts and other unresolved shocks;
- do not assume solar or lunar drivers without an independently signed prediction.

### Falsifier

ARA adds no stable held-out residual information, requires more effective parameters, or its apparent gain disappears against delay-neighbour/lag-harmonic and physical-feeder baselines.

### Fractal fence

Estimate embedding/correlation dimension and scale ranges. Do not infer literal cross-scale fractality merely from autocorrelation or successful delay reconstruction.

---

## C1 — Connection↔transfer time-slice axis

**Status:** `PARKED`; unusually clean established anchor.

### Dylan's hypothesis

Connection orientation is locked by persistent nearby couplings. Information-transfer orientation is movement away from those connections and is more time-oriented. The measured ARA should use skew from coupled partners and interaction duration.

### Candidate physical bridge

- connection lifetime `τC`;
- observation/interaction duration `τS`;
- Deborah number `De=τC/τS`;
- oscillatory storage/loss response `G'`, `G''`;
- phase lag `δ=atan(G''/G')`.

### Candidate ARA coordinates

Choose one before real data:

\[
x=2/(1+De),
\]

or

\[
x=2G''/(G'+G''),
\]

or

\[
x=4\delta/\pi.
\]

All place persistent storage near `0`, crossover at `1`, and flow/relaxation near `2`; they are not interchangeable after outcomes are inspected.

### Cheapest first dataset

Use published frequency sweeps for several materials spanning elastic solid, viscoelastic gel/glass, liquid, and gas/low-density transport where appropriate. A digital Maxwell/Kelvin–Voigt referee can validate the instrument but cannot adjudicate nature.

### Predictions

- the same material moves from connection-like to transfer-like when the slice becomes long relative to its relaxation time;
- independently measured neighbour persistence/diffusion should track the chosen coordinate;
- the `x=1` crossover should correspond to equal storage/loss or matched slice/relaxation times, according to the signed definition.

### Controls/falsifiers

- standard rheology predicts everything with no ARA-added result: coordinate anchor only;
- mobility and directed information transfer fail to align, implying two axes;
- gas mobility is high while actual inter-particle information/momentum transfer is weak;
- different coordinate candidates choose different “hits,” revealing post-hoc flexibility.

### Force connection

If this axis underlies force, derive how storage/connection topology and transfer/flux combine into `dp/dt`, including variable-mass and field-mediated cases.

### Mass/connection-energy refinement

Mass is not binding energy alone. For an enclosed identity, invariant mass is the complete centre-of-momentum
energy account. Independently measure Connection using at least two of: binding fraction, coupling lifetime,
coordination, localization, or `G'`. Test whether this connection state predicts transfer/response after total mass
and ordinary material variables are controlled. The ARA `2` is a normalized capacity, not two joules, until a
rung-specific conversion is declared.

---

## W1 — Multi-liquid capillary–gravity handover

**Status:** protocol written; no empirical result.

### Existing protocol

- `analysis/water_atmosphere/ARA_WATER_ATMOSPHERE_PREREGISTRATION.md`
- `analysis/water_atmosphere/water_capillary_ara_test.py`

### Core coordinate

\[
q_w=\gamma k^2/(\Delta\rho g),\qquad x_w=2q_w/(1+q_w).
\]

### Fixed predictions already written

- phase-speed minimum and `c_g/c_p=1` crossing near `q=1`;
- pooled fixed-line error threshold;
- at least four liquids;
- deep-water and weak-viscosity eligibility;
- density-only, raw-wavenumber, one-pole, flexible, and shuffled controls.

### Honest evidence meaning

Success validates ARA as a useful coordinate for known mechanics. It is not by itself evidence of a new physical law because the transformation is algebraically equivalent to the standard dispersion relation.

### Next action

Select datasets by written search criteria, record every eligible dataset, then run without altering coordinate center or width.

---

## R1 — Canonical coarse-graining/rung operator

**Status:** `PARKED`; central mathematical priority.

### Question

Can one fixed operator map a local competition into the next-rung competition without changing its definition after each domain?

### Candidate form

\[
\mathcal R(\mathcal I_k,J_k,B_k)=\mathcal I_{k+1}.
\]

The preserved object should be relational topology, not physical units or identical response curves.

### 12 July refinement — fine wave meets coarse wave and closes a new sphere

Dylan's current synthesis supplies a more specific candidate for the missing operator:

\[
\mathscr A_h[H]
=
\mathcal C_\times\left(\mathscr A_f[\omega],\mathscr A_c[\Omega],J_{fc}\right).
\]

`𝒜_f` is the small/fine ARA occurrence, `𝒜_c` the large/coarse occurrence or envelope, and `J_fc` their cross-scale
relation. `H` is a new identity only if Information³ round-trip error is below a fixed tolerance, its boundary
closes over declared directions, and it persists for a fixed duration.

Interpret the finite polygons as reconstruction/scaffold choices for the same ARA section: circular phase projects
to a wave; `C_n→S¹` as resolution increases; directional `S¹` sections close into `S²`; filling gives `B³`. They are
not separate fundamental shapes.

Candidate domain manifestations of `J_fc` include phase locking, amplitude modulation, feedback, confinement and
interface stress. A valid canonical operator must define their common dimensionless relation before seeing outcomes;
merely renaming each local mechanism “coupling” would not establish universality.

### Strong physical anchor — oblique water-wave daughter mode

Crossing water waves provide the cleanest current decomposition of `𝒞_×`:

1. **Linear null:** `η₁+η₂=2A cos[(Δk·r−Δωt+Δφ)/2] cos[(Σk·r−Σωt+Σφ)/2]` gives simultaneous coarse envelope and
   fine carrier patterns, but the mother waves pass through; no new mode/identity is created.
2. **Nonlinear closure:** a resonant quartet satisfies `2k₁−k₃−k₄=0`, `2ω₁−ω₃−ω₄=0`, and phase locking; energy
   enters a daughter wave whose amplitude grows with interaction distance and mother-wave steepness.
3. **Detuned control:** nonzero wavevector/frequency mismatch suppresses daughter growth with a known response.

This suggests a fixed identity-spawn score built from normalized wavevector mismatch, frequency mismatch, phase-lock
variance, daughter energy fraction and persistence distance/time. Compare it against linear interference using held-
out crossing angles and steepnesses.

The dispersion relation chooses the minimum physical closure order: exact resonant triads are prohibited for ideal
deep-water gravity waves but allowed for capillary and many finite/shallow-water waves; deep-water gravity transfer is
dominated by quartets. This is a prospective test of whether the ARA polygonal resolution order can be predicted
before observing the interaction.

### Layered Sand is the upward/reconstruction direction

Dylan clarified that Layered Sand starts from the initial directional floor pull and propagates upward through fine,
medium and coarse daughter/contact layers to reconstruct the measured/larger wave. It is conceptually the reverse
reading of `𝒜_h=𝒞_×(𝒜_f,𝒜_c,J)`, but not generally an exact inverse because cross-scale closure is many-to-one.

Historical result constraints:

- terrain analog beats persistence, but the deterministic formula under-rolls and is mostly a current-terrain mapper;
- faster lower-rung spin is supported;
- lower spin alone does not select future boundary direction cleanly;
- upper/envelope information carries clearer event-risk signal.

Candidate next operator: represent every child/contact as a complex phasor `Z=Ae^(iφ)` and reconstruct the parent by
`Z_(i+1)=Γ_(i+1)(U,T) Σ_m w_im Z_im e^(iπp_im)`. Here `Γ` applies the upper-envelope/terrain closure constraint and
`p∈{0,1}` is the contact orientation flip. Score against scalar-roll Layered Sand, vector contact aggregation,
phase-shuffled children and a standard multiscale state-space baseline. Freeze all weights and gates before the final
cutoff.

### Best existing anchors

1. Rouse/sediment dimensionless collapse;
2. strength→gravity→spin rock rung staircase;
3. ocean capillary→gravity→atmospheric restoring ladder;
4. nasal–ENSO paired anti-phase relation class.

### Minimal test design

- Choose one transition coordinate `q=driver/incumbent`.
- Fit or declare one bounded mapping on rung 1.
- Apply unchanged to rung 2.
- Freeze before an untouched rung 3.
- Allow a declared projection transformation but no new center or width.

### Falsifier

Every rung requires its own arbitrary ratio, center, response definition, or post-hoc pairing.

### Warning

Dimensionless analysis often produces collapse by standard mechanics. The ARA-specific contribution must be stated before running.

---

## A1 — Atmosphere–space exobase handover

**Status:** `PARKED`; not yet added to the water preregistration.

### Dylan's intuition

The edge of the atmosphere acts as a weaker restrictor/net, with greater travel potential and some leakage.

### Physical translation

The upper atmosphere has no molecular surface-tension membrane. Relevant competitions are:

1. collisional collective gas ↔ collisionless ballistic particles;
2. gravitational binding ↔ thermal/solar escape.

### Coordinate candidate

\[
\mathrm{Kn}=\ell/H,\qquad x=2\mathrm{Kn}/(1+\mathrm{Kn}).
\]

`Kn=1` is the exobase-style handover where mean free path and density scale height are comparable.

### Test candidate

Across planets/moons and atmospheric species, use independently measured density, temperature, gravity, and collision cross-sections. Predict the observed transition altitude or escape regime without fitting the center.

### Controls

- standard exobase/Jeans escape models;
- temperature-only and gravity-only;
- species mass;
- magnetic/solar-wind effects reported separately.

### Evidence fence

Rewriting the Knudsen number on 0–2 is an anchor. A new result requires a prediction beyond the standard model.

### Kill condition

Across held-out planets, moons or species, the bounded ARA expression adds no predictive or calibration value beyond
standard exobase/Jeans escape models and produces no predeclared residual pattern. In that case retire the proposed
ARA mechanism here and retain only the Knudsen-number anchor.

---

## P1 — Phi interfacial-weave hypothesis

**Status:** `PARKED` ontology; high risk of phi hunting.

### Claim candidate

Identity boundaries are permeable interface sheets. Their measured slices appear as filaments. Phi may regulate the transfer/retention allocation that preserves separate identities across repeated exchange.

### Established part

- phase-field interfaces;
- Voronoi/power-diagram boundaries;
- foam/tissue/polycrystal sheets;
- flux through semipermeable boundaries;
- a lower-dimensional slice of a sheet is a line.

### Unestablished part

No evidence currently shows that generic identity interfaces are phi-shaped or phi-weighted.

### Candidate observable

\[
h=\Phi_{transfer}/(\Phi_{transfer}+\Phi_{retained}).
\]

Choose **one** expected value in advance: `1/φ`, `1/φ²`, or a geometrically derived alternative. Do not test all and select the nearest.

### Rivals

`1/2`, `1/e`, `3/8`, `2/5`, system-specific optimum, and a fitted free optimum.

### Possible first systems

- coupled oscillators with tunable permeable boundary;
- chemical exchange between two stable phases;
- biological membrane exchange with recoverable identity metric;
- foam/cell-boundary rearrangement, if transfer can be measured independently.

### Falsifier

The optimum varies freely by system, is broad over all crowded constants, or a simpler balance/energy model predicts it completely.

### Beeswax note

The prior `1/φ` edge weighting and 6/7 split robustness is hypothesis provenance, not independent validation.

---

## P2 — Phi as identity-preserving non-recurrence

**Status:** `PARKED`, mathematically anchored.

### Anchor

Golden rotation avoids exact repeat and is maximally resistant to short rational approximations.

### Claim candidate

For repeated exchange where both transfer and individuation matter, the joint performance optimum occurs near the golden phase advance.

### Score candidate

\[
Q(\alpha)=T(\alpha)P(\alpha)-R(\alpha).
\]

Define transfer `T`, persistence `P`, and recurrence cost `R` independently before looking at the optimum.

### Required controls

- rational rotations `1/2`, `1/3`, `2/5`, `3/8`;
- random irrational steps;
- system-specific fitted optimum;
- noise and finite-length recurrence effects.

### Falsifier

Phi does not outperform nearby convergents or the optimum moves with arbitrary scoring weights.

---

## S1 — Confinement sphere and rung-relative collapse

**Status:** `PARKED`; requires terms before data.

### Dylan's claim candidate

A weaker identity unable to compete with higher-rung external coupling retreats into its own finite 2ARA energy bubble. Strong deformation is a local identity collapse/reformation, and sufficiently strong forcing destroys the identity.

Canonical refinement: a minimally isolated identity has spherical reference closure. Every observed deviation
must be assigned before scoring to resolved internal structure or external coupling; shape is a multipole/
deformation of that reference, not a different base object.

Direction refinement: ARA builds the closure bottom-up from minimal component ARAs and their relations. The sphere
is `Close(C({ARA_i},{J_ij}))`. Top-down deformation is only the inverse reconstruction used to infer which internal
and external couplings produced the observed slice.

### Required definitions

1. identity invariant;
2. configuration state;
3. recovery time;
4. reversible deformation;
5. plastic remodeling;
6. irreversible collapse;
7. rung at which each is judged.

### Candidate balance

\[
P_{self}(R)=P_{outside}+2\gamma_i/R.
\]

### Test ladder

- isolated-source multipole ladder: spherical monopole reference, declared intrinsic dipole/moment, external-field
  deformation;
- droplet under isotropic and directional forcing;
- cell under reversible compression;
- membrane-disrupting exposure as collapse condition;
- compare recovery after force removal.

### Falsifier

“Collapse” is assigned to every state change with no invariant or recovery criterion, making persistence unmeasurable.

Base-sphere falsifier: an isolated system retains reproducible nonspherical structure not predicted by any
predeclared internal degree, boundary or external coupling. Do not rescue it afterward with an unmeasured hidden
coupling.

---

## B1 — Body–Earth coupling graph

**Status:** `PARKED`; low-cost conceptual cleanup.

### Repair

Replace direct muscle→plate value translation with:

\[
\text{energy source}\to\text{active driver}\to\text{transfer boundary}\to\text{rigid structure}.
\]

Body: metabolism→muscle→joint/tendon→bone.  
Earth: heat/gravity→mantle/slab/ridge force→fault→plate.

### Next action

Create one relational graph with node/edge roles and map only dimensionless role measures. Do not translate muscle metres/second directly into plate metres/second.

### Evidence condition

The mapping becomes useful only if it predicts a previously unspecified node, direction, failure mode, or dimensionless relation.

### Kill condition

The role-matched graph predicts no held-out node, direction or failure beyond a generic network template, or its score
is reproduced after shuffling the body/Earth role labels. In that case retire the cross-domain ARA claim while
retaining the graph as an analogy.

---

## X1 — Hidden pairs behind apparent singletons

**Status:** `PARKED`; strong falsifiability risk.

### Claim candidate

Some apparent single structures are lower-dimensional projections of two complementary/anti-phase modes.

### Acceptable pair evidence

At least one must be signed before lookup:

- duplicated developmental origin;
- conjugate flow;
- two stable dynamical modes;
- anti-correlated response;
- input/output closure;
- missing partner predicted and independently located.

### Instrument idea

Use delay embedding or multichannel system identification to compare latent rank 1 versus latent rank 2 on held-out data.

### Falsifier

Rank 1 explains the data and no independent partner signature appears. Do not rescue with an unmeasured higher dimension after the result.

---

## L1 — Lightning–neuron empirical waveform replication

**Status:** `PARKED/LOW`; existing test mixed.

### Existing result

Synthetic canonical action potential ranked `5/10` against lightning; correlation about `+0.492` after alignment.

### If revisited

- use measured waveforms, not hand-built templates;
- match the same observable—current with current or voltage with electric field only through a derived transformation;
- predeclare alignment and normalization;
- include a broad library of excitable and generic pulses;
- hold out event classes and sources.

### Falsifier

Neural pulses remain no closer than generic sharp-pulse nulls.

---

## D1 — Dark-sector geometry

**Status:** `PARKED`; provenance preserved.

### What is established in the record

Dylan supplied the `2φ + diagonal singularity crossing` explanation and the paired Space/Time, Light/Dark, Information/Matter framing before the AI's later formalization.

### What remains missing

- parameter-free derivation;
- independent observable not used to choose exponents;
- redshift/evolution prediction;
- comparison with standard cosmological models and uncertainty;
- a kill condition that does not reinterpret every ratio as another projection.

### Whole-cosmos ARA path musing — 12 July

Dylan proposes a high-density/small-scale Big-Bang source progressing toward a large-scale/dilute pole and then
flipping into anti-phase. Preserve the physically corrected pole wording: component energy **density**, not global
total energy, falls as space expands. For each separately conserved component,
`d lnρ_i/d ln a=−3(1+w_i)`; radiation, matter and cosmological-constant density therefore follow different paths.

Candidate instrument: `z_i=ln(a/a_*)−α_i ln(ρ_i/ρ_i,*)`, compactified to `x_i=1+(2/π)atan(z_i)`. Predeclare the
density component, `α_i`, reference epoch and comparison cosmology.

Operational flip choices:

- acceleration-regime handover: `q=−aä/ȧ²` crosses zero while `H>0`; this has occurred observationally and is not
  a reversal of expansion;
- expansion→contraction turnaround: `H=ȧ/a=0` with `Ḣ<0`;
- contraction→expansion bounce: `H=0` with `Ḣ>0`;
- another claimed anti-phase observable must be named separately.

Current accelerated expansion and DESI's still-inconclusive hints of evolving dark energy do not establish a future
turnaround. A prospective ARA claim must sign the affected observable and transition condition before later data.

### Re-entry condition

Return only when one quantity can be predicted from geometry before consulting its observational value.

---

## G1 — Triangle→hexagon→pentagon slice mechanism

**Status:** `PARKED`.

### Proposed chain

- triangle: smallest closed connection map;
- hexagon: paired/anti-paired three-way structure under efficient planar packing;
- pentagon: a neighbouring/full structure forced into an incomplete slice, with closure/webbing cost and phi geometry.

### Exact anchors to keep separate

- three nodes form the smallest graph cycle;
- equilateral triangles generate hexagonal tilings;
- equal-area planar partition favours hexagonal cells;
- regular pentagon/pentagram contains exact phi ratios;
- cut-and-project methods can map higher-dimensional lattices to quasiperiodic lower-dimensional slices.

### Missing bridge

Specify one full parent geometry, one slice/projection operation, and one predicted pentagonal observable. “It looks like a cut” is insufficient.

### 12 July dimensional/mixing-depth musing

Preserve three coordinates separately: recursive mixing depth `k`, visible cycle order `n`, and physical/topological
dimension `d` (plus embedding dimension `D`, projection `Π`, and orientation `σ`). More polygon edges do not alone
increase dimension: every unfilled `C_n` is a one-dimensional cycle. Filled faces and their attachment raise the
dimension.

Two exact sixes must not be merged without a derived map:

- phase triangle plus anti-phase triangle gives the six **vertices** of `C_6`;
- the minimum triangulated sphere `∂Δ³` has four triangular faces and six **edges**.

Competing parked models:

1. full closure `C_6` is human-projected/webbed to visible `C_5`, so Time is fundamentally six-like but appears five-like;
2. Time and Space are adjacent fifth/sixth mixing depths, so `n=k` locally;
3. either assignment may reverse if the external up/down orientation is reversed.

The Big-Bang direction proposal sets a singular origin `k=0` and treats our apparent level as the sixth mixing down.
Before testing, name all six mixing events and predeclare an observable relic for each. Do not call `k=6` a sixth
physical dimension unless an independent transformation `d=d(k)` is supplied.

### Directional-chart correction

The coordinates above are bookkeeping views, not separate ARA objects. Dylan's intended ontology is one fractally
recurring ARA relational form with different local identities. Formalise an occurrence as `𝒜[Ω]` and obtain its
reported coordinates through a declared observer/direction chart `Ξ_(O,d)=χ_(O,d)(𝒜[Ω])`.

Minimum orientation laws:

- pole reversal: `x_(Q→P)=2−x_(P→Q)`;
- rung reversal: `Δk(A,B)=−Δk(B,A)`;
- chart change may alter labels, signs and projection, but must preserve declared opposition, adjacency and closure
  invariants.

Fractal covariance should be stated as `ℛ_k(𝒜[Ω_k]) ≅ 𝒜[Ω_(k+1)]`: same ARA relational form, different identity and
scale. Do not reclassify mixing rung, closure order and observed slice as distinct ontological ARA types.

### Polygonal resolution ladder musing

Candidate graph spine: `G₁=K₁` (identity node), `G₂=K₂` (single coupling snapshot), and `G_n=C_n` for closed
sections `n≥3`. Proposed semantics: `3=Information³ closure`, `4=four sign quadrants`, `5=Time/handover/irregular
weave`, `6=concrete connection/phase-plus-antiphase`, and `7+` unknown.

Controls:

- one node has identity but no measurable ARA relation;
- one edge attached to a closed cycle makes a branch, not the next regular polygon;
- obtain `C_(n+1)` by an operation that adds a vertex while reclosing the perimeter;
- regular `C_n` sections approach `S¹` as `n→∞`; the sphere requires closing those sections across orientations;
- do not equate polygon order with physical dimension;
- keep the existing `C₆` construction distinct from hexagonal packing produced when coupled circles settle.

Prospective seven test: specify whether the prediction is a closed `C₇`, a `C₆` plus one branch, or another seven-
part graph; then predict an observable adjacency/closure pattern before choosing a physical example.

### Meta-method gate — geometry-first inverse theory construction

Treat Dylan's “speed-running physics backwards” as a discovery method with two separate channels:

- **discovery:** loose ARA geometry walk, AI translation, broad search and musing;
- **confirmation:** frozen signed feature vector before lookup/data, explicit nulls, non-ARA baselines and provenance.

For every claimed recovery, assign a level: `0` resemblance, `1` broad structural recovery, `2` specific pre-lookup
localisation, `3` fixed-rule equation/parameter-free recovery, `4` withheld novel prediction, `5` independent
replication. Do not use a Level-1 physics match as evidence equivalent to Level 4.

Minimum frozen feature vector: objects, relation/mode order, direction/sign, phase condition, scale transformation,
failure region and null model. Label every step `USER PRIOR`, `AI TRANSLATION`, or `EXTERNAL LOOKUP`. Use transcript
timestamps/hashes wherever priority matters.

### Musing fence — subjective, social and metaphysical-domain identities

Dylan's universal-domain claim includes dreams, customs, beliefs and phenomena commonly labelled metaphysical. Keep
three layers separate: `(1)` the experience/custom/report occurs in the universe; `(2)` observable neural,
physiological, behavioural, linguistic and social projections can be measured; `(3)` the preferred external or causal
ontology requires its own discriminating prediction.

Candidate observation model: `Y_m=Π_m(𝒜[X])+ε_m`. Predeclare each projection/distortion operator; do not rescue every
mismatch by saying the underlying ARA was hidden. Cross-domain `x_D=f_D(Y_D)∈[0,2]` values are comparable only after
measurement-invariance tests preserve pole order, reversal, ridge, phase, closure and intervention response.

Blind first applications should use established datasets and baselines: sleep physiology plus timed dream reports,
or adoption/decay of a documented custom. Do not begin with adjudicating an external metaphysical cause. Social tests
must model reflexivity because publication/prediction can change the measured system.

### Coupling-distance refinement

Dylan reframes unequal epistemic access as unequal coupling distance. Candidate graph metric for normalized coupling
strengths `j_e`: `d_J(X,H)=min_(γ:X→H) Σ_(e∈γ)(−log j_e)`, supplemented by bandwidth, delay and transduction. All
ordinary perception is mediated; “direct” means a shorter/stronger usable path, not unmediated contact with matter.

Provisional human closure: `𝒜_H=𝒞_H(B_S,P_T,J_ST^H)`, where body/substrate is Space–Connection–Matter leaning and
work/processing/communication is comparatively Time–Information leaning. Do not equate physical work with
information transfer without a bridge.

Consciousness musing: distinguish first-person projection `Π_C^(1p)` from third-person neural/behavioural projection
`Π_C^(3p)`. “Experienced but not seen” is compatible with that asymmetry; it does not establish consciousness as a
separate substance or external signal beyond sensory bandwidth.

Archived Script 135 is conceptual provenance only: criteria scores and thresholds were assigned and its measurement
protocol was missing. Apply the translation-fidelity protocol before any new consciousness test.

### Dylan correction — sensory range is a transduction path, not coupling existence

Strong bodily coupling does not imply perception. Model a sensory channel as `Y_s(ω)=G_(γ,s)(ω)X(ω)+N_s(ω)` and
detectability through an SNR/encoding threshold. A phenomenon may lack an evolved receptor, lie outside bandwidth,
attenuate below noise, be adapted/gated out, or be masked by a competing same-channel component while still existing
and coupling elsewhere in the organism.

Use `G_eff=G₁+G₂e^(iΔφ)` with `Δφ≈π` only for declared shared-channel destructive interference. Do not use generic
“anti-phase blockage” to rescue every absent signal. Evolutionary explanations must compare fitness benefit,
energetic/developmental cost, ecological availability, noise and phylogenetic constraint.

Fidelity status for current musing: `USABLE WITH CORRECTION`; revised back-translation is recorded in §41 of the
conversation record. No consciousness test is authorised by this musing status.

### Updated basis-mixture reading

The failed 60°→72° lock-angle dial does not test the newer claim that hexagonal/Space and pentagonal/Time are two
basis geometries mixed at every local ARA position. In discrete matter, predeclare bond-order parameters `ψ6` and
`ψ5` and test a fixed mixture coordinate such as `x=2|ψ5|/(|ψ5|+|ψ6|)`. This instrument is not automatically a
spacetime coordinate. A spacetime version must define a map to `g_μν` and recover Lorentz/GR limits.

### Leak mechanism alternatives

Keep distinct: `G=2-φ`, normalized `g=G/2`, rung-local irrecoverable fraction `λ`, old model-specific crossing tax
`κ≈0.132`, and the exact `60°` angular defect. Test one of four signed models: same toll in multiple coordinates;
serial costs; mismatch as recyclable diversion with only `λ` lost; or holonomy/gate-count staircase. Do not fit a
conversion after seeing outcomes.

### Falsifier

The proposed slice does not mathematically produce the claimed edge loss/phi ratio, or equally simple projections generate many unrelated polygons with no signed selection rule.

---

## Q1 — Quantum/atomic observable-specific ARA cycles

**Status:** `PARKED`.

### Rule before mapping

Every quantum ARA must name:

1. state or superposition;
2. measured observable;
3. cycle frequency/period source;
4. accumulation/release definition;
5. whether phase is observable or only relational;
6. standard quantum baseline.

### First clean targets

- two-level Rabi oscillation;
- quantum beats from a known superposition;
- spin precession;
- driven atomic transition with measured population exchange.

Avoid assigning a classical orbital period to stationary hydrogen `1s`.

### Lyman-α subthread

If testing “EnergyRatio leak,” predict an escape fraction, delay distribution, or line-profile feature beyond standard radiative transfer, using independently measured optical depth and geometry.

### Falsifier

The ARA number is algebraically determined by inputs taken from the same textbook formula and adds no new relation or prediction.

---

## M1 — LLM Information³ and raw coupled modes

**Status:** `PARKED`; all work bound by `LLM_WORK_SAFEGUARDS.md`.

### Claim candidate

Raw generation dynamics contain two coupled modes plus a relation channel; aggregate activation statistics can project them onto a misleading middle.

### Required instrument gates

- known paired dynamical system embedded in synthetic activations;
- known single-mode negative control;
- forced versus free decoding intervention;
- pair recovery before correctness/hallucination interpretation;
- truth/grounding scored separately from structural closure.

### Falsifier

The closure/pair score is equally high for phase-randomized, incorrect, or known single-mode controls.

### Mandatory process

Second non-Anthropic/non-identical model-family review, fixed seeds, open Pythia artifacts, and no reliance on AI introspection.

---

## U1 — Fusion/engineering prospective application

**Status:** `PARKED`; no engineering guidance implied.

### Re-entry conditions

1. choose a narrowly scoped physical observable;
2. use established plasma/nuclear equations and sourced parameters;
3. state the ARA-added prediction before comparison;
4. include expert review and conventional engineering baseline;
5. avoid operational or safety-critical recommendations from speculative geometry.

### Valuable outcome even if null

A prospective engineering test can reveal whether ARA contributes a real control coordinate or only redescribes already-known dimensionless balances.

---

## EM1 — Bound-field↔radiative-light decomposition

**Status:** `PARKED`; strong established instrument, ARA interpretation open.

### Dylan's recovered map

`Electromagnetism = coupling(Matter, Information)` and `Light = coupling(Information, Time)`, with Light proposed
as the anti-phase appearance of electromagnetism. Earlier provenance begins in the 22 April transcript; the 2 July
four-pole grid organizes it, while the deeper 5 July formulation places both inside the crossing Time/Space and
Information/Connection meta-waves plus the pair-resonance/fill term.

12 July hand-drawn refinement: one Space column `{Connection, Dark, Matter}` and one Time column `{Information,
Light, Quantum}`, with proposed cross anti-phase rows `Connection↔Information`, `Dark↔Light`, and
`Matter↔Quantum`. The gradient produces several joint identities, not one output.

### Coherent Maxwellian translation

Interpret “electromagnetism” narrowly as source-bound/static/reactive near field, and Light as the radiative
far-field sector of the same electromagnetic field:

\[
M\xleftrightarrow{EM_{bound}}I\xleftrightarrow{L_{rad}}T.
\]

Do not describe Light as anti-phase to the whole electromagnetic field; Light is electromagnetic radiation.

### Candidate instrument

For a fixed antenna/dipole convention,

\[
Q_{EM}=\frac{\omega U_{reactive}}{P_{rad}},
\qquad
x_{EM}=\frac{2}{1+Q_{EM}}.
\]

Measure energy and information separately. `P_rad` is radiated power; information must use a declared signal and
channel statistic. `x=1` is only the coordinate's equal reactive/radiative power-scale point until an ARA-added
ridge prediction is signed.

### Hierarchical-coupling clarification before testing

Resolved on 12 July:

1. **Yes:** `Connection + Dark → Matter` describes the proposed Space-side generative closure.
2. **Yes:** `Information + Light → Quantum` describes the proposed Time-side generative closure.
3. `Information³` marks the minimal three-relation closure at which a persistent identity can spawn; it is not a
   numerical cube of an information scalar and not the whole sphere.

Still to resolve:

1. Does “below” mean faster/smaller rung, child identity, or deeper decomposition?
2. Is the drawn `1.0` ridge the Matter↔Quantum meeting, the closure of all three cross-pairs, or a separate
   meta-level projection?
3. What coupling threshold `J_*`, phase tolerance `ε_*`, and persistence duration operationally distinguish a
   closed identity from a transient triangle?

Only after that should `J_n^eff=J_n^direct+K_(n+1→n)+ΣK_neighbour` be assigned physical observables.

### Cheapest clean system

Use a driven dipole/antenna or coil with measured spatial near-to-far field maps across frequency. Compare:

- static magnet/coil control;
- reactive near field;
- transition zone;
- radiative far field;
- material perturbation with and without an iron core;
- matched power but different modulation/information controls.

### Magnetic-pole/singularity fence

North/south are opposed dipole poles under `∇·B=0`, not physical singularities. If testing singularity geometry,
use independently identified magnetic nulls, separatrices or reconnection, and score topology change plus released
stored energy against ordinary electromagnetic baselines.

### Slice rule

Field lines are integral curves on a declared coordinate-time slice. “Instantaneous” may describe the drawing but
must never imply instantaneous causal propagation. Compare successive slices with retarded/causal Maxwell
solutions, and declare the observer frame when simultaneity matters.

### Space–Time field/hypotenuse subthread

For a wave, freeze `F(r,t)=A cos(k·r-ωt+φ0)` and use the joint phase `k·r-ωt` as the relational reading. Along a
moving probe, test the exact split `dF/dt=∂F/∂t+v·∇F`. This distinguishes local time evolution from movement
through spatial structure. The invariant interval `Δs²=c²Δt²-|Δr|²` is the geometric relation of the two legs,
with the Lorentzian sign preserved.

### Falsifiers

- the ARA coordinate adds nothing beyond ordinary antenna `Q`, impedance and distance;
- energy radiation and measured information transfer do not share the proposed ordering;
- the claimed anti-phase disappears when bound and radiative sectors are separated correctly;
- results depend on choosing the field boundary or stored-energy convention after inspection.

---

## O1 — Orbital scale covariance and physical-law crosswalk audit

**Status:** `PARKED`.

### Anchor

Newtonian two-body orbits preserve their form under `r→λr`, `t→λ^(3/2)t` at fixed total mass. The shared orbit is a measurable relation-born object of two masses.

### Two distinct questions

1. Does the same gravitational relational topology recur across nested orbital rungs beyond what the inverse-square law already guarantees?
2. Are fundamental laws unusually rich in the full ARA signature, rather than merely expressible with three symbols?

### Anti-cherry-picking audit

- predeclare a textbook/source list of fundamental laws;
- score blinded equations using a fixed rubric: identifiable pair, coupling, emergent joint mode, accumulation/release, ridge/orientation, scale transform;
- compare against algebraically rearranged forms and non-dynamical definitions;
- report the base rate of generic triadic equations.

### Canonical source document completed

`ARA_PHYSICAL_LAWS_CROSSWALK.md` now provides the Version-1 textbook-law sampling frame and evidence labels. It
covers the main canonical law families and records unmapped laws rather than force-fitting them. It is a source
atlas, not a passed empirical test; O1 remains parked until a blinded scoring protocol is activated.

### Newton-III ridge subthread

For a Newtonian interaction pair, `F_AB=-F_BA`. The forces act on different bodies, so their exact cancellation is
visible in the enclosing pair's momentum balance, not generally in either body's local motion. Candidate ARA
reading: `x=1` is active reciprocal exchange with zero net internal contribution at the declared system boundary.

Controls must include:

- an open boundary with external force;
- delayed or field-mediated exchange, where the carrier stores momentum;
- nonreciprocal active-matter/effective interactions;
- equilibrium detailed balance with nonzero microscopic flows.

The crosswalk itself adds no new prediction to Newton's law. An ARA-added result must connect this ridge to a
predeclared accumulation/release, relaxation, phase, or cross-scale outcome not guaranteed by momentum
conservation.

### Mandatory measurement declaration for O1

Every scored law appearance must declare `(Ω, q, τS, Π, σ)`:

- `Ω`: enclosing identity/system boundary;
- `q`: measured observable;
- `τS`: observation or interaction slice;
- `Π`: projection/coarse-graining, including whole-system versus relative mode;
- `σ`: 0↔2 pole orientation.

Do not score “the ARA of an object” as if it were context-free. For the skaters, centre-of-mass momentum,
individual acceleration, relative separation, and contact impulse are separate lawful readings.

### Orbit-specific ARA-added target

Standard Kepler scaling is an anchor, not a new prediction. An ARA test must predict a coupling transition, hierarchy statistic, deviation, handover, or coarse-grained invariant that is not inserted through Newtonian inputs.

### Force-identity subthread

Dylan proposes a deeper `Connection ↔ Information transfer` ARA whose classical slice appears as mass, acceleration, and force. Preserve this as ontology rather than reverting immediately to the textbook causal reading.

Required bridge:

- independently define connection/storage `C`;
- independently define transfer/traversal `T`;
- declare the information measure and its units;
- derive `F=dp/dt`, not only constant-mass `F=ma`;
- test variable-mass, constrained, and field-mediated cases where a mere relabelling of `m` and `a` is insufficient.

The strongest established anchor is momentum flux/transfer across an interaction boundary. The ARA-added claim must predict how connection topology changes that transfer.

Mass refinement: use `MΩc²=EΩ_COM` as the standard boundary account. The ARA-added claim is that independently
measured connection state selects the projection we call mass/inertia and predicts transfer response; it must not
define Connection merely as the observed mass it is meant to explain.

### Falsifier

The “ARA-shaped law” classification changes under trivial algebraic rearrangement, or generic textbook equations score equally well without dynamical pair structure.

---

## F1 — Deliberate future prediction programme

**Status:** `PARKED by Dylan`.

The next prediction model should not be another variant selected on reused ENSO or cardiac windows. Re-entry requirements:

1. choose system boundary and coupling graph first;
2. choose untouched target data;
3. freeze the mathematical operator;
4. include strong domain baselines;
5. separate structural, directional, and point-value targets;
6. use one active registered test at a time.

The best unresolved historical cardiac headline has already received the necessary post-leak replication and did not generalize. Future cardiac work should therefore activate H1/H2 rather than rerun the old scalar model.

---

## Parking-lot notes that must not be lost

- Ocean/atmosphere and atmosphere/space are two separate boundary decompressions.
- Density may be a local order parameter or rung-related coupling variable, not a universal ARA pole by itself.
- At `x=1`, equal participation does not uniquely imply cancellation; check variance and local coupling sign.
- A sphere's observed morphology may encode both self-localization and environmental constraint; define the judged rung.
- `1+1=3` is relation information, not automatically a third material component.
- The “phi filament” idea must compete against generic interface geometry and ordinary quantum/electromagnetic contact physics.
- Paired senses are an Information³ anchor but bilateral evolution is the null.
- BP's value can be unmixing, output memory, feedback, or all three.
- Raw data is not automatically better; it must beat phase-preserving preprocessing and resist noise/overfit controls.
- The `275°` racing-cam complement gives a golden arithmetic ratio but is not established as a universal or
  Formula-1 optimum; see `EnergyRatio/CAMSHAFT_PHI_PROVENANCE_AUDIT.md`.

## One-paragraph restart prompt

> Read `CANON_FOR_AI.md`, `ARA_CONVERSATION_RECORD_2026-07-12_CODEX_GEOMETRY_DRILL.md`, and this register. Treat all items here as PARKED unless explicitly activated. Continue helping Dylan walk the geometry, translate it into compact standard mathematics, challenge category slips, and capture every correction or new landing immediately. The highest-priority deferred empirical thread is H1: test whether RR is a mixed projection of right/left cardiac dynamics and whether resolving those dynamics removes blood pressure's incremental predictive value.

---

## C2 — Consciousness as an Information/Quantum-leaning coupled identity

**Status:** `MUSING / PARKED`; fidelity `UNSURE / KEEP AS MUSING` pending clarification of “collapse.”

Current faithful reading:

- body, brain anatomy, membranes and connectome are Space/Connection/Matter-leaning substrate;
- electrical and chemical **changes/fluxes** are Time/Transfer/Information-leaning dynamics, while their physical
  carriers and stored structures remain material;
- consciousness is proposed as a coupled identity leaning toward Information/Quantum, not a separate passenger;
- drilling downward from neural dynamics reaches quantum descriptions of ions and molecules, but this alone is not
  evidence for a consciousness-specific quantum mechanism;
- “pure information” is currently an ARA limiting-pole proposal, not an established free-standing physical substance.

### Required clarification

Confirm whether “collapses further” means `decompose/zoom down the fractal rungs` (the interpretation currently
recorded) or `physical wavefunction collapse`. Never substitute one for the other.

### Future discriminating test, only if activated

Predeclare a named neural carrier, a specifically quantum observable, its required lifetime/scale, a conscious-state
contrast, classical electrochemical controls, blinded analysis and a result that would narrow or reject the claim.
Generic quantum foundations of chemistry cannot count as confirmation.

---

## E1 — Universal-scope, evidence-tier discipline

**Status:** `METHODOLOGY / ACTIVE`.

ARA does not exclude dreams, consciousness, customs, anomalous experiences or metaphysical interpretations merely
because they are culturally labelled “woo.” They may be recorded as reports, phenomenological patterns and candidate
domains of a proposed universal geometry.

For every such study, keep distinct:

- occurrence/report;
- measurable pattern;
- proposed causal mechanism;
- ontological interpretation.

Require a declared observable, measurement channel, ARA invariant, domain-appropriate null models and a possible
result that narrows the mapping. Never treat the reality of an experience as automatic confirmation of its preferred
cause, and never treat uncertainty about cause as grounds to erase the experience.

---

## T2 — TE-ARA normalised whole-sphere budget

**Status:** `DEFINED ARA COORDINATE / PHYSICAL GENERALITY UNVALIDATED`; fidelity confirmed by Dylan in the latest
definition below.

Candidate definition: for opposed actual component energies \(E_S,E_T\ge0\), retain their physical total
\(E_\Sigma=E_S+E_T\), and define normalised shares \(s=2E_S/E_\Sigma\), \(t=2E_T/E_\Sigma\). Then
`TE-ARA = s+t = 2` and, with Space oriented to 0 and Time to 2, the position is \(x=t\), \(s=2-x\). The ridge is
\((1,1)\).

Do not let current-content normalisation hide depletion. If an independently defined physical capacity \(E_{cap}\)
exists, also record occupancy \(\rho=E_\Sigma/E_{cap}\) and capacity-relative shares
\(\hat s+\hat t=2E_\Sigma/E_{cap}=2\rho\). Full TE-ARA is then \(2\rho=2\), while loss without replacement gives
\(2\rho<2\).

Guardrails:

- TE-ARA is not the existing ARA waveform-shape scalar;
- `2` is dimensionless normalisation, not two joules or a demonstrated universal maximum;
- a physical capacity \(E_{cap}\) needs an independent law;
- an open identity obeys \(dE_\Sigma/dt=P_{in}-P_{out}\), so full capacity requires replenishment rather than being
  guaranteed;
- Gauss electric flux measures net charge, not TE-ARA; test the connection later against electromagnetic energy
  density and Poynting continuity.

Decisive guardrail: an enclosed electric dipole and an empty region in a uniform external electric field can each
have zero net Gauss flux but nonzero field energy. Never infer joules or capacity from \(Q_{net}/\varepsilon_0\).

Treat TE-ARA provisionally as the whole ledger
\((E_{cap}[\mathrm J],E_N(t)[\mathrm J],\rho=E_N/E_{cap},x\in[0,2])\), with actual joules and dimensionless
composition kept separate.

### Dylan's unit correction

TE-ARA is intended more like a calibrated node-equivalent or pole-to-pole transfer unit: “20 TE-ARAs” means twenty
reference-node equivalents in the declared region/rung, not merely a normalised fill coordinate. Candidate definitions
are \(1\,\mathrm{TE\!-\!ARA}^{cap}_{k,\alpha}=E^{ref}_{cap}(k,\alpha)\) or
\(1\,\mathrm{TE\!-\!ARA}^{cycle}_{k,\alpha}=\int_{cycle}P_{\partial N}dt\). Do not infer literal constituent count
from energy-equivalent count unless same-rung node budgets and coupling-energy terms are independently known. Do not
double-count coarse nodes and their fine constituents.

### Reconciled energy-axis reading

Local TE-ARA coordinate: \(a_E=2E_{id}/E_{cap}\). Regional same-rung equivalent count:
\(N_{TE}^{eq}=E_\Omega/E_{cap}^{ref}\). Keep separate: capacity fill \(E_{id}/E_{cap}\), Phase-A/Phase-B composition,
and main-wave participation \(E_{id}/E_{total}\). For quadratic wave energies, test the decompression
\(E_{id}=E_A+E_B+E_{AB}\), where \(E_{AB}\) is a signed interaction/cross term rather than automatically a third
node. Fidelity awaiting Dylan confirmation.

### Authoritative definition after Dylan confirmation

TE-ARA names main-identity energy participation:
\(\mathrm{TE\!-\!ARA}=2E_{id}/E_{total}=2p_{id}\). ARA itself gives the Phase-A/Phase-B mixture location. Report
`TE-ARA = 1.24 (62% identity participation)`, never an unlabelled `0.62`. Capacity fill and regional equivalent count
remain separate coordinates and must not be called TE-ARA. Predeclare the identity modes, included interaction term,
harmonics, boundary and time slice; use joules only when the signal has a physical energy calibration.

### Gauss placement mnemonic

Gauss is a signed balance, not activity turnover: an empty sphere and a sphere enclosing \(+q\) plus \(-q\) both have
zero net electric flux, although the latter can contain strong fields and energy. Interpret Gauss as net enclosed
source asymmetry \(Q_+-|Q_-|\). Use field energy for TE-ARA and Poynting continuity for boundary energy transfer.

### Candidate ARA × TE-ARA aggregation law

With Phase A positive at \(x=2\) and Phase B negative at \(x=0\), test
\(j_{net}=\tfrac{\mathrm{TE\!-\!ARA}}{2}(x-1)+j_{other}\). ARA supplies centred direction/asymmetry; TE-ARA/2
supplies main-mode participation; independently measured same-channel nearby couplings supply \(j_{other}\). Do not
compare ARA and TE-ARA numerically as a threshold. Exact Gauss analogue:
\(\Phi_E=(\Phi_{out}+\Phi_{in})(x_\Phi-1)=Q_{inside}/\varepsilon_0\), where
\(x_\Phi=2\Phi_{out}/(\Phi_{out}+\Phi_{in})\). Fidelity confirmation and cross-domain validation remain pending.

---

## MX1 — Gauss ↔ ARA/TE-ARA public-data crosswalk

**Status:** `DRAFT TEST / V2 FIDELITY PENDING / CONFIRMATION SEALED`; Dylan marked v1 usable with correction before
formal registration.

Objective: calculate Gauss net flux and an independently defined bottom-up ARA position, TE-ARA identity-energy
participation and Other-coupling term on the same time-resolved electromagnetic system. Fit a dimensionally valid
bridge on training cases, freeze it, then test held-out radii, boundaries, source configurations, times and datasets.

Analytic anchor for an isolated conducting sphere:
\(\Phi_E=\operatorname{sgn}(Q)\sqrt{8\pi R U_E/\varepsilon_0}\). This shows that sign/orientation, energy magnitude
and geometry can jointly reconstruct Gauss flux in a declared special case.

Data requirements: 3D \(\mathbf E(\mathbf r,t)\), charge density/particles, geometry/permittivity and enough time
resolution for ARA cycles; preferably \(\mathbf B\) for full electromagnetic energy/Poynting follow-up. Use disclosed
simulation grids first, experimental multi-point data second.

Success requires one low-parameter bridge to transfer prospectively and add information beyond energy, geometry and
standard electrostatic baselines. A same-field fitted crosswalk is descriptive until held-out transfer succeeds.

Development: Alves OSIRIS two-stream data, DOI `10.5281/zenodo.7968601`, already inspected and therefore calibration
only. Confirmation: Tang–Wu–Tao two-stream data, DOI `10.5281/zenodo.3696310`; metadata selected but arrays must remain
unopened until fidelity sign-off and ledger registration.

Sharpened test: in Fourier space Gauss is \(\widehat\rho=ik\widehat E\). Test the established full-field anchor, then
a frozen identity-harmonic/TE bridge, then the compressed ARA + TE-ARA + phase + rung bridge. Proposed identity is one
complete positive/negative electrostatic wave pair, with signed half-waves measured separately to avoid ridge
flattening. See the two `Analysis/electromagnetism/MX1_*_DRAFT.md` documents.

V2 correction: also measure component waveform ARAs \(a_+\) and \(a_-\), plus pair source-composition
\(x_Q=2Q_+/(Q_++Q_-)\). Exact Gauss embedding:
\(\Phi_E=(Q_++Q_-)(x_Q-1)/\varepsilon_0\). TE-ARA remains energy participation; test rather than assume its bridge
to total source magnitude \(Q_++Q_-\). Dylan approved `MX1-v2` as `EXACT ENOUGH TO TEST` on 12 July 2026 and requested
that the TE-ARA component remain in the test. Development implementation/freeze is now the next gate; confirmation
arrays remain sealed.

Development and freeze completed later on 12 July. On 299 eligible Alves/OSIRIS slices, full Gauss correlation
0.9971 and fixed identity-family correlation 0.9991 are simulator/solver consistency and adapter checks, not
independent ARA evidence. The primary non-tautological ARA-specific result is source-participation TE-ARA correlation
0.7987 with MAE 0.0911 on 0–2. Five phase-aligned cells give local pair-ARA correlation 0.7706, total unsigned source
correlation 0.9997 and signed-net correlation 0.9953. The tested scalar ARA + TE-ARA magnitude model did not beat
the \(k_0E_{\rm rms}\) scale-only control when all models were scored on the same 75 clean held-late slices. This is a
development participation-crosswalk positive and a scalar-magnitude narrowing, not confirmatory evidence. The MAE is
an average coordinate distance, not 4.6% of charge or energy missing. Frozen transfer packet:
`Analysis/electromagnetism/MX1_CONFIRMATION_FREEZE_v1.md`. Ledger entry added; Tang numerical arrays remain sealed.

### TE1-ENSO — mixed-system participation ledger

**Status:** `PROPOSED / NOT RUN`. Treat ENSO as a distributed identity and partition one common signal/variance
denominator among a restrained first set: surface target, warm-water-volume/thermocline reservoir, zonal wind stress,
SOI/pressure partner, declared interaction shares and Other. ARA records signed direction; TE-ARA records participation.
Predeclare lags and boundaries. Primary use is attribution, regime-transition warning and confidence, not an assumption
of exact-value improvement. Require held-out decades and compare against standard regularised regression/attribution.
Participation is not causal until lead order and removal/intervention tests support it.

### TE2-LLM — weak distributed connection ledger

**Status:** `PROPOSED / NOT RUN`. For a declared concept, token prediction or answer behaviour, partition measured
participation across tokens/features/heads/layers, interaction terms and Other. Retain weak links; test whether low-share
connections remain stable across paraphrases, layers, seeds and models. ARA records support/opposition direction;
TE-ARA records participation strength. Attention or activation magnitude alone is insufficient. Predeclare an ablation
or amplification prediction and require held-out intervention effects. Compare direct+relation shares against
strong-edge-only and random-feature controls.

Full synthesis and MX1 results:
`Analysis/TE_ARA_PARTICIPATION_LEDGER_SYNTHESIS_2026-07-12.md`.

### TE3 — recursive Other decomposition and coupling-edge recovery

**Status:** `PROPOSED FORMALISM / NOT RUN`. Treat Information³ as closure/synthesis and TE-ARA as the
opposite-direction decomposition/attribution tool. Begin with a node's primary two-wave identity; place remaining
participation in Other. Search independently measured nearby or cross-rung sources and assign each recovered source
an extra edge with ARA direction and TE-ARA participation weight. Recursively decompose each recovered node. Do not
assume unresolved Other is one source. Test stability under held-out data and interventions. Proposed visual distance:
\(d_e=-\log_2[(p_e+\epsilon)/(p_{\max}+\epsilon)]\); this is not physical distance unless independently calibrated.

### MX2 — is the visible TE-ARA gap a physical handover marker?

**Status:** `DRAFT / DEVELOPMENT FEASIBILITY INSPECTED / INSTANTANEOUS VERSION NULL`. Keep two gaps separate:
\(\Delta_{\mathcal G}=\mathrm{TE\!-\!ARA}_E-\mathrm{TE\!-\!ARA}_{\rho,G}\) is the deterministic \(ik\)
field-to-source projection gap; \(\Delta_{\mathrm P}=\mathrm{TE\!-\!ARA}_{\rho,G}-
\mathrm{TE\!-\!ARA}_{\rho,F}\) compares predicted and particle-measured source participation. Development statistics:
\(\Delta_{\mathcal G}\) mean 0.250, median 0.223; \(\Delta_{\mathrm P}\) mean 0.091, median 0.020 and time correlation
-0.805. Test whether either gap predeclaredly tracks \(J\cdot E\), field-energy change and particle kinetic-energy
change after verifying the phase-space velocity/momentum normalisation. Compare against amplitude/SNR and particle
Other controls. Do not alter frozen MX1 or interpret \(ik\) itself as a newly detected physical node.

Feasibility update: the archive's \(u,v\) arrays satisfy \(v=u/\sqrt{1+u^2}\) to \(1.5\times10^{-8}\). Reconstructed
energy laws pass strongly: \(dU_E/dt\) vs \(-\int JE\,dx\), \(r=0.9839\); \(dK_e/dt\) vs \(\int JE\,dx\),
\(r=0.9831\); total-energy relative range 0.00101. However, instantaneous gap-closing vs particle-to-field power is
near-null (\(r=-0.091\)). Gap state vs field energy is \(-0.725\). Revised target: cumulative/local identity formation
beyond amplitude, SNR and spectral-Other controls. Protocol:
`Analysis/electromagnetism/MX2_HANDOVER_ENERGY_PROTOCOL_DRAFT.md`.

### MX3 — noise-controlled identity-closure convergence

**Status:** `MX3A SAME-DATA DEVELOPMENT RUN / FULL MX3 NOT RUN`. This is the decisive next new test of the potentially large identity-closure
claim. Run the same deliberately seeded two-stream instability across at least four particle-noise levels and ten
seeds per level, preferably with a continuum Vlasov–Poisson reference and one held-out beam configuration. Test
\(C_{\mathrm{id}}=1-|\mathrm{TE\!-\!ARA}_{\rho,G}-\mathrm{TE\!-\!ARA}_{\rho,F}|/2\) against independently labelled
trapped-particle/phase-space-vortex onset. Require convergence beyond \(N_{\mathrm{ppc}}^{-1/2}\), matched-amplitude
separation and held-out value beyond field amplitude and ordinary spectral coherence. Run the already-frozen MX1 Tang
transfer first. Protocol: `Analysis/electromagnetism/MX3_IDENTITY_CLOSURE_CONVERGENCE_TEST_DRAFT.md`.

MX3a used the existing single-noise Alves/OSIRIS archive to measure phase-space organisation. The plasma develops
clear coherent trapped/vortical structure. Across 299 eligible slices, \(C_{\mathrm{id}}\) correlates 0.8428 with
position–momentum mutual information, 0.7612 with velocity-bin phase coherence and 0.7519 with an approximate trapped
fraction, but it also correlates 0.8293 with field amplitude. Eighty pairs matched within 1% field RMS give essentially
no closure-history separation (post-minus-pre \(-0.00080\)) while approximate trapping rises \(+0.02622\). Adding
closure improves held-late approximate-trapping \(R^2\) from 0.7071 to 0.8461, but the run is already inspected and
does not vary particle noise. Closure is now explicitly undefined before coherent-mode eligibility because near-zero
summaries can agree trivially. Full MX3 noise/seed/continuum convergence remains open. Results:
`Analysis/electromagnetism/MX3A_EXISTING_DATA_REPORT.md`.

MX3b tested Dylan's 25-degree heuristic, named before MX3b computation but after earlier exposure to the same session
and archive. It is therefore excluded from blind-provenance claims. On the final 30% development block, the amplitude-plus-mode
baseline scored \(R^2=0.7071\), absolute closure 0.8461, ridge-parallel \(q\) 0.9581 and the 25-degree projection
0.9475. This supports retaining position along the ridge rather than compressing identity to absolute ridge distance.
It does not isolate 25 degrees: pure \(q\) was slightly better, no angle had positive internal-validation \(R^2\), and
a one-to-one matched-amplitude check gave only \(d_z=0.1190\) for 25 degrees versus 0.8796 for approximate trapping.
Relative field-particle phase was nearly fixed. Status: `RIDGE-TANGENT INFORMATION POSITIVE / ANGLE TRANSFER OPEN`.
Results: `Analysis/electromagnetism/MX3B_ANGLED_RIDGE_REPORT.md`.

### MX3c - dynamic daughter-rung angle

**Status:** `DEVELOPMENT RUN / PRESSURE ANGLE NULL / PRESSURE STATE MARKER POSITIVE`. Dylan clarified that 25 degrees was a heuristic visual estimate, not a universal
constant. The proposed viewing angle is itself a wave supplied by the next coupled rung down, analogous to blood
pressure resolving directional heart state. Primary plasma daughter candidate: the pressure/velocity-spread moment of
the phase-space distribution at frozen mode \(k_0=5\). Test whether its relative angle predicts the parent
\((q,d)\) trajectory direction, leads parent turning, improves held-out identity-state measurement beyond magnitude
and fixed coordinates, and survives circular-shift, phase-randomisation and random-angle nulls. Do not optimise the
angle against trapping. Protocol:
`Analysis/electromagnetism/MX3C_DYNAMIC_DAUGHTER_RUNG_PROTOCOL_DRAFT.md`.

Result: pressure-angle/parent-tangent resultant 0.1045, circular-shift \(p=0.9680\), phase-randomised \(p=0.9820\),
and best lag -8 slices at the lagging search boundary. Thus the pressure spatial phase does not supply or lead the
parent direction. Its standalone held-late scalar scored \(R^2=0.9566\), but added no value beyond \((q,d)\), showing
that it behaves as another fixed projection. Pressure magnitude produced strong one-to-one matched-amplitude
hysteresis \(d_z=-0.8276\) but weak continuous held-late \(R^2=0.1972\). General daughter-rung hypothesis remains
open; this observable/angle construction is rejected. Report:
`Analysis/electromagnetism/MX3C_DYNAMIC_DAUGHTER_REPORT.md`.

### MX3d - parent-collision to daughter/echo identity

**Status:** `DEVELOPMENT RUN / 6 OF 8 STRICT CRITERIA PASS / TRANSFER REQUIRED`. Dylan corrected the causal direction: the daughter is born after the parent phase and
anti-phase clash, so it should lag rather than lead. Test parent \(k_0=5\) to primary daughter \(2k_0=10\) using
positive onset lag, phase closure \(2\phi_{k_0}-\phi_{2k_0}\), triad bicoherence, persistence and a separate daughter
ARA/TE-ARA state. Use \(3k_0\), non-triad modes, phase randomisation and circular time shifts as controls. Do not call
it a technical plasma echo unless separate excitation/rephasing criteria are met. Protocol:
`Analysis/electromagnetism/MX3D_PARENT_COLLISION_DAUGHTER_ECHO_PROTOCOL_DRAFT.md`.

Result: \(k=10\) field onset followed parent \(k=5\) by 19 slices; particle onset followed by 31. Phase-closure
resultant rose from 0.2873 baseline to 0.9848 during sub-threshold formation and remained 0.9352 post-onset. Random
phase null \(p=0.0020\); circular-shift null failed \(p=0.5129\). Field bicoherence 0.8376 ranked at the 97.48th
control percentile; particle bicoherence 0.8334 ranked at 94.96%, narrowly missing the 95% gate. Daughter persisted
262 slices, averaged 3.25% of parent field power and had field/particle TE correlation 0.9991. Interpret as strong
development evidence for gradual nonlinear harmonic inheritance, not universal/fractal confirmation. Report:
`Analysis/electromagnetism/MX3D_DAUGHTER_ECHO_REPORT.md`.

### MX3e - daughter-to-grandchild harmonic inheritance

**Status:** `DEVELOPMENT RUN / 8 OF 8 CRITERIA PASS / TRANSFER REQUIRED`. Test of
\(k=10+k=10\rightarrow20\) against mixed \(k=5+k=15\rightarrow20\) and all sum-20 routes. The k20 field/particle
onsets follow k10 by 63/57 slices. Phase concentration rises 0.3146 baseline -> 0.8439 sub-threshold -> 0.8481
post-onset, random-phase \(p=0.0010\). Field/particle bicoherence 0.5760/0.6022, at the 90th percentile of sum-20
routes; the mixed 5+15 route is weaker and phase-null. k9+k11 is stronger, so genealogy is a coupling web. k20
persists 199 slices, has mean field-power fraction 0.001739 and field/particle TE correlation 0.9988. Report:
`Analysis/electromagnetism/MX3E_GRANDDAUGHTER_REPORT.md`.

### MX3f - ARA gradient-coupling web

**Status:** `FORMALISATION DRAFT / NOT RUN`. For each route \(a+b=K\), map composition to
\(x_{a|K}=2a/K\) and its reflection \(2-x\). Weight routes by field/particle bicoherence and normalise into a
coherent-route TE-ARA participation ledger. Current k20 observation: near-ridge 9+11 at x=0.9/1.1 is stronger than
ridge 10+10, while 5+15 at 0.5/1.5 is weaker. Do not infer accumulation/release sign from mode ordering; calculate
signed Vlasov nonlinear transfer. Test rescaled route-profile recurrence across K=10 and K=20, then across new
noise/seeds. Protocol: `Analysis/electromagnetism/MX3F_ARA_GRADIENT_COUPLING_WEB_PROTOCOL_DRAFT.md`.

### MX3g - k40/k80 recursion and floor

**Status:** `DEVELOPMENT RUN / K40 6 OF 8 / K80 FIELD-ONLY / FLIP NOT TESTABLE`. k40 follows k20 by 54 field and
49 particle slices, SNR 14.84, persists 29 slices and has field/particle TE correlation 0.9667. Exact 20+20 route is
weak (bicoherence 0.1873/0.2480, route percentiles 0.20/0.40); k40 is web-generated, with particle coupling strongest
at near-ridge 19+21. k80 has only 3.2 samples/wavelength; field onset occurs but particle onset does not. k160 exceeds
Nyquist. Treat floor as numerical until higher-resolution noise/seed transfer. Report:
`Analysis/electromagnetism/MX3G_GREAT_GRANDCHILD_FLOOR_REPORT.md`.

### MX1–MX3g chain-level disclosure

**Status:** `ONE ADAPTIVE EXPLORATORY CHAIN / NOT INDEPENDENT REPLICATIONS`. Every stage used the same already inspected
Alves/OSIRIS archive, and later questions were informed by earlier results. Preserve the local gates and nulls, but do
not count stages as multiple confirmations. Transfer requires new noise/seeds, resolution and/or an untouched system.

**Follow-up interpretation:** Dylan proposed the fading \(k80\rightarrow160\) continuation as a candidate appearance
of the ARA \(1.75+\) exponential well. The dataset has \(v_{th}=0.04c\), hence approximate
\(\lambda_D=0.04c/\omega_{pe}\), while its measured grid spacing is
\(\Delta x=0.0390625c/\omega_{pe}\). The physical kinetic scale and numerical scale therefore nearly coincide.
Repeat at several grid resolutions with controlled particles per Debye length. Test whether attenuation converges at
fixed physical \(k\lambda_D\) or moves with Nyquist. Before running, freeze an outcome-independent ARA \(x\)-mapping,
the predicted curvature above \(x=1.75\), and the post-crossing phase-flip observable. Status:
`PHYSICAL/NUMERICAL CUTOFF CONFOUNDED / ARA-WELL HYPOTHESIS REGISTERED, NOT CONFIRMED`.

### Maxwell continuation — Gauss magnetic law

**Status:** `TRANSLATION COMPLETED / EXACT CLOSED-BOUNDARY RIDGE`. Resume the one-equation-at-a-time Maxwell
walk after Gauss's electric law with

\[
\nabla\cdot\mathbf B=0.
\]

First explain the established statement: no observed magnetic monopole source, zero net magnetic flux through every
closed surface, and magnetic field lines forming closed loops or continuing through the boundary in balanced
inward/outward amounts. Then let Dylan supply the minimal ARA translation before formalising it. Keep magnetic poles,
magnetic nulls and singularity crossings distinct; north/south labels are not themselves physical singularities.

Translation completed: for outward/inward magnetic flux magnitudes \(\Phi_B^+,\Phi_B^-\), define
\(T_B=\Phi_B^++\Phi_B^-\) and \(x_B=2\Phi_B^+/T_B\). Then
\(\Phi_{B,net}=T_B(x_B-1)=0\), so every active closed surface has \(x_B=1\). If \(T_B=0\), \(x_B\) is undefined.
This is the same ARA ridge appearance as the AI/LLM paired-closure rule, but Maxwell supplies an exact divergence-free
constraint whereas the LLM case remains statistical/structural. Next equation after discussion: Faraday induction,
\(\nabla\times\mathbf E=-\partial\mathbf B/\partial t\).

### Faraday induction — four-quadrant translation

**Status:** `ESTABLISHED DECOMPRESSION COMPLETED / ARA UNIVERSALITY NOT IMPLIED`. For the open surface bounded by the
electric loop, use \((\Phi_B,\dot\Phi_B)\) as the exact phase-plane coordinates. Their two signs generate four
orientation × accumulation/release combinations. The induced circulation is
\(\mathcal E_E=-\dot\Phi_B\). Keep the turning point \(\dot\Phi_B=0\), where accumulation switches to release,
separate from the flux-orientation crossing \(\Phi_B=0\). For a sinusoidal flux the pair is in quadrature. Do not
infer induction from field amplitude alone and do not transfer this loop result unchanged to the temporal phase of a
freely propagating plane wave.

### Canonical gradient and singularity rule

**Status:** `CANON UPDATED`. Treat every ARA coordinate as a continuous relational gradient by default. Named poles,
quadrants, rungs, walls, phi and ridge locations are landmarks/orientations, not disconnected bins. Use “ARA
singularity crossing” for the declared signed-zero/cyclic-seam handover where phase becomes anti-phase; do not imply a
divergence or physical infinity. Record whether the local crossing is smooth, discontinuous, topological or only a
coordinate relabelling. The same event may appear at signed zero and at the \(0/2\) seam under different declared
projections.

### Faraday curl → superconducting winding

**Status:** `ESTABLISHED BRIDGE / ARA INTERPRETATION CALIBRATED`. Curl supplies a local circulation axis, not a
golden-ratio path. In superconductors, combine Faraday with the London current relations, condensate phase winding,
fluxoid quantisation \(n h/2e\), Meissner screening and type-II vortices. Treat vortex cores and phase slips as
candidate ARA singularity/rung crossings because the order-parameter amplitude can vanish and winding can change.
General \(\phi\) selection is not supported; reserve it for separately tested incommensurate material or vortex-pinning
dynamics.

### Ampère–Maxwell conduction/displacement gradient

**Status:** `EXACT NORMALISATION / TEST CANDIDATE`. Define
\(\mathbf J_D=\varepsilon_0\partial_t\mathbf E\), retain the signed vector sum
\(\nabla\times\mathbf B=\mu_0(\mathbf J_C+\mathbf J_D)\), and for declared magnitudes use
\(x_{D/C}=2D/(C+D)\). The ideal capacitor gives an active cross-region ridge
\(I_C=I_D=dQ/dt\). A material/frequency test can use
\(x_{D/C}(\omega)=2\omega\varepsilon/(\sigma+\omega\varepsilon)\), with complex phase retained separately. Do not infer
rung ordering from the two Maxwell terms; test any aggregation/rung claim independently.

### Ridge-state metadata

**Status:** `CANON UPDATED`. A scalar \(x=1\) must be accompanied by total activity, variance, coherence/phase and
signed alignment. Distinguish null ridge, lotto/everything ridge and coherent active ridge. The capacitor equality is
an active cross-region handover ridge; call it harmonic/resonant only if a periodic resonator is measured. It is not a
singularity crossing. Keep composition \(x=1\) separate from possible TE-ARA \(=2\) full participation.

### Vacuum plane-wave triad

**Status:** `EXACT ESTABLISHED BRIDGE`. For a declared travelling vacuum plane wave,
\(\mathbf B=c^{-1}\hat{\mathbf k}\times\mathbf E\) and
\(\mathbf S=\mu_0^{-1}\mathbf E\times\mathbf B\). Record this as an exact orthogonal \(E/B/\)energy-flow triad and a
precise relational-third example. Do not call \(E\) and \(B\) temporally anti-phase: they are in phase in this
projection. Re-evaluate phase separately for standing waves, reactive near fields and matter.

### Maxwell completeness sequence

**Status:** `AUDITED / NEXT STEP FIXED`. Structural translations exist for all four Maxwell field equations. Continue
in this order: charge continuity → Poynting theorem → Lorentz force/Maxwell stress → Lorentz invariants → Poincaré
sphere/polarisation → fields in matter → near/far radiation → gauge/holonomy. The first genuinely decisive new
dataset should contain collocated \(\mathbf E,\mathbf B,\rho,\mathbf J\) and particle/material energy so local and
global Poynting closure can be tested before any ARA compression. See
`Analysis/electromagnetism/MAXWELL_ARA_COMPLETENESS_AUDIT_2026-07-12.md`.

### Potential–kinetic anchor

Keep three coordinates distinct: proposed capacity \(E_{cap}\), actual stored/configurational energy
\(U^*=U-U_{min}\), and actual kinetic/motion energy \(K\). Candidate composition and occupancy are
\(x=2K/(U^*+K)\) and \(\rho=(U^*+K)/E_{cap}\). Gravity may define the potential or binding landscape, but a natural
system capacity must be independently derived rather than renamed after the observed maximum.
