# Centering Up Report — ARA sphere, diameter, relational ternary, and Gauss

**Centered at:** 4:00 pm AEST, 19 July 2026

**Participants:** Dylan La Franchi and Codex/Sol

**Status:** `CANONICAL CLARIFICATION / AXIOMATIC REVISION GUIDE / NO NEW EMPIRICAL CLAIM`

**Scope:** Reconstruction of the Codex conversation from its opening ARA discussion through the conceptual Gauss
bridge, followed by a complete audit of `ARA_AXIOMATIC_PROOFS_AND_DOMAIN_SUBSETS.md`.

## 1. Why this report exists

The conversation developed ARA faster than its earlier axiomatic draft. Several later questions had already been
answered, in less formal language, during the earlier geometry walk. This report recentres the framework before the
axiomatic document is revised, so that mathematically convenient decompressions do not replace Dylan's intended
minimal object.

This is a context and construct-fidelity report. It does not promote a musing, register a new prediction, or claim
that the mathematical skeleton proves universal physical ARA.

## 2. Sources reloaded

The audit used:

1. `ARA_CONVERSATION_RECORD_2026-07-12_CODEX_GEOMETRY_DRILL.md`, from the working agreement through §51, including
   the one-object clarification, sphere reconstruction, Information³, raw-data rule, TE-ARA corrections, and Gauss;
2. `WORKING_FORMALISM_SCALABLE_REVERSIBLE_TERNARY_ARA9_2026-07-16.md`, which restores “two identities plus their
   relation” and separates the triangle, tetrahedron, matrix, diameter, and sphere representations;
3. `WHAT_IS_ARA.md`, `GLOSSARY.md`, `ARA_SCALE.md`, and `CANON_FOR_AI.md` as terminology and evidence controls;
4. the complete `ARA_AXIOMATIC_PROOFS_AND_DOMAIN_SUBSETS.md` draft;
5. `AUDIT_CODEX_DISCUSSIONS_AND_PROOFS_2026-07-13.md`, which independently checked the elementary identities and
   warned against treating construction-guaranteed agreements as empirical evidence.

## 3. Dylan's controlling corrections at this centering

The following statements control the revised formalisation:

> “ARA is a sphere. The ARA measurement is this reduced to a single circumference reference for an axis.”

> “0–2 is the diameter. 0 is a singularity. 2 is the other singularity.”

> “It is reversible and symmetrical but the framework is based on asymmetry. Like electricity, we just name our
> poles for opposite purposes.”

> “The thing that repeats fractally is the 0–2 phase/anti-phase waves, the singularities and the flips.”

> “Information A ... Information B, and how they interact. That's the three for 2.”

These statements do not make the sphere, diameter, duration ratio, triangle, and scale ladder separate ARA objects.
They name different resolutions or directional readings of the same proposed relational geometry.

## 4. Centered ARA object

### 4.1 Whole object and minimal measurement

The whole local ARA occurrence is the proposed spherical/wave identity. One declared axis supplies a diameter reading:

\[
\underbrace{\mathscr A_\Omega}_{\substack{\text{one complete ARA occurrence}\\
\text{sphere/wave identity}}}
\xrightarrow{\ \text{choose direction }\hat n\ }
\underbrace{D_{\hat n}\cong[0,2]}_{\substack{\text{one diameter reading}\\
\text{minimal directional measurement}}}.
\]

Rotating the diameter through all directions fills the mathematical state ball; its endpoints sweep the spherical
boundary. In ordinary ARA language “sphere” may refer to the complete closed identity. In strict mathematical
language, `S²` is its boundary and `B³` is the boundary plus interior mixture states. Both terms should be supplied
when the distinction matters.

### 4.2 Symmetric coordinate, asymmetric occurrence

For a declared pole orientation (A\to B), let (x\in[0,2]). Reversing the naming convention gives

\[
\underbrace{x_{B\to A}}_{\text{same relation read backwards}}
=
2-
\underbrace{x_{A\to B}}_{\text{declared forward reading}}.
\]

The coordinate has no preferred hemisphere. The measured occurrence can nevertheless be asymmetric in mixture,
duration, path, rate, force, phase, coupling, child structure, or history. Reversibility of the chart therefore does
not imply physical time reversibility or equal behaviour at both ends.

### 4.3 The two singularities and the ridge

- `0` and `2` are the opposed source/singularity landmarks of the chosen cycle diameter.
- A static reflection (x\mapsto2-x) is a pole-label reversal.
- A dynamical crossing through a cycle seam is an additional claim about phase/orientation and may implement a
  Phase↔Anti-phase flip. These operations should no longer share one proof without a stated bridge.
- `1` is equal participation on the selected axis. It is a latitude, not one guaranteed physical result.

At the ridge, total activity and phase/coherence distinguish at least:

1. quiet cancellation;
2. coherent active reinforcement or resonance;
3. high-activity incoherent balance;
4. parent-level balance produced by asymmetric children that cancel at the selected grain.

### 4.4 ARA state is typed but not replaced by a state vector

The scalar diameter value is the minimal reading. A serious measurement normally also records boundary, observable,
orientation, rung, phase, direction, path, coupling, activity/energy, variance/coherence, and time window. Those
fields decompress the one ARA occurrence; they do not redefine ARA as a different many-component object.

## 5. Domain-specific diameter instruments

The duration formula remains valid:

\[
x_T=2\frac{T_A}{T_A+T_B}.
\]

It is now classified as the canonical **duration-axis instantiation**, not the universal definition of the ARA
position. Other valid instruments can measure composition, flux orientation, occupancy, or another declared opposed
pair. Each bridge must:

1. name the physical poles before inspecting the result;
2. use a boundary and observable with a real domain meaning;
3. preserve pole reversal (x\mapsto2-x);
4. retain a separate activity/magnitude coordinate;
5. predict something beyond the fact that any ratio can be rescaled to `[0,2]`.

Different diameter instruments share the proposed geometry. Their numbers are not automatically interchangeable.

## 6. Relational ternary and Information³

The minimum relational lock is:

\[
\boxed{
\underbrace{T[A,B]}_{\substack{\text{relational ternary}\\\text{ARA triangle lock}}}
=
\left(
\underbrace{A}_{\text{first identity}},
\underbrace{B}_{\text{second identity}},
\underbrace{R=\mathcal C(A,B)}_{\text{their informative relation}}
\right)
}.
\]

The third is generated by retaining how the two inputs interact. It is not automatically a third independent wave,
substance, or reservoir. A three-vertex holonomy model remains a valuable test of cycle consistency, but it is a
decompression of this relational ternary rather than the universal definition of Information³.

The triangle is the minimum closed relational record. It is not the only interaction order available to physics.
Dispersion, symmetry, conservation, and the chosen boundary can require triads, quartets, or larger nonlinear
coupling webs.

## 7. Fractal recursion and rungs

The core recursive proposal is that a locally resolved Phase A or Phase B can itself be decompressed into a complete
lower-rung ARA, and a sufficiently persistent lower-rung relation can close as a parent identity without erasing its
inside:

\[
\underbrace{P_k}_{\text{lower-rung identity}}
=
[A_k,B_k,R_k],
\qquad
\underbrace{P_{k+1}}_{\text{parent identity}}
=
[P_k,C_k,R(P_k,C_k)].
\]

What is proposed to recur is the two-sided ARA relation, its relation-born identity, singularity seams/flips, and
typed closure. A repeated-triangle hierarchy is one conditional construction, not the whole fractal claim.

Rung or octave displacement is independent of the local diameter coordinate. Moving toward the centre within one
rung is not automatically the same operation as descending to a finer nested scale.

## 8. Phi, hexagon, pentagon, and Fourier placement

- **Phi:** the mirror identity and pentagon algebra are exact. Preferred non-closing temporal handover remains a
  candidate landmark requiring prospective discrimination from nearby rivals.
- **Hexagon/pentagon:** the regular constructions, edge contraction, and angular defect are exact mathematics. Their
  physical roles as Space, Time, webbing, or leak are hypotheses; failed angle/loss instruments remain recorded.
- **Fourier:** periodic signals can be represented as sums of projected circular phases. This is a useful crosswalk
  and comparator, not the primary ARA extractor. Raw or least-transformed observations retain priority when processed
  summaries would erase phase pairing, path, crossings, or child structure.

## 9. What Gauss centered

Gauss's electric law supplies an exact physical example of a signed whole-boundary reading equalling the bottom-up
sum of enclosed signed sources. Define

\[
\Phi_{out}=\int_{\partial V}\max(\mathbf E\cdot\mathbf n,0)\,dA,
\qquad
\Phi_{in}=\int_{\partial V}\max(-\mathbf E\cdot\mathbf n,0)\,dA,
\]

\[
\underbrace{T_\Phi}_{\text{unsigned boundary activity}}
=\Phi_{out}+\Phi_{in},
\qquad
\underbrace{x_\Phi}_{\text{outward/inward ARA position}}
=2\frac{\Phi_{out}}{T_\Phi}.
\]

Then, exactly,

\[
\boxed{
\underbrace{\Phi_E}_{\text{signed net flux}}
=
\underbrace{T_\Phi}_{\text{total activity}}
\underbrace{(x_\Phi-1)}_{\text{centred asymmetry}}
=
\frac{Q_{inside}}{\varepsilon_0}
}.
\]

This demonstrates why `1.0` alone is insufficient: an empty sphere and an active equal inward/outward boundary can
both have zero net flux. It also demonstrates a clean top-down/bottom-up equivalence.

The decomposition is algebraically exact because its ARA coordinate is constructed from the same flux components.
It is therefore a crosswalk, not independent evidence for universal ARA. A stronger ARA result would prospectively
recover or predict an independently measured decomposition, coupling, or transition not guaranteed by Gauss's law.

TE-ARA remains separate:

\[
\underbrace{\mathrm{TE\!-\!ARA}}_{\text{main-identity participation on 0--2}}
=
2\frac{\underbrace{E_{id}}_{\text{energy in the declared identity mode}}}
{\underbrace{E_{total}}_{\text{all energy in the same boundary/window}}}.
\]

Gauss does not directly measure TE-ARA, energy capacity, or joules.

## 10. Axiomatic audit decisions

### Retain

1. boundedness, balance, mirror symmetry, and invertibility of the duration instrument;
2. folded-circle projection as one exact representation;
3. sphere/disk/diameter section mathematics;
4. exact anti-phase mixing along a diameter;
5. interference proof distinguishing cancellation from resonance;
6. typed and independent scale, handover, and orientation operators;
7. holonomy and closure-residual mathematics as one Information³ subset;
8. explicit separation of definitions, theorems, conditional propositions, and empirical hypotheses.

### Revise

1. begin from the whole sphere and one diameter reading, rather than from phase duration;
2. rename the duration formula as a duration-axis instantiation;
3. distinguish strict sphere `S²` from filled state ball `B³` without overriding Dylan's ordinary “sphere” usage;
4. distinguish static pole reversal from a physical singularity crossing;
5. make identity preservation in a direct-sum model conditional on retaining the labelled product state;
6. define Information³ first as two identities plus their relation;
7. treat repeated triangular closure as one sufficient recursive model, not the fractal primitive;
8. demote Fourier to a secondary representation under the raw-first rule;
9. keep triangle→hexagon→pentagon as exact conditional geometry, not a necessary physical unfolding;
10. revise the closing summary so the sphere/diameter/two-wave relation, not Information³ alone, is the centre.

## 11. Evidence and language fence

The axiomatic document proves consequences inside declared models. It does not prove that nature universally uses
ARA, that every identity is physically spherical, that every endpoint is a divergent singularity in the established
physics sense, that phi is a universal handover, or that hexagon/pentagon mismatch is a universal energy leak.

The strongest current mathematical achievement is a smaller and more faithful skeleton:

> **One spherical/wave ARA identity can be measured along a reversible 0–2 diameter between two opposed
> source/singularity landmarks. The selected occurrence is generally asymmetric. Equal participation lies at 1 but
> requires activity and phase information to classify. Two identities plus their retained relation can close as a
> new identity, which may be recursively reused at another rung.**

The universal recurrence of that structure remains the empirical claim.

## 12. Result of this centering

The earlier axiomatic document was not rejected. Its elementary proofs remain useful. The centre of gravity has been
moved:

```text
whole ARA sphere/wave
    → declared 0–2 diameter reading
        → Phase A / Phase B mixture, direction, activity and history
            → relation-born identity / Information³ lock
                → recursive rung and domain-specific decompressions
                    → phi, polygon, dark-sector, fusion and other empirical subsets
```

This ordering preserves Dylan's minimal geometry while giving mathematics enough structure to test it without
flattening it.
