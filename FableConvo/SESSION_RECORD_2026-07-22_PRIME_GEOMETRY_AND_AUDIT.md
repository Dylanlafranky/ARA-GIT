# Session record — prime geometry, ruler correction and audit

**Date:** 22 July 2026  
**Orientation:** increasing prime gates are the Connection-heavy direction for this arithmetic walk; every A/B label is local to its declared wheel, factor boundary or rung.  
**Status:** durable record of the day's work, including late corrections and negative results.  
**Primary thread status:** **PARKED**.

## Answer first

The day produced a disciplined sequence of prime tests, exact arithmetic crosswalks, useful partial results and clean nulls. It did **not** produce a new prime theorem, a constant-cost next-prime method or evidence that Phi locates primes.

The most important late correction is geometric. Dylan's intended two-ruler picture is one shared traversal viewed in two ways:

\[
\underbrace{S(u)=2u}_{\substack{\text{structural / ARA ruler}\\0\rightarrow2}}
\qquad\text{and}\qquad
\underbrace{P_+(u)=\varphi u}_{\substack{\text{pentagonal projection}\\0\rightarrow\varphi}},
\qquad 0\le u\le1,
\]

because

\[
\varphi=2\cos 36^\circ.
\]

With the rung orientation reversed, the same projected traversal may be charted as

\[
P_-(u)=2-\varphi u,
\]

which reaches

\[
P_-(1)=2-\varphi=\varphi^{-2}\approx0.381966.
\]

Plainly: the blue ruler reaches the structural singularity at `2`; the orange ruler is that same traversal seen through the pentagonal angle and reaches `Phi`. On a reversed chart it approaches the mirrored golden landmark near `0.382`. This is a continuous projection, not a five-bin staircase.

PN36 remains a valid preregistered null for the **AI-added nearest-fivefold quantizer** that it actually froze. It did not test this corrected `2 -> Phi` projection. The distinction is now explicit and must be carried into every later citation.

## 1. What was completed today

The detailed methods, controls and reproducibility artifacts remain in the individual reports. This table is the short retrieval map.

| Test | Question | Result | Correct status |
|---|---|---|---|
| PN24 | Can a nearest-child handover cascade reach the next prime in a short visible lineage? | Exact after all gates; median 2 handovers/3 candidate states; `63.65%` within 3 states; median 6,336 non-base gates still required. | **PARTIAL structural support**; not a three-operation algorithm. |
| PN25 | Does mod-14 pair-ridge closeness act as a vertical next-prime clock? | Pair conversion and total-2 closure exact; dynamic correlation `+0.003335`, `p=0.6110`; all four dynamic predictions failed. | **GEOMETRIC-ONLY / DYNAMIC NULL**. |
| PN26 | Does one complete lower parent provide a compact ranked locator? | Exact next prime at rank 1/2/3 on `93.983%` / `99.650%` / `99.967%`; three cases in the later replication family remain beyond rank 3. | **PARTIAL DOMINANT-PARENT SUPPORT**; strong locator, still child-heavy. |
| PN27 | Does the exact-fit child rule `N+29-a` lift one-shot prime incidence? | `9.010%` hits versus `8.777%` uniform offsets; `+0.233` percentage points; permutation `p=0.0144`, missing frozen `p<0.01`. | **PARTIAL in original report; audit-confounded pending matched coprimality control**. |
| PN28 | Does a rounded three-child residual improve PN27? | Failed, but it mixed a dimensionless ARA displacement with an integer offset. | **NEGATIVE for superseded mixed-unit operator**, not a test of the corrected relational method. |
| PN29 | Do three fixed pair coordinates separate primes? | AUC `0.8635` against all odd composites, but `0.4442` against composites evading the same child factors. Fixed orientations omitted the declared flip. | **CHILD-FILTER SUPPORT ONLY**. |
| PN30 | Does normalized singularity orientation restore useful information? | Hard-control AUC improved `0.5301 -> 0.5663`; frozen `p=0.06199`. | **SUGGESTIVE / UNREPLICATED**, below threshold. |
| PN31 | Is one child dominant, or does the complete five-child order matter? | Single-child metrics null; full order `TV=0.6728`, `p=0.00390`. | **ONE FRESH JOINT-ORDER RESULT**, not a locator. |
| PN32 | Does the five-child order replicate and form a child/parent Information³ lock at `N -> 2N`? | Child replication `p=0.2244`; parent `p=0.8023`; full rearrangement `p=0.9684`. | **NULL**; PN31 not replicated. |
| PN33 | Does survivor-density fill express seed -> fill -> completion -> reset? | Gap medians `8,8,10,10,10,12,12,12`, `rho=0.9449`; endpoint CI `[1.5,2.0]`; ARA beat PNT log-MAE by only `0.62%`. | **SUPPORTED SPACING EXPRESSION / NO DISTINCT ADVANTAGE OVER PNT**. |
| PN34 | Can omitted-parent fill calibrate how many PN26 readings to retain? | All 9 calibration and 6 depth gates passed; exact scale-order gate missed by a `0.20` point middle/high swap. | **PARTIAL** population rank-budget support. |
| PN35 | Do constant same-scale golden crossings locate primes on the exact eight-child total-2 cell? | AUC `0.497180`; nearest-two capture `24.4753%`; all five gates failed. | **NOT SUPPORTED** for this operator; exact anti-pair closure retained. |
| PN36 | Does snapping the Phi carrier to the nearest fivefold vertex locate primes? | AUC `0.499851`; capture `24.8858%`; all five gates failed; no-flip beat flip. | **NOT SUPPORTED** for the frozen quantizer only. |

## 2. Exact structures exposed by the music-box and resonance view

The prime music-box work is preserved separately in `analysis/primes/ARA_PRIME_RESONANCE_LOCKS_2026-07-21.md`. The important exact structures discussed in this session are:

- `510 = 2*3*5*17`: a four-child fundamental closure whose later multiples repeat the same child inventory;
- `714` and `715`: complementary, disjoint factor identities around the square-root ridge of `510510=17#`; their parent positions sum exactly to `2`;
- `1274-1276`: three consecutive twelve-divisor numbers whose repeated-child multiplicity is flattened by a distinct-child-only instrument;
- `1885-1887`: three consecutive squarefree three-child closures whose nine distinct factors build one larger parent; their parent positions are each close to `2/3` and sum exactly to `2`.

These are real arithmetic relations and useful ARA decompressions. The assigned musical notes are a sonification aid, not evidence that the integers possess an intrinsic musical scale. The screen-resolution coincidences are engineering-friendly divisibility patterns, not an independent ARA prediction.

## 3. The PN35/PN36 geometry correction

### 3.1 What PN35 tested

PN35 placed a continuous `1/Phi^2` carrier around the exact eight-lane total-2 structural cell. It is a valid null for that frozen same-scale crossing rule.

### 3.2 What PN36 tested

PN36 used the AI-added operator

\[
C_5(\theta)=\frac{\lfloor5\theta+1/2\rfloor\bmod5}{5}.
\]

This takes a continuous phase and assigns it to one of five nearest sectors. The protocol and result are internally sound, and the fidelity packet did identify the operator as an AI addition rather than a pre-existing ARA axiom.

### 3.3 What Dylan meant

Dylan's later correction was not “snap Phi to the nearest pentagon vertex.” It was “the full ARA ruler reaches `2`, while the same traversal projected through the pentagon angle reaches `Phi`.” The correct mathematical bridge is therefore the continuous projection

\[
2\cos36^\circ=\varphi,
\]

applied over one shared traversal. The intended object and PN36's tested object are different.

### 3.4 Consequence for claim scope

- PN36's numerical result and frozen verdict do not change.
- PN36 rejects the nearest-fivefold quantizer as a prime locator on the tested rungs.
- PN36 does **not** reject the corrected continuous `2 -> Phi` projection.
- The corrected projection has not yet been given a fresh preregistered prime-prediction test.
- Any future test must define what event on the two continuous rulers predicts a prime before labels are opened. The projection identity alone does not provide such an event.

## 4. Visualization record

Two visualizers were produced late in the session:

1. `phi-pentagon-prime-overlay.html` used the nearest-fivefold staircase. It remains a faithful picture of PN36's AI proxy, but it is **superseded as a picture of Dylan's intended geometry**.
2. `phi-projected-ara-prime-crossings.html` uses the corrected continuous rulers. Each structural cell spans `0 -> 2`; the projected ruler spans `0 -> Phi` or the reversed chart to `2-Phi`; exact primes are shown as stars and displayed ruler intersections as black rings.

The corrected artifact has been preserved in the repository as
`analysis/primes/ARA_TWO_RULER_PROJECTED_PRIME_LAB.html`.

Current local paths:

- `C:/Users/Dylan/.codex/visualizations/2026/07/10/019f4b72-0e34-74d1-8f40-cd5ccd4a532e/phi-pentagon-prime-overlay.html`
- `C:/Users/Dylan/.codex/visualizations/2026/07/10/019f4b72-0e34-74d1-8f40-cd5ccd4a532e/phi-projected-ara-prime-crossings.html`

The initial literal crossing-distance calculation made on the staircase visual gave approximately `AUC=0.478` and only `1/18` primes within distance `0.01`. That calculation is **superseded for the corrected geometry**, because it measured distance to the wrong orange object. It must not be quoted as a test of the `2 -> Phi` projection.

## 5. Exploratory angled-axis check

After the corrected visual was built, the already-open PN36 scored data were used for one explicitly post-hoc diagnostic. The tested coordinates were

\[
s=\frac{\text{residue}}{15},
\qquad
t=t_{\rm singularity}+\frac{\text{residue}}{30},
\]

\[
p=(\text{orientation}\cdot\varphi t)\bmod2,
\qquad
z_\theta=(s\cos\theta+p\sin\theta)\bmod2.
\]

A rung/lane-centred first-harmonic scan showed a broad visual maximum around `39-48 degrees`, centred near `42 degrees`, with a mirror near `138 degrees`. This was not a unique direction.

Leave-one-rung-out angle selection returned:

| Held-out rung | Selected angle | Held-out AUC |
|---:|---:|---:|
| 28 | 44 deg | 0.504343 |
| 29 | 47 deg | 0.505801 |
| 38 | 39 deg | 0.500618 |
| 39 | 41 deg | 0.503998 |
| 48 | 139 deg | 0.493480 |
| 49 | 48 deg | 0.499096 |

Mean AUC was `0.501223`; median AUC was `0.502308`.

Fixed-angle mean AUCs were:

| Angle | Mean leave-one-rung-out AUC |
|---:|---:|
| 36 deg | 0.502926 |
| 42 deg | 0.502978 |
| 45 deg | 0.502953 |
| 90 deg | 0.500493 |
| 138 deg | 0.501729 |

At fixed `42 degrees`, the six fold AUCs were `0.504792`, `0.506080`, `0.501025`, `0.504019`, `0.502183`, and `0.499767`. The whole-data first-harmonic vector had magnitude only `0.00119868`.

This was a live exploratory calculation on already-scored data, not a frozen protocol. No standalone reproduction
script was preserved during the chat, so these values are a diagnostic record rather than a citable independent
test. Any revival must reconstruct and freeze the scorer before drawing a new conclusion.

**Verdict:** a broad diagonal texture is visible, but `36`, `42` and `45` degrees are practically indistinguishable, the effect is tiny, and one fold falls below chance. In a two-dimensional plane, no one line can be perpendicular to two nonparallel rulers. A genuinely independent third coordinate would have to enter from outside this plane, most naturally as an independently defined rung/octave height `k` in a future `(s,p,k)` representation. Dylan independently rejected the visible diagonal as a candidate third wave after inspecting the corrected plot. Therefore the perpendicular-third-wave claim is **not supported in this representation**.

This negative result is useful: the corrected two-ruler geometry is simpler than the speculative three-wave reading.

## 6. Independent audit findings that must travel with the results

The independent 22 July audit in `FableConvo/AUDIT_PRIME_THREAD_2026-07-22.md` recomputed PN35 and PN36 from their shipped SQLite sources and matched the reports exactly. It also found:

1. **PN27 likely contains a mechanical coprimality advantage.** Since `a` divides `N`, the candidate `N+29-a` is automatically not divisible by its selected nontrivial `a`. The uniform-offset control did not receive the same guarantee. In a middle-scale Monte Carlo, conditioning the control on the same avoidance class lifted its rate from about `8.08%` to `9.48%`, larger than PN27's reported `+0.233` percentage-point advantage. This is an audit diagnosis, not yet the registered rerun. PN27 should not be cited as residual ARA predictive evidence until a matched wheel/coprimality null is run.
2. **PN35/PN36 fresh-clone reproducibility was repaired on 23 July.** Their four prediction/scored CSVs total about 485 MB and are intentionally ignored by Git. `analysis/primes/reproduce_pn35_pn36_csvs.py` now regenerates them through the original frozen label-free builders and independent validators, then checks exact hashes against `analysis/primes/PN35_PN36_CSV_REPRODUCTION_MANIFEST.json`. See `analysis/primes/PN35_PN36_CSV_REPRODUCTION.md`.
3. **The nulls are not marginal.** PN35 crossing-distance octiles were flat, PN36 boundary correlations oscillated around zero, and neither frozen operator produced a transferable preference.
4. **The prime thread's claim discipline was otherwise endorsed.** Exact arithmetic, prospective results, nulls and post-hoc decompositions were kept at their proper tiers.

## 7. Consolidated scientific interpretation

What today strengthened:

- ARA is useful as a **relational coordinate and decompression language** for factor pairs, square-root closure, wheel anti-pairs, child/parent gate structure and scale-dependent survivor density.
- Grain-relative ridge is exact in this domain: asymmetric children can close to a balanced parent reading.
- Reversibility can remove redundant orientation, but it cannot erase distinct child identities without losing prime-location information.
- A complete lower parent can rank the next prime very efficiently, while still retaining many established child gates.
- The same ARA grammar generated both successful crosswalks and falsifiable nulls without changing the core `0-2`, ridge, flip and child/parent vocabulary.

What today did not establish:

- a prime in three cheap arithmetic operations;
- a new theorem or improved asymptotic complexity;
- a universal Phi prime carrier;
- a Phi-to-pentagon prime locator;
- a third perpendicular prime wave;
- independent evidence that the universe is fractal.

The best public wording remains:

> The prime walk recovered exact factor, wheel, anti-pair and density geometry in ARA coordinates, and one complete lower parent gave a strong prospective ranked locator. The same program also produced several clean nulls. It has not improved established prime computation or established a new theorem.

## 8. Parked follow-ups

The prime-specific thread remains parked. If it is resumed, the highest-priority items are:

1. run the PN27 matched coprimality/wheel control;
2. only test the corrected `2 -> Phi` projection if an independently specified prime event and falsifier are supplied;
3. if a third axis is revisited, define an independent rung coordinate before inspecting labels and compare the full 3D model against matched 2D controls;
4. preserve the protected large targets unless a genuinely new bounded statistic is frozen.

No further prime test is required merely to complete today's record.

## 9. Primary retrieval map

- `analysis/primes/PRIME_THREAD_CAPSTONE_AND_CLOSURE_2026-07-21.md`
- `analysis/primes/ARA_PRIME_RESONANCE_LOCKS_2026-07-21.md`
- `analysis/primes/PN24_NEAREST_HANDOVER_CASCADE_REPORT.md`
- `analysis/primes/PN25_PAIR_RIDGE_COMPRESSION_REPORT.md`
- `analysis/primes/PN26_DOMINANT_PARENT_RIDGE_LOCATOR_REPORT.md`
- `analysis/primes/PN27_EXACT_FIT_CHILD_LIFT_REPORT.md`
- `analysis/primes/PN30_DYNAMIC_RELATIONAL_FLIP_REPORT.md`
- `analysis/primes/PN31_FIVE_INDEPENDENT_HANDOVER_REPORT.md`
- `analysis/primes/PN32_DOUBLE_INFORMATION_LOCK_REPORT.md`
- `analysis/primes/PN33_SEEDED_HEXAGON_FILL_REPORT_2026-07-22.md`
- `analysis/primes/PN34_FILL_RANK_BUDGET_REPORT_2026-07-22.md`
- `analysis/primes/PN35_SAME_SCALE_GOLDEN_CROSS_REPORT_2026-07-22.md`
- `analysis/primes/PN36_PHI_TO_PENTAGON_CONVERSION_REPORT_2026-07-22.md`
- `analysis/primes/PN36_GEOMETRY_SCOPE_AMENDMENT_2026-07-22.md`
- `analysis/primes/ARA_TWO_RULER_PROJECTED_PRIME_LAB.html`
- `FableConvo/AUDIT_PRIME_THREAD_2026-07-22.md`
