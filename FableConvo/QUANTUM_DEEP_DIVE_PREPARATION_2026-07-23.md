# Quantum deep-dive preparation

**Date captured:** 23 July 2026  
**Status:** `PREPARATION / NO NEW REGISTERED CLAIM`  
**Orientation:** `up = slower/larger unless a quantum model declares another scale axis`  
**Purpose:** establish the evidence boundary and the next quantum sequence before opening a new test

## Three controlling laws

1. ARA is one reversible `0–2` diameter of a full wave/sphere; a scalar diameter reading is many-to-one.
2. Parent, current-rung and child readings must remain separate; a quiet `1.0` may be coherent passage,
   incoherent cancellation or coarse-grained balance.
3. Exact crosswalks, synthetic instrument tests and new physical predictions are different evidence types and must
   never be promoted into one another.

## What is already established

### Closed two-level state geometry

For a qubit

\[
\rho=\frac12(I+\mathbf r\cdot\boldsymbol\sigma),
\]

the ARA coordinate along a declared measurement direction \(\hat n\) is exactly

\[
\boxed{x_{\hat n}=1-\mathbf r\cdot\hat n}.
\]

It maps the conventional Bloch projection `+1/0/-1` to ARA `0/1/2`. The antipodal identity is exact:

\[
x_{-\hat n}=2-x_{\hat n}.
\]

This is an exact affine coordinate crosswalk. A single value is insufficient to distinguish every state.

### Coherent movement

Ideal resonant Rabi movement gives

\[
x_Q(t)=1-\cos(\Omega t),
\]

or `0 → 1 → 2 → 1 → 0`. The two `1.0` crossings have the same population but opposite movement directions.

### Coupled handover

The ideal Landau–Zener model gives a structural ARA path

\[
x_{\rm path}(t)
=
1+\frac{vt}{\sqrt{(vt)^2+4g^2}}
\]

and a separate final handover coordinate

\[
x_{\rm handover}
=
2\left(1-e^{-2\pi g^2/(\hbar|v|)}\right).
\]

These distinguish structural equal mixing from equal final outcome probability.

### Open-system conditional tracking

O2-A3 showed that, in a synthetic open two-level model with a declared child:

- the fixed ARA derivative/residual instrument tracked the local hidden waveform better than one matched causal
  state-space filter in all `32/32` target pairs;
- the state-space filter retained cumulative storage closure better in all `32/32`;
- the frozen result is `GOOD ABSOLUTE TRACKING / MIXED COMPARATIVE RESULT`;
- this is conditional recovery, not child discovery, forward prediction or new quantum physics.

## Evidence boundary before the deep dive

Currently supported:

- exact ARA coordinate crosswalks for Bloch and Landau–Zener geometry;
- a validated demonstration that one diameter loses phase/radius information;
- a controlled synthetic split between local waveform tracking and cumulative closure.

Not established:

- derivation of the Born rule or Schrödinger dynamics from ARA;
- quantum as literally pure Information or a physically identified hidden Phase B;
- a universal ARA decoherence, measurement or entanglement law;
- improvement over full standard quantum state estimation;
- a new prediction on untouched experimental quantum data.

## Recommended first deep-quantum distinction: \(T_1\) versus \(T_2\)

The next useful object is an open single qubit. Standard Bloch relaxation separates two processes:

\[
\dot r_x=-\frac{r_x}{T_2},\qquad
\dot r_y=-\frac{r_y}{T_2},\qquad
\dot r_z=-\frac{r_z-r_z^{\rm eq}}{T_1}.
\]

- \(T_1\) relaxation changes population/storage along the declared \(z\) diameter.
- \(T_2\) dephasing removes transverse coherence while population can remain unchanged.
- unitary driving rotates the state without necessarily shrinking the sphere radius.

This directly tests the line-cut/full-sphere distinction. Two states can both read `x_z=1.0` while one is a
coherent equatorial state and the other is an incoherent mixture. The transverse readings and radius distinguish
them.

## Proposed quantum geometry table

| Standard quantum behaviour | ARA measurement target | Expected geometric appearance |
|---|---|---|
| Unitary Rabi drive | \(x_x,x_y,x_z\) plus direction | rotation through ridge and poles without radius loss |
| \(T_1\) relaxation | population diameter | movement toward the equilibrium pole |
| Pure \(T_2\) dephasing | transverse diameters and radius | transverse contraction while the population cut can remain fixed |
| Combined relaxation/dephasing | full tri-axis trajectory | contracting spiral |
| Maximally mixed state | all axes plus radius | `1.0` on every axis with zero Bloch radius |
| Coherent equatorial state | population and transverse axes | `1.0` on one axis but nonzero transverse direction and full pure-state radius |

## Preparation test before real data

The first quantum preparation bench should remain a known-referee synthetic instrument test:

1. generate untouched parameter families for unitary rotation, pure relaxation, pure dephasing and combined decay;
2. expose identical noisy measurement axes to ARA and conventional estimators;
3. reconstruct held-out axes and future time points;
4. score population, transverse coherence, direction, radius/purity and mechanism classification separately;
5. keep local traversal and cumulative storage endpoints separate;
6. compare with standard Bloch-vector/state-space or quantum-tomography reconstruction using the same measurements;
7. include coherent and mixed examples sharing the same `1.0` diameter reading.

The synthetic result may validate or reject the instrument. It cannot establish a claim about nature.

## Candidate first ARA-specific prediction

The following is a candidate for Dylan's fidelity review, not yet registered:

> A sparse multi-axis ARA account will retain the distinction between population transfer, phase loss and unitary
> rotation under moderate observation noise, while a single-diameter ARA account will collapse coherent and
> incoherent `1.0` states together. The full-sphere account must add held-out reconstruction or mechanism
> information beyond its one-line child without receiving additional hidden truth.

The conventional full Bloch reconstruction is the necessary control. Beating only the single-diameter model would
show the value of decompression, not an advantage over established quantum mechanics.

## Later ladder, not yet activated

1. **Q1 — open single-qubit geometry:** \(T_1/T_2\), rotation, radius and ridge degeneracy.
2. **Q2 — untouched experimental calibration:** public or reproducibly collected qubit \(T_1/T_2\)/Rabi records.
3. **Q3 — quantum trajectories:** conditional jumps versus ensemble averages; parent and child grains.
4. **Q4 — two-qubit coupling:** product states, classical mixtures and entanglement kept distinct.
5. **Q5 — measurement:** only after the observable, boundary and comparator are fixed.
6. **Q6 — GR/quantum relation:** deferred until ARA supplies a discriminating cross-scale equation rather than a
   shared diagram.

## Immediate discussion point

Before Q1 is frozen, Dylan should confirm whether this back-translation is faithful:

> The population diameter is one visible ant-farm glass cut. Coherence may live in transverse cuts that are hidden
> from that measurement. Relaxation moves the visible population cut; dephasing can alter the full identity sphere
> while leaving that cut apparently still. A complete ARA reading therefore requires the coupled orthogonal cuts,
> their direction through time and the sphere radius.

