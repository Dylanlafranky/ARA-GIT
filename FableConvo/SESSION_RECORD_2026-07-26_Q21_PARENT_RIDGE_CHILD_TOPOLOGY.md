# Session record — Q21 parent ridge and recursive child topology

**Recorded:** 26 July 2026  
**Status:** test complete; frozen claim not supported

## Dylan's governing correction

After Q20, Dylan clarified:

> Averaging the entire detector sphere appears to let local child asymmetries
> cancel at the parent ridge. This is the 1.0 at the parent ridge of the below.
> We encountered it heavily in LLM results. When you get all the children and
> measure the whole, you get the parent's 1.0 ridge.

This was accepted as the central Q21 geometry. The test did not treat a parent
near `1.0` as local quiet.

## Outcome-blind confirmation

Across q4_7/r13 and the fresh q6_5/r30 detector events, all twelve recompressed
parent means were between `0.996595` and `1.049297`. The grandchildren and
local handovers remained variable beneath those parent readings.

## Frozen construction

- four spatial children: `AA, AB, BB, BA`;
- each opened into time A/B grandchildren;
- eight grandchildren retained as one total-2 allocation;
- all sixteen directed adjacent-time child handovers retained as a separate
  total-2 relation allocation;
- twenty-four primary coordinates;
- development q4_7/r13;
- untouched holdout q6_5/r30;
- protocol SHA-256:
  `bd26fa2e70c1e4ddbb4e5d768b6099cb6caaea3c96ab1ce3cac545d6575cd24d`.

The fresh target files were extracted only after this freeze.

## Result

**NOT SUPPORTED — 2/8 gates.**

Mean holdout AUROC:

- child topology: `0.512172`;
- grandchildren only: `0.512371`;
- spatial parent: `0.513420`;
- Q20 global x–time: `0.506218`;
- count: `0.506055`;
- topology plus count: `0.512532`;
- spatial misassignment: `0.503705`.

Primary permutation p-values were `0.126` for X and `0.006` for Z. The Z-only
signal is descriptive and cannot satisfy the two-basis claim.

Independent validation passed `95/95` checks and reran all `1,998`
permutations.

## Meaning

The test supports the internal construction fact that local children can
recompress to a parent near `1.0`. It does not show that the registered child
and aggregate-handover coordinates predict across detector patches.

The aggregate handovers added no mean benefit beyond grandchildren. The
remaining scientific uncertainty is now:

> Is the child geometry useful within one patch but rotated between patches,
> or is this aggregate child representation insufficient even locally?

## Post-result geometry correction and next proposed test

Dylan identified that Q21 had opened Tier 4 and measured mainly sideways
Tier-3 handovers, while omitting the vertical ARA wave from the Tier-4
grandchildren back to the Tier-1 whole/meta relation.

The explicit hierarchy is:

- Tier 1: whole/meta relation `J`;
- Tier 2: parent diameters `X,Y`;
- Tier 3: `AA,AB,BB,BA`;
- Tier 4: each Tier-3 child's time A/B grandchildren.

Q22 should calculate `ARA(Tier 1, Tier 4)` on untouched patch q6_9, fit on r13
and score r30. Controls must include Tier 1 alone, Tier 4 alone, Q21 topology,
count and a broken cross-rung pairing. This targets the missing wave between
scales rather than merely increasing lower-tier detail.

Entanglement remains deliberately parked until the local QEC hierarchy is
stable enough to transfer without flattening.

## Main report

`analysis/quantum/Q21_WILLOW_RECURSIVE_CHILD_TOPOLOGY_REPORT_2026-07-26.md`
