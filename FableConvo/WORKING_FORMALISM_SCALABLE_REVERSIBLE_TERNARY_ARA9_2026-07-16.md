# Working formalism — scalable reversible ternary, ARA^9, and leak coordinates

**Date:** 16 July 2026  
**Participants:** Dylan La Franchi and Codex/Sol  
**Status:** `REGISTERED CONCEPTUAL DELTA / MATHEMATICAL SCAFFOLD / PARKED EMPIRICAL TESTS`  
**Orientation:** up = slower/larger and down = faster/smaller unless a local test declares the reverse.  
**Evidence fence:** exact algebra and established geometry are labelled separately from ARA interpretations. This
document does not promote a musing into `CLAIMS_STATUS.md`, does not freeze a prediction, and does not replace the
historical archived scripts.

## 1. Dylan's controlling statements

The working object was clarified in this sequence:

> “Another way I have spoken about ARA in previous chats that might resonate with you, is scalable and reversible
> ternary.”

> “Information A ... Information B, and how they interact. That's the three for 2. But then you add a third, and you
> get the coupling from AB BA CA CB BC AC. Which is what we are doing now.”

> “There is a leak which I propose is the Hex_Pentagon document. The asymmetry.”

> “Though there is exponential withdrawal of change, so we'd only need to track itself, and 2 others. Which is where
> I got the idea for ARA^9. Which is ARA in 3 axis taking 2 steps from the central node being measured.”

These statements supersede the loose description of archived ARA^9 as merely “multi-axis/nested expansion.” They do
not erase the older formulations. The current interpretation is a re-derivation that must be compared with them.

## 2. Minimal relational ternary

Let two locally declared identities or readings be \(A\) and \(B\). Their measured relation is

\[
\underbrace{R}_{\substack{\text{mathematical relation}\text{ARA: informative third}}}
=
\underbrace{\mathcal C}_{\substack{\text{coupling/interaction map}\text{must be declared locally}}}
\left(
\underbrace{A}_{\text{first identity}},
\underbrace{B}_{\text{second identity}}
\right).
\]

The minimal closed record is therefore

\[
\boxed{
\underbrace{T[A,B]}_{\substack{\text{relational ternary}\text{ARA triangle lock}}}
=
\left(A,B,R=\mathcal C(A,B)\right)
}.
\]

The third entry is not automatically a third independent wave or a third unit of substance. It is the interaction
needed to retain how the first two are related. Depending on the domain, \(R\) may require more than one number—for
example strength/coherence and phase orientation.

### Established showcase: two-path interference

For two coherent amplitudes,

\[
I
=I_1+I_2
+
\underbrace{2\sqrt{I_1I_2}\,|\gamma_{12}|\cos\delta}_{\substack{\text{cross term}\text{relation strength and orientation}}}.
\]

The cross term contains both coupling strength \(|\gamma_{12}|\) and relative phase \(\delta\). It redistributes the
observed pattern without adding net probability. This is an established quantum identity and a faithful physical
showcase of “two identities plus their relation”; it is not an ARA derivation of quantum mechanics.

## 3. Binary projection and the tetrahedron

For a deliberately coarse signed projection,

\[
x,y\in\{-1,+1\},
\qquad
r=xy.
\]

Then

\[
xyr=1,
\qquad
r=xy,
\qquad
x=ry,
\qquad
y=rx.
\]

Any two entries recover the third. The four allowed points

\[
(+,+,+),(+,-,-),(-,+,-),(-,-,+)
\]

form a regular tetrahedron in the lifted ((x,y,r)) coordinates. This exact result from MX8 is the binary/parity
projection of the triangle lock. It is not the whole continuous ARA object: it discards magnitude, phase path,
coherence, rung, time, and local coupling context.

The parent may be represented as a weighted barycentre of the four routes. A separate visual apex is an optional ARA
drawing, not forced by the parity proof.

## 4. Scalable and reversible ternary

### Local structural reversibility

The binary closure above is reversible because any two labelled entries recover the third. A continuous version is
available whenever the state has an invertible composition law. For nonzero complex amplitudes, for example,

\[
\underbrace{R}_{\text{relative transform}}=BA^{-1},
\qquad
B=RA,
\qquad
A=R^{-1}B.
\]

This is **structural reversibility**: the full labelled relation retains enough information to reconstruct a missing
member under the declared model. It is not automatically physical time reversibility. Dissipation, decoherence,
coarse-graining, censoring, saturation, noise, or an irreversible measurement can destroy the information needed for
the inverse.

### Recursive scaling

If a sufficiently persistent lower-rung relation closes as a parent identity, retain its internal structure:

\[
P_k=\left[A_k,B_k,R_k\right],
\qquad
P_{k+1}=\left[P_k,C_k,R(P_k,C_k)\right].
\]

Flattening (P_k) to one unlabelled scalar before the next step generally removes the reversibility that the ternary
was meant to preserve. The compact working claim is:

> **ARA is a recursively scalable relational ternary, locally reversible when its full relation is retained.**

This is a mathematical formulation of the intended framework. Its recurrence across physical scales remains an
empirical claim.

### Three named identities and nine addressable slots

For three named identities \(A,B,C\), retaining direction gives six ordered relations:

\[
AB,BA,AC,CA,BC,CB.
\]

Together with the three self-state readings, these form the nine-slot bookkeeping map

\[
\begin{pmatrix}
A & AB & AC\\
BA & B & BC\\
CA & CB & C
\end{pmatrix}.
\]

The count \(3+6=9\) is exact bookkeeping. It does not establish that all nine slots are independent, that every
physical system needs this matrix, or that ARA fractality has been confirmed.

## 5. Representation discipline: the same object needs declared transforms

Triangle, tetrahedron, time-triangulation, pyramid, matrix, diameter line, and sphere can be projections or
decompressions of the same proposed relational object without being numerically interchangeable. A representation
change \(\Pi\) should be tested with the commuting condition

\[
\boxed{
\Pi\!\left(\mathcal C(A,B)\right)
\stackrel{?}{=}
\mathcal C_{\Pi}\!\left(\Pi A,\Pi B\right)
}.
\]

Plainly: couple first and then project, or project first and couple with the projected rule. If the answers differ,
the projection discarded information that matters to the relation. That failure does not necessarily reject the
underlying object; it rejects the claim that this particular coarse representation preserves it.

This is the clean test for Dylan's suggestion that several apparently different triangle-lock descriptions may be
the same object viewed through different distortions.

### Credibility assessment at this landing

This is not empty wordplay. The project terms have identifiable mathematical homes: cross terms, relative
transforms, parity closure, tetrahedral lifting, directed relation matrices, state-space operators, conservation
accounts, and angular defect. That is enough to justify serious formal development. It is not yet a proof that these
objects instantiate one universal physical geometry. That stronger claim needs the same frozen transformation or
operator to predict independently measured relations across systems or rungs better than established alternatives.

## 6. ARA^9 — current two-step, three-axis interpretation

### Observed local packet

Let \(\mathbf z_n=(z_{x,n},z_{y,n},z_{z,n})^T\) be three declared ARA-axis readings at the central observation \(n\).
Taking the central state and two successive steps produces nine labelled readings:

\[
\boxed{
\mathcal A^{(9)}_n
=
\left[
\mathbf z_n\;\;\mathbf z_{n+1}\;\;\mathbf z_{n+2}
\right]
=
\begin{pmatrix}
z_{x,n} & z_{x,n+1} & z_{x,n+2}\\
z_{y,n} & z_{y,n+1} & z_{y,n+2}\\
z_{z,n} & z_{z,n+1} & z_{z,n+2}
\end{pmatrix}.
}
\]

This is the present best mathematical transcription of “ARA in three axes taking two steps from the central node.”
The three columns are position \(0\), step \(1\), and step \(2\); “two steps” therefore supplies three sampled
positions.

### Simplest exact one-axis lock

If one axis follows a constant local complex multiplier

\[
z_{n+1}=\mu z_n,
\qquad
\mu=q e^{i\theta},
\]

then

\[
z_{n+2}=\mu^2z_n,
\qquad
\boxed{z_{n+1}^2=z_nz_{n+2}},
\qquad
\mu=\frac{z_{n+1}}{z_n}=\frac{z_{n+2}}{z_{n+1}}.
\]

When denominators are nonzero, either endpoint can be reconstructed:

\[
z_{n+2}=\frac{z_{n+1}^2}{z_n},
\qquad
z_n=\frac{z_{n+1}^2}{z_{n+2}}.
\]

This is an exact testable three-point lock for the simplest local model, not a universal ARA law.

### Exponential withdrawal and the stopping rule

If \(q<1\), amplitude decreases geometrically. After retaining positions \(0,1,2\), the unobserved scalar-amplitude
tail is proportional to

\[
\sum_{m=3}^{\infty}q^m=\frac{q^3}{1-q}.
\]

Therefore two steps are sufficient only when the tail falls below a predeclared tolerance. If \(q\approx1\), two
steps are not enough. If \(z\) is an amplitude, the associated one-step energy reduction is \(1-q^2\); if \(z\) is
already an energy-like quantity, it is \(1-q\). The observable's units must be fixed before using either expression.

### Three-axis transition operator

A more general local law is

\[
\boxed{
\mathbf z_{n+1}
=
\underbrace{M_n}_{\substack{\text{three-axis coupling operator}\text{nine coefficients before constraints}}}
\mathbf z_n
+
\underbrace{\mathbf u_{n+1}}_{\substack{\text{measured forcing, return}\text{or unresolved input}}}.
}
\]

Without external input and with a locally constant operator,

\[
\mathbf z_{n+2}=M^2\mathbf z_n.
\]

The operator is locally reversible only if \(\det M\neq0\). Its eigenvalue magnitudes describe contraction or growth
of its modes; phases describe rotation; off-diagonal entries describe cross-axis transfer. A single three-state
trajectory does not identify all nine free coefficients of \(M\). Estimation needs constraints, multiple independent
nodes/initial conditions, or a longer time series with held-out data.

Keep two objects distinct:

1. \(\mathcal A^{(9)}_n=[\mathbf z_n,\mathbf z_{n+1},\mathbf z_{n+2}]\): the observed nine-reading packet;
2. \(M\): a candidate nine-coefficient transition law acting on the packet.

They may be related but they are not identical.

### Historical provenance and correction

- `archive/numbered_tests/197_formula_nine.py` used three parent channels, each decompressed into three subchannels,
  with a phi-period cascade and fitted forecasting machinery.
- `archive/numbered_tests/201_phi_nine.py` explicitly used “three systems x three axes = nine couplings” and linked
  phi powers to a very small solar sample.
- `archive/numbered_tests/196_formula_cubed.py` belongs to the same historical expansion family.

Those scripts preserve genuine prior conception of three-by-three coupling. They do not establish the current
two-step local stencil or a universal operator. Their phase scans, assumed phi cascades, tiny samples, and inspected
targets prevent their numerical fits from serving as clean evidence for the present formulation.

## 7. Pi Leak, Bedrock, and Hexagon–Pentagon: related candidates, not one number

### Pi Leak — exact boundary geometry

For a regular hexagon inscribed in a same-radius circle,

\[
P_{hex}=6r,
\qquad
P_{circle}=2\pi r,
\]

so the normalized perimeter difference is exactly

\[
\boxed{
\ell_{\pi}
=1-\frac{P_{hex}}{P_{circle}}
=1-\frac{3}{\pi}
=\frac{\pi-3}{\pi}
\approx0.045070341.
}
\]

Keep it distinct from the raw topology remainder

\[
\pi-3\approx0.141592654.
\]

The ratio is exact geometry. It is not automatically an energy-loss law. Archived script 103 directly falsified a
universal per-cycle minimum-loss reading using high-Q oscillators and found no clean Landauer relation. Archived
script 104 supplied the exact circle/hexagon boundary result and a redistribution/shared-boundary interpretation.
Pi's irrationality alone does not imply entropy and does not prevent a dynamical state from completing a cycle.

Script `200c_no_pi_leak.py` proposed \(1/\phi^4\approx0.145898\) as a replacement for the raw \(\pi-3\) remainder.
The values are close but not equal, and that old forecast family does not establish the replacement.

### Bedrock — radial or handover diversion reference

The current Bedrock reference distinguishes the raw ARA-width mismatch

\[
G=2-\phi=\phi^{-2}\approx0.381966
\]

from a normalized width-two diverted share

\[
g=\frac{2-\phi}{2}\approx0.190983,
\qquad
\tau=\frac{\phi}{2}\approx0.809017,
\qquad
g+\tau=1.
\]

If \(Q_k\) is a typed one-pass quantity and \(\lambda_k\) is the locally measured irrecoverable fraction of the
diverted share, then the working bookkeeping is

\[
L_k=\lambda_k gQ_k,
\qquad
R_k=(1-\lambda_k)gQ_k.
\]

\(L_k\) is permanent loss under the declared boundary; \(R_k\) is available for storage or cross-rung recycling.
`THE_BEDROCK_REFERENCE.md` explicitly labels this as a posited one-pass reference rather than an empirical universal
constant. Real-system estimates scatter widely. The same-junction recycling shortcut failed as a forecaster; the
current architecture routes possible return through separately measured lower-rung reservoirs and gates.

### Hexagon–Pentagon — exact angular defect

Six equilateral triangles around a vertex sum to \(360^\circ\) and close flat. Five sum to \(300^\circ\), leaving

\[
\boxed{\Delta\theta=60^\circ},
\qquad
\frac{\Delta\theta}{360^\circ}=\frac16.
\]

The angle is an exact curvature/closure fact, not an energy percentage. The empirical proposal that a lock angle
should climb from \(60^\circ\) toward \(72^\circ\) with ARA was not supported, and the later ARA/angle/loss surface
also failed. The geometry remains a useful candidate orientation or curvature coordinate; its energy conversion is
open.

### Current coordinate hypothesis

The cleanest non-conflating possibility is:

- Pi Leak: circumferential/boundary mismatch or lateral redistribution;
- Bedrock: radial/scale/handover diversion before recycling;
- Hexagon–Pentagon: angular/curvature/orientation defect.

They may be three coordinate views of one local handover, but no unit-valid transformation currently connects

\[
0.045070341,
\qquad
0.190983,
\qquad
\frac16.
\]

Do not equate, add, or multiply them without a predeclared physical model.

## 8. Candidate unified operator, with an honest ceiling

The three-axis operator offers a place to test the candidate coordinates without assuming they are the same number:

\[
\mathbf z_{n+1}=M\mathbf z_n+\mathbf u_{n+1}.
\]

Possible assignments to test—not facts already established—are:

- diagonal magnitude/retention terms: direct same-axis persistence;
- off-diagonal terms: lateral boundary transfer or coupling into another axis;
- eigenphase/rotation: angular handover or holonomy;
- residual input \(\mathbf u\): external forcing or delayed recycled return;
- unexplained contraction after a full typed account: candidate irreversible loss.

The conservation fence is that an apparent local “leak” may be transfer outside the selected axis or rung rather than
destruction:

\[
Q_{in}
=Q_{direct}
+Q_{lateral}
+Q_{down}
+Q_{diss}
+\Delta Q_{stored}.
\]

Only the term that remains missing after widening the measured account, tracking return, and including stored change
may be called irreversible dissipation under that boundary.

The constant worth testing is therefore an intrinsic **one-pass local operator or invariant before recycling**, not
the final net loss of an arbitrarily bounded system.

## 9. MX8 result reclassified without erasure

MX8 established the exact binary tetrahedron, parity closure, Hadamard four-route decomposition, and nine-slot
state/relation count. Its held-out Warp plasma test of one global relation-strength coefficient failed:

- relative L2: additive `0.131527553`, relation `0.131517869`;
- favourable change: only `0.00736%` against a frozen `5%` gate;
- bootstrap interval crossed zero;
- direction was microscopically worse;
- the simpler marginal baseline (`0.123651`) remained better.

This remains a valid null for the tested hypothesis: one average interaction coefficient learned in early time does
not transfer as the local route-strength law. It did not test the full continuous E/B temporal triangle, the current
ARA^9 local operator, or representation invariance. It should be retained as a side branch that demonstrates why a
global relation scalar can flatten strong positive and negative local couplings toward zero.

## 10. Parked tests produced by this formalism

### RT1 — representation consistency

Choose one independently measurable physical relation and two representations. Freeze \(\Pi\), \(\mathcal C\), and
\(\mathcal C_{\Pi}\) before target outcomes. Test whether coupling then projection agrees with projection then
coupling. Compare the discrepancy against measurement noise and simpler baselines.

### A9 — local three-axis, two-step operator

Use many independent local triplets or multiple initial conditions. Estimate a constrained \(M\) on training data
only. Predict \(\mathbf z_{n+1}\) and \(\mathbf z_{n+2}\) on held-out times, nodes, and ideally another simulator or
physical system. Compare against persistence, independent-axis, unrestricted linear, nonlinear, and shuffled-axis
controls. Test whether the same declared operator or invariant transfers across one adjacent rung.

### EL1 — leak-coordinate discriminator

Measure, in common physical units, the incoming amount, direct retained/output amount, lateral transfer, lower-rung
deposit, delayed return, stored change, and irreversible dissipation. Fit the three geometric candidates separately
on training data. Do not manufacture all measurements from the same quantity. Require held-out prediction of at least
one independently measured flow. A result showing only accounting closure is calibration; novelty requires the
ARA-constrained operator to beat standard domain models or predict a new withheld relation.

## 11. Plain-language summary

The simplest current picture is still “two things and what happens between them.” That is ternary because keeping the
relationship is necessary to lock the identity, even though only two starting waves were supplied. It is reversible
only while the relationship has been kept in enough detail. It scales by letting a closed relation become a new
parent without throwing away its inside.

ARA^9 is now most cleanly read as three axes measured at the centre, one step later, and two steps later. Those nine
readings can be used to look for a local three-axis rule. The rule may reveal that what looked like lost energy simply
moved sideways, moved down a rung, or returned later. Pi Leak, Bedrock, and the Hexagon–Pentagon defect are presently
three different geometric candidates for those different directions. Their geometry is real; the claim that they
are one physical handover law is the part still needing a clean test.
