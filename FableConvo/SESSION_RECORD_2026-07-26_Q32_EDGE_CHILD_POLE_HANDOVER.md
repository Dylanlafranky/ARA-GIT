# Session record — Q32 edge-child pole handover

**Date:** 26 July 2026  
**Ledger:** T286  
**Thread:** quantum singularity evidence before revisiting the 1.5/3.5 route

## User direction

Dylan clarified that the singularity flip already has a wider test lineage and
that the immediate task was its quantum appearance. Before retrying `3.5`, he
proposed looking for movement coming into and leaving the lattice edge and
opening the receiving children, which might begin near an asymmetric pole.

## Correction to Q30

Q30 represented:

- `1.5` as the unique triangle-closing edge between the nonshared endpoints of
  source and child;
- `3.5` as source-to-child span `2` plus that closing edge.

That route failed. Q32 did not alter Q30. It changed the order of investigation
by first asking whether an empirically selected receiving child exists.

## Frozen Q32 translation

- source: local ARA `x>=1.5` and one-step release;
- exact child: active pair sharing the named endpoint, selected only by lowest
  starting `x`;
- outcome: later signed gain, positive accumulation, release/accumulation
  overlap and descriptive flow ARA;
- controls: nonadjacent topology, seed displacement and time displacement;
- development chose lag; evaluation kept it fixed;
- all exact children were retained for a starting-coordinate gradient.

## Result

Development selected lag `1`. The evaluation half contained `23,591` events
across all `200` branch/seed strata.

Exact signed child gain:

- exact `+0.07565`;
- topology `+0.03302`;
- seed `+0.00700`;
- time `+0.00491`.

Exact overlap:

- exact `0.14051`;
- topology `0.01965`;
- seed `0.06785`;
- time `0.07174`.

All incoming and coupling gates passed, all associated cluster-bootstrap
probabilities were `1.000`, and both connectivity strata had the same
direction.

## Pole result and important gradient

The child was not usually still at the low pole when source release was
observed:

- median start `0.6311`;
- `27.59%` at `x<=0.25`;
- `43.52%` at `x<=0.5`.

The frozen pole-origin claim therefore failed. But the child's starting ARA
position strongly ordered its next movement:

- pole `+0.19186`;
- lower gradient `+0.10168`;
- upper gradient `+0.01922`;
- crest `-0.13716`.

The likely next cut is backward in time: the receiving child may already have
left the pole before the source's visible release slice.

## Method caveat

The topology control selected the minimum from several nonadjacent candidates,
whereas an endpoint usually supplied one exact child. Its starting-position
comparison had unequal order-statistic pressure. This is recorded rather than
repaired post-result. The absolute pole gate also failed, while exact movement
still beat the one-candidate seed and time controls.

## Scientific boundary

Q32 supports an immediate ordered edge-child handover inside one public
simulator. It remains:

- retrospective;
- from the same source lineage as Q27–Q31;
- exactly diagonal;
- not a universal flip, Phase B, conserved energy or completed `3.5` route.

## Durable files

- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_FIDELITY_v1.md`
- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_PROTOCOL_v1_FROZEN.md`
- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_RESULTS.json`
- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_REPORT_2026-07-26.md`
- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_GEOMETRY.png`
- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_NOTEBOOK.ipynb`
- `analysis/quantum/Q32_EDGE_CHILD_POLE_HANDOVER_VALIDATION.json`

