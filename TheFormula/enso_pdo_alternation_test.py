"""Test Dylan's sharpened PDO idea: at the green/gold crossing PDO "takes over" = reversed
trough/crest every ~2nd ENSO wave (period-doubling), with PDO's slow phase selecting which
alternation state. Does PDO-as-alternation-gate LIFT the gold-engine-phase direction skill (~0.73)?
Strict-causal. Real NINO3.4 1870+ & ERSST PDO. (Logic in /tmp run.)

RESULT — NOT supported (all 3 parts):
 1) NO period-doubling: gold-engine event-magnitude lag-1 autocorr = +0.63 (consecutive events
    SIMILAR, not alternating; true alternation needs NEGATIVE lag-1). Subharmonic power
    (95-130mo)/engine(45-70mo) = 0.14. Sign-flip fraction of mag-steps = 0.42 (<0.5).
 2) PDO phase does NOT select the alternation state: corr(PDO, state) = -0.016; sign(PDO) predicts
    state at 0.468 (below chance).
 3) +PDO gate HURTS direction skill: h=18 0.560 vs engine-phase 0.732; drags toward chance at all h.
 => PDO carries no ENSO-predictive info in this framing either. Engine-phase direction (~0.73 @
    18-24mo) remains the sole keeper. (Real decadal ENSO amplitude modulation exists but is not a
    clean 2-cycle PDO-gated reversal.)
"""
