# Session log — 9 June 2026

The river/geometry session. Started from "does anything take energy OUT of ENSO?", ran through the whole
river-landscape picture, and ended trying to build the morphed fractal sphere as a literal geometry-driven
engine. Two validated findings, several clean nulls, and one still-unfinished 3D model. Dylan's verbatim
quotes on energy movement + geometry captured in `Retrodiction/MORPHED_SPHERE_MODEL_AND_QUOTES.md`.

## The arc
1. **Siphons** — ENSO does shed energy (→WWV +0.011, →IOD +0.005, →PDO +0.001 = the 0.382 shed; SOI = anti-phase
   mirror). But siphons are FOLLOWERS: null for value, null for wobble (gap-imbalance lead +0.18 failed
   split-half). A drain echoes the source's past, already in memory.
2. **Catchment recombination** — combining a shattered crest's spike-cluster = amplitude DISCRIMINATOR (24mo
   event-size +0.77 vs +0.61), not a height recovery; spread-only (fails ECG QRS).
3. **Watershed / φ-thalweg** — φ is the fast lane on the slope; valley floor (fastest) at balance ~1.0.
   ⭐ **Conserved φ-thalweg VALIDATED**: high-energy φ-lane +28–33% calmer than the turbulent middle
   (bootstrap P≈0.99), advantage grows with energy = beats regression-to-mean. ENSO only (concentration rule).
4. **Regime map VALIDATED** — terrain position predicts behaviour: bank→snap (+0.38/+0.46/−0.30), channel→
   bounded oscillation. Causal, non-circular.
5. **Geometry-driven forecast** — pure rollout (energy on the flow, no regression) gives DIRECTION +0.40→+0.68.
   2-axis ≡ clock; **3-axis (engine+wobble+energy) beats the clock** +0.02–0.07 value. Molding the full sphere
   added real (small) drive. Value still goes to regression — geometry delivers direction + confidence.
6. **The morphed sphere build** — long iteration toward Dylan's vision (canonical axes Mapping/Rung-fractal/
   Coupling, multiple ARAs from multiple directions morphing the surface, fractal, run by rotating+wobbling
   with gravity-vs-counter-wave). Mechanism captured; **terrain still too smooth ("keeps flattening")** — not
   finished. Live run demo: `3D Models/ENSO_running_on_morphed_sphere.html`.

## Nulls flagged (don't re-walk)
Siphons-as-predictor; forward-turbulence (middle loses BOTH value+direction); clock-centralizes-turbulence
(symmetric synth puts error at extremes); collision-by-max-error (regression-to-mean confound);
terrain-weighted multi-scale blend ≡ equal-weight averaging.

## Dylan's standing model (his words, condensed)
Training morphs a fractal sphere; **multiple ARAs from multiple directions (the axes) morph the surface**;
**φ-thalwegs = conserved high ridges the energy rides, the middle = the low dissipative sink** where opposing
flows collide and cancel; clocks = symmetric banks colliding at 1.0, bends shift the collision; **turbulence
is forward** (cancels then is propelled on); to run it, **calibrate position, rotate + wobble, energy rolls to
the lowest unless the counter-wave lifts it up a ridge.** Full quotes in MORPHED_SPHERE_MODEL_AND_QUOTES.md.

## Open thread (next time)
Finish the morphed sphere properly: surface-morph on canonical axes, **genuinely fractal** (rung-nested + real
training roughness, hi-res, minimal smoothing — stop flattening), conserved relief, then **run it** (calibrate
→ rotate+wobble → gravity-vs-counter-wave) and score the path against truth. Pick-up checklist in the model doc.
