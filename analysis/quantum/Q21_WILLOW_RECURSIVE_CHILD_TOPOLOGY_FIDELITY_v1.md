# Q21 ARA fidelity packet — recursive child topology beneath the parent ridge

**Recorded:** 26 July 2026  
**Claim:** `Q21-WILLOW-RECURSIVE-CHILD-TOPOLOGY-v1`  
**Purpose:** preserve Dylan's geometry before the fresh logical outcomes are opened

## Governing ARA correction

The Q20 global average sitting near `1.0` is not, by itself, a surprising
failure or evidence that the detector cloud is locally quiet.

When all children of one parent are measured together, their opposing local
asymmetries can cancel at the parent's `1.0` ridge. This parent closure was
encountered repeatedly in the LLM work:

\[
\underbrace{\text{children above and below their local ridges}}_
{\text{retained local asymmetry}}
\xrightarrow{\text{coarse-grain}}
\underbrace{\text{parent near }1.0}_{\text{expected whole reading}}.
\]

Q21 must therefore look beneath that parent reading. It must not demand that
the recompressed parent move far from `1.0`.

## Registered geometry

The detector plane is opened along physical `x` and `y`. Soft ARA gradients
form four spatial children in circular order:

\[
AA\rightarrow AB\rightarrow BB\rightarrow BA\rightarrow AA.
\]

Each child is then opened along cycle/time into two grandchildren:

\[
c_A,\qquad c_B.
\]

The resulting eight grandchildren are retained separately. They sum to one
parent TE-ARA:

\[
\sum_{c\in\{AA,AB,BB,BA\}}
\left(G_{cA}+G_{cB}\right)=2.
\]

Q21 also preserves all sixteen directed handovers from each spatial child at
one time slice to every spatial child at the next:

\[
H_{c\rightarrow d},\qquad c,d\in\{AA,AB,BB,BA\},
\]

with:

\[
\sum_{c,d}H_{c\rightarrow d}=2.
\]

The twenty-four retained coordinates are:

- eight child/time grandchildren;
- sixteen directed child-to-child handovers.

Only the control models are allowed to recompress these into the parent
coordinates.

## Tier and capacity interpretation

Every opened child can be normalized locally to its own `0–2` ARA. Relative
to the parent boundary, however, a completed tier down has half the parent's
maximum capacity. Q21 preserves this distinction by:

- keeping the eight grandchildren as allocations of one parent TE-ARA `2`;
- separately keeping the sixteen handover allocations as one relational
  TE-ARA `2`;
- never adding the two totals and calling the result physical energy.

These are two cuts through the same event identity: composition and local
movement.

## Fresh-data fence

Development uses the already-open `d5_at_q4_7/r13` records.

Fresh scoring uses `d5_at_q6_5/r30`. Before this packet and protocol were
frozen, only these fresh-patch members were extracted:

- `metadata.json`;
- `circuit_ideal.stim`;
- `detection_events.b8`.

No `obs_flips_actual.b8` member from `d5_at_q6_5` had been extracted or read.

## What would count as support

Support requires the retained child topology to predict the untouched logical
outcome better than:

- the recompressed `x–y` parent;
- the failed Q20 global `x–time` compression;
- event count;
- a frozen spatial-coordinate misassignment;
- development-label permutations.

If the local construction does not clear those controls, the correct result
is that this particular child/topology cut is insufficient. It does not
license changing the geometry after the fresh result is visible.

