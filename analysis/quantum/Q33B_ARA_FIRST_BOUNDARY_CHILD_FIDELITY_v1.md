# Q33B fidelity packet — ARA-first boundary-child route

**Date:** 26 July 2026  
**Ledger:** T288  
**Status:** translation lock before Q33B outcome calculation  
**Source:** already-open Q27/Q28 public simulator lineage

## Geometry first

ARA is the invariant generating geometry. Physical energy, amplitude,
connection strength and coupling load are variable flows over it.

Q33 incorrectly used an unbounded raw capacity ratio to redefine a rung
coordinate. Q33B forbids that substitution.

The adjacent-rung landmark is treated as one fractal location seen from two
frames:

- child frame: singularity/boundary;
- parent frame: `1.0` ridge.

A complete boundary child contributes `1` at its own rung. Projected one
octave upward:

\[
\mathcal R_\uparrow(1_c)=\frac12.
\]

This coefficient is fixed by the declared ARA geometry. It is not estimated
from raw energy.

The ordered route is therefore

\[
\underbrace{2}_{\text{complete same-rung span}}
+
\left(
\underbrace{1}_{\text{current-rung contribution}}
+
\underbrace{\frac12}_{\text{boundary child one octave up}}
\right)
=
\frac72.
\]

Every eligible event receives this same structural coordinate. Q33B does not
ask whether a raw amplitude happens to equal `0.5`.

## Directed boundary child

The Q33 source has two endpoint recipients. Q33 averaged them; Q33B preserves
the directed route.

The source is releasing from its high/`2` side. After the singularity flip,
the receiving child is expected to enter from its low/`0` side. Among the two
endpoint recipients, choose the one with the smaller development-normalized
starting closure:

\[
z_c(t)
=
\frac{h_c(t)}{Q_{0.95}^{dev}(h_c)},
\qquad
h_c=|\det C_c|^{1/3}.
\]

This monotone ratio is used only to order which child is nearer the declared
low boundary. It is not promoted to an exact bounded ARA coordinate.

Ties are broken by pair index. No future recipient value participates.

## What is empirically scored

ARA supplies the route. The simulator supplies the flow over it.

For the chosen child:

\[
g_c(t)
=
\frac{h_c(t+1)-h_c(t)}
{Q_{0.95}^{dev}(h_c)}.
\]

The flip-direction prediction is:

- source closure falls;
- the chosen low-boundary child closure rises;
- its rise exceeds the sibling and relation-broken routes.

Raw connected-matrix energy gain is retained as a secondary flow diagnostic.
It cannot change the structural `0.5`.

## Controls

1. **Sibling:** the other exact endpoint recipient.
2. **Topology:** baseline-match two non-endpoint active relations to the exact
   two recipients by starting `z`, then apply the same lower-`z` boundary
   selection.
3. **Seed:** apply the complete two-endpoint selection at seed `+37`.
4. **Time:** apply it at the fixed within-split time `+137`.

Every comparison therefore applies the same one-of-two boundary rule.

## Evidence claim

Q33B can support or reject the predicted **flow routing consequence** inside
this simulator.

It cannot establish the octave coefficient by recovering `0.5` from energy,
prove a universal singularity flip, or validate the dark-sector
\(\varphi^{3.5}\) ratio.

