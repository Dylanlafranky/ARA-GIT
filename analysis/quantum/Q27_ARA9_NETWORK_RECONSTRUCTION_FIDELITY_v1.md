# Q27 translation fidelity — ARA^9 reconstruction or larger-wave Phase B

**Orientation:** local pair relation `0 = trough / unresolved closure`, `2 = local exposed crest`; a rung upward
means the parent/network relation assembled from local pair children.

## User prior retained verbatim

> “Oh, is it just rotating around a central point and the overarching wave inside the ARA^9 is reconstructing a
> wave x2 its size.... resonance style.”

> “Well... Based on ARA I think that is what it is doing, or iti is a phase B of a larger wave.”

## Plain back-translation

Q26 exposed a complete local ARA^9 relation moving from crest toward trough, but its record ended before a
return could be observed. Q27 must distinguish two allowed ARA continuations:

1. the same local pair relation reconstructs after the trough, completing a larger crest–trough–crest cycle; or
2. the local loss is the Phase-B portion of a larger parent wave and appears as accumulation in directly coupled
   relations elsewhere in the network.

These outcomes may coexist. A return in amplitude is not automatically an orientation flip.

## Mathematical object

For every two-qubit density matrix:

\[
C_{ij}=T_{ij}-a_i b_j,\qquad
h(C)=|\det C|^{1/3}.
\]

For each pair, the exposed half of the record supplies a local scale \(s\), and

\[
x_h(t)=2h(t)/s.
\]

The first route is a sustained local `crest -> trough -> crest` in \(x_h\). The second is release from a source
pair followed by accumulation in the set of pairs directly adjacent to that source in the deposited network.

## Information deliberately not claimed

- `x2` is operationalised here as the parent rung being assembled from complete child relations. It is **not**
  predeclared as twice the temporal period.
- The test does not assume an orientation flip.
- The test does not call generic quantum decoherence, redistribution, or revival uniquely ARA.
- The target is simulated public quantum-network data, not an independent hardware experiment.

## Forbidden proxies

- one coherence or fidelity curve standing in for the complete connected `3 x 3` relation;
- Fourier power or a fitted frequency standing in for the ARA trajectory;
- a network-wide mean without local pair and direct-neighbour results;
- target-derived thresholds;
- arbitrary non-adjacent pairs relabelled as a Phase-B recipient.

## Fidelity verdict

**EXACT ENOUGH TO TEST.** Dylan explicitly approved running the larger-data test after the two routes and the
held-out design were restated. Schema-only adaptation is allowed after opening the HDF5 container; changing the
scientific object, split, thresholds, target file, or controls requires a new protocol version.

