# Q18 translation-fidelity packet - grouped-diameter residual geometry

**Claim ID / version:** `Q18-GROUP-RESIDUAL-v1`  
**Captured:** 26 July 2026  
**Orientation:** every grouped diameter is reversible under `x -> 2-x`  
**Evidence class:** `(E/R)` exploratory follow-up on the already-open Q16/Q17 deposit

## Dylan correction and prior

- At the grouped parent rung, use **Phase A** and **Phase B**.
- Reserve **anti-phase** for the pure reversal revealed at the child-below rung.
- Q17 showed that two children may share a pole group without being opposite poles of each other.
- Remove one grouped diameter and test whether the other two grouped diameters remain.

## Frozen ARA object

The four children support three candidate grouped diameters:

\[
D_U=(C00+C01)-(C10+C11),
\]

\[
D_V=(C00+C10)-(C01+C11),
\]

\[
D_J=(C00+C11)-(C01+C10).
\]

Each expression places two children on Phase A and the other two on Phase B for that diameter. `D_J` is the
Coupling-ARA diameter.

Q18 mathematically removes one frozen diameter cut from the 45-cut measured sphere, then asks whether the other
two grouped Phase A/B diameters remain stable in holdout records.

## Primary and secondary branches

- **Primary:** remove `D_J`, because Q17 selected its two-child pole grouping as the strongest repeated division.
- **Secondary symmetry audit:** repeat after removing `D_U` and `D_V`.

The primary is frozen before Q18 calculation. The secondary audit cannot replace a failed primary verdict.

## Permitted operations

1. Derive all grouped directions from development records only.
2. Remove one direction by orthogonal projection in the 45-cut ARA measurement space.
3. Apply the identical projection to holdout records.
4. Re-estimate only the two remaining grouped directions inside the development residual.
5. Score leakage, retained energy, persistence, Phase A/B classification, residual dimensionality and four-child
   recovery in holdout.
6. Use balanced-label shuffles and within-one-archive pseudo-children as controls.

## Forbidden substitutions

- no established preparation names in construction or scoring;
- no density matrix, Pauli, Ramsey or Hahn coordinates;
- no holdout rotation or threshold repair;
- no calling the mathematical projection a physical removal;
- no treating exact development removal as empirical evidence;
- no using "anti-phase" as a synonym for grouped Phase B;
- no changing the primary removed diameter after seeing Q18 results.

## Plain bridge

| Native ARA | Established mathematical operation |
|---|---|
| Freeze one grouped ARA diameter. | Estimate a development-only contrast vector. |
| Remove that diameter from the measured sphere. | Orthogonally project records into its complement. |
| Look for the other two Phase A/B diameters. | Recompute the two remaining grouped contrasts in residual space. |
| Test whether they survive the next time slice. | Apply frozen residual axes to holdout records. |

## Fidelity verdict

`EXACT ENOUGH TO TEST`

## Evidence fence

Development residual rank reduction is guaranteed by projection and is not evidence. Empirical content comes
from low holdout leakage, preserved remaining-axis energy and direction, held-out Phase A/B decoding, held-out
four-child recovery, and failure of randomized controls to reproduce the complete result.
