# Q49 external time-vector fidelity packet v1

**Claim ID:** Q49-ETV-v1  
**Date:** 30 July 2026  
**Status:** `EXACT ENOUGH TO TEST` — reconfirmed by Dylan after context
compaction  
**Ledger:** T309

## USER PRIOR — verbatim

> “I didn't want the movement turns, we know in relation they already go in a
> circle. I wanted the overall vector of movement, of the circle stretching
> THROUGH time to be pointed in the 1/e - Phi directional path. You're
> measuring the wrong relation.”

After the corrected back-translation:

> “Yes, that sounds good.”

## Identity/system being measured

The **complete internally rotating quantum ARA circle as one moving
identity**, at one observable-pair lineage and one completed parent cycle.

## Axis or relation

The external/meta centreline made by the successive positions of the whole
circle's centre through time.

This is not an internal angular coordinate. The test concerns the direction
in which the entire rotating circle is carried forward.

## Ordered poles and direction

Express centreline heading as a fraction of one full turn:

\[
h=\operatorname{frac}
\left[
\frac{\operatorname{atan2}(\Delta c_v,\Delta c_u)}{2\pi}
\right].
\]

The declared oriented carrier arc is:

\[
\underbrace{\frac1e}_{\text{local ARA 0}}
\longrightarrow
\underbrace{\operatorname{frac}(\phi)=\phi-1}_{\text{local ARA 2}}.
\]

Full Phi is the upper pole. Anti-Phi \(2-\phi\) is **not** the upper pole in
this test.

## Scale/rung origin

Each centre belongs to one completed parent cycle of the already established
Q39 ARA phase-plane circulation. The centreline tangent acts one relational
level outside that internal circulation.

## Observable needed

1. Fit the centre of every complete local phase-plane circle.
2. Order those centres by parent cycle.
3. Use a centred finite difference
   \(\mathbf d_r=\mathbf c_{r+1}-\mathbf c_{r-1}\) as the primary external
   tangent.
4. Convert the tangent heading to unit turns \(h_r\).
5. Test whether headings preferentially occupy and traverse the oriented
   \(1/e\rightarrow(\phi-1)\) arc.

## `3/8` triangulation landmark

\[
\frac38=0.375
\]

lies close to the \(1/e=0.367879\ldots\) end of the declared heading arc.
It is a possible endpoint-area triangulation landmark, not the carrier
midpoint and not a forced gate.

## Permitted decompression

- circle-fit centre, centroid centre, and extrema-midpoint centre as
  estimator sensitivity checks;
- tangent magnitude relative to circle radius;
- development/evaluation strata;
- ordered heading runs and forward/return traversal;
- matched quarter-turn rotated arcs as controls.

## Forbidden substitutions/proxies

- internal phase angle around the circle;
- parent-to-parent angular turning amount;
- average of quadrant flip distances;
- \(k/8\) closure-state bands;
- anti-Phi as the upper pole;
- Fourier-generated carrier coordinates;
- treating a static heading histogram alone as proof of a wobble.

## Wrong-object conditions

The construct is wrong if the script measures how far the circle rotated,
how many internal strands flipped, or where an internal point sits on the
circle instead of where the **whole circle's centreline points through time**.

## Plain restatement

The quantum identity is already rotating internally. Ignore that known
rotation. Watch the whole rotating circle move through time as though it were
a spinning wheel being carried along a road. Draw the road through the
successive wheel centres and measure which way that road points. Test whether
that direction lies and moves along the arc from \(1/e\) to full Phi.

## Mathematical representation

\[
\mathbf c_r
=
\operatorname{CircleCentre}
\left\{(u_t,v_t):t\in\text{parent cycle }r\right\},
\]

\[
\mathbf d_r=\mathbf c_{r+1}-\mathbf c_{r-1},
\qquad
h_r=\operatorname{frac}
\left[
\frac{\operatorname{atan2}(d_{r,v},d_{r,u})}{2\pi}
\right],
\]

\[
x_r
=
2\,
\frac{h_r-1/e}{(\phi-1)-1/e}
\quad\text{for headings on the declared oriented arc.}
\]

## Back-translation

Each internal cycle is reduced only to where its whole circle is centred.
Three consecutive centres reveal the direction of the larger path carrying
those circles through time. That direction, not the internal spin, is placed
between the declared \(1/e\) and Phi poles.

## AI assumptions/additions

- The observable phase-plane circle centre is an adequate projection of the
  whole identity's external position in this simulator.
- The phase-plane \(u\) and \(v\) axes provide a consistent orientation across
  cycles within a lineage.
- Full Phi must be reduced modulo one only because heading is measured in
  full turns; this retains \(\phi-1\), not anti-Phi.
- A three-centre tangent is the smallest symmetric estimate of the external
  vector.

## Information discarded

- dimensions outside the two-coordinate Q39 phase plane;
- absolute physical space;
- off-axis connected-matrix structure absent from the diagonal simulator;
- any centreline movement too small to distinguish reliably from numerical
  or circle-fit jitter.

## First possible distortion

The largest remaining bridge assumption is that movement of the fitted
phase-plane centre represents the whole circle's external time extrusion.
The test must report tangent magnitude and centre-estimator agreement so
near-zero jitter cannot masquerade as a meaningful direction.

## Dylan fidelity verdict

`EXACT ENOUGH TO TEST`

