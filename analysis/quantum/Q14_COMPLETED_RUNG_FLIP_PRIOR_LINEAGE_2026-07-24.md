# Prior lineage of the completed-rung flip rule

**Recorded:** 24 July 2026  
**Purpose:** show that the Q14 clarification is a recurrence of an older ARA rule, not a rule invented to rescue
the quantum result.

## Canonical rule

\[
\boxed{
\text{remain within the rung}\Rightarrow\text{no flip},
\qquad
\text{complete the 0–2 cycle / cross its seam}\Rightarrow\text{flip}.
}
\]

Equivalently, if \(N_{\partial T}\) counts completed TE-ARA boundaries:

\[
\boxed{
\mathbf u_{\rm destination}
=
S^{N_{\partial T}}\mathbf u_{\rm source},
\qquad
S=
\begin{pmatrix}0&1\\1&0\end{pmatrix}.
}
\]

This compact operator notation is new to Q14. The underlying conditional rule is not.

## 1. Formula/engine lineage — 10 June 2026

`TheFormula/18 - Recoil, phi-rung pump & singularity-flip stack
(11-06-26)/SINGULARITY_FLIP_CONJECTURE.md` states explicitly:

- the flip is **not** uniform rung-to-rung;
- it occurs only when the trajectory goes over the singularity;
- the whole `360°` / `0–2` cycle must be filled;
- staying within a rung without reaching `0` or `2` produces no flip.

That document arose because uniform per-rung phase locking was null. The singularity-gated formulation reconciled
the result but was retained as a conjectural geometric lens. Phase slips concentrated at low energy in ENSO,
sunspots and QBO; only two systems exceeded different null endpoints, and direct value incorporation hurt.
Therefore this is prior theoretical and soft diagnostic lineage, not a universally confirmed law.

## 2. Prime-number lineage — 22 July 2026

### PN29 and PN30

`analysis/primes/PN29_DYNAMIC_FLIP_AMENDMENT_2026-07-22.md` records that PN29 had held child-pair orientation
fixed despite the already-declared singularity flip.

`analysis/primes/PN30_DYNAMIC_RELATIONAL_FLIP_REPORT.md` restored reversible orientation using each child's
normalized cycle progress:

\[
\theta_w(N)=\frac{N\bmod w}{w},
\qquad
x_{B\to A}=2-x_{A\to B}.
\]

The reflection reversed signed displacement around the `1.0` ridge while preserving its magnitude. On a fresh
interval it changed hard unresolved-composite AUC:

\[
0.5301\rightarrow0.5663,
\]

but the frozen one-sided result was `p=0.06199`. This is suggestive directional improvement, not confirmed
prime-specific evidence.

### PN35

`analysis/primes/PN35_SAME_SCALE_GOLDEN_CROSS_REPORT_2026-07-22.md` explicitly treated eight structural children
as one parent ARA with total `2` and the doubled octave boundary as the parent's `2→0` singularity/orientation
flip.

Its proposed golden prime locator failed: lane-stratified AUC `0.497180`, every support gate failed, and the
flip model did not improve the no-flip control. Thus PN35 preserves the prior geometric declaration while
rejecting that particular prime-incidence bridge.

## 3. Physics lineage

### Multi-arm pendulum

`analysis/pendulum_scripts/PENDULUM_ARA_RESULT.md` maps a full rotation as:

\[
1\rightarrow2\rightarrow0\rightarrow1.
\]

The public free-swing recordings never reached `0` or `2`, so the report explicitly states that no singularity
flip was tested or expected. A driven tumbling dataset is required. This is the same “within rung: no flip”
classification in a mechanical domain.

### Cross-rung recycling

`EnergyRatio/ARA_CROSS_RUNG_RECYCLING_MODEL.md` distinguishes:

- a one-rung return path with orientation flip;
- a two-rung return path with same-spin return.

That is the same odd/even parity structure:

\[
S^1=S,\qquad S^2=I.
\]

The recycling model labels itself a working framework equation and is not yet empirically validated.

### Axiomatic boundary

`ARA_AXIOMATIC_PROOFS_AND_DOMAIN_SUBSETS.md`, Definition 3.2a, separates a coordinate pole-label reversal from a
physical dynamical seam crossing. It states that no universal physical crossing map is proved and that adding a
rung change or phase shift remains a domain-specific empirical choice.

## 4. What Q14 adds

Q14 is a new **quantum-domain encounter** with the earlier rule. The data strongly prefer same-label rather than
an unmatched swap between the Ramsey and Hahn child sets.

After Dylan identified the same/near-rung status, that result became consistent with the older rule:

\[
N_{\partial T}=0\Rightarrow I.
\]

This consistency is useful but not an independent confirmation of the completed-rung flip, because the rung
classification was clarified after Q14's outcome and the records do not contain a predeclared odd-boundary pair.

The new information is therefore:

1. the quantum child sets do not support an extra odd-parity swap between them;
2. their same-label correspondence is compatible with the prior within-rung/no-flip rule;
3. a direct quantum test still requires a pair frozen beforehand to straddle exactly one complete TE-ARA seam.

## Status

- **Prior rule:** clearly documented before Q14.
- **Cross-domain consistency:** present in Formula/engines, primes, pendulum classification, recycling parity and
  now the Q14 same-rung interpretation.
- **Universal empirical law:** not established.
- **Required decisive test:** predeclare seam-crossing versus non-crossing events, then test flip concentration
  and odd/even parity without assigning rung status after observing orientation.

