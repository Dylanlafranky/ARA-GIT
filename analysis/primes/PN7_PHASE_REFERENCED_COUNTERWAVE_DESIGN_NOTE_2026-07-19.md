# PN7 design note — triangulating the opposite wave from the adult connection wave

**Status:** `GEOMETRY CORRECTION RECORDED / DESIGN ONLY / NOT FROZEN / NO NEW TARGET OPENED`  
**Date:** 19 July 2026  
**Protected boundary:** the sealed PN1H p31 full-wheel target remains unopened

## Dylan's prior

> “It sounds like there is a wave itself of how the larger rung appears. We should ARA that. Then look/triangulate
> the time wave based off the space wave Phase A, its occurrence in relation, to locate the time as it'd be opposite.”

## Centered interpretation

This changes the search order.

PN3B asked whether a second coordinate extracted from absolute number-line position and future removal gate already
looked like one common larger Time-like wave. The result was negative for that representation.

Dylan's corrected proposal is:

1. identify the **adult connection wave itself** as Phase A;
2. ARA its complete appearance rather than searching the raw plane for an unreferenced second wave;
3. use where and how that Phase A recurs or moves as the first triangulation landmark;
4. look at the geometrically opposite/counterphase location for an independently measured Phase B candidate;
5. call it Time-like only if the independent measurement recurs across rungs and survives a frozen target.

Plainly: do not search an unaligned room for “Time.” First locate Space/Connection Phase A, establish which way it is
facing and moving, and then search the opposite relational direction.

## What PN6 has already supplied

PN6 directly ARA-mapped the adult connection path. For decimal rung (r) and later-gate cell (g):

\[
\underbrace{S_r(g)}_{\substack{\text{direct candidate}\\\text{survival share}}}
\longrightarrow
\underbrace{x_{A,r}(g)=2[1-S_r(g)]}_{\substack{\text{adult Phase-A}\\\text{ARA diameter position}}}
\longrightarrow
\underbrace{\theta_{A,r}(g)=\arccos[2S_r(g)-1]}_{\substack{\text{adult Phase-A}\\\text{circumference position}}}.
\]

The adult's cross-rung occurrence/movement was then

\[
\underbrace{V_r(g)}_{\substack{\text{how the adult appearance}\\\text{moves between scale rungs}}}
=
\underbrace{\theta_{A,r}(g)-\theta_{A,r-1}(g)}_{\substack{\text{same gate-relative location}\\\text{compared across two rungs}}}.
\]

PN6 showed that this movement repeats with a broadly shared withdrawal relation across candidate and edge identities.
It did **not** establish that (V) itself is the missing Time wave. It supplied a reference direction from which that
wave can now be sought.

## The proposed counterphase triangulation

The geometric counterphase of a full circular Phase-A location is

\[
\underbrace{\theta_{B,\mathrm{pred}}}_{\substack{\text{predicted opposite}\\\text{location only}}}
=
\underbrace{(\theta_A+\pi)\bmod 2\pi}_{\substack{\text{anti-phase of the}\\\text{adult connection wave}}}.
\]

This equation predicts **where to look**, not what will be found. Constructing
(	heta_B=	heta_A+pi) cannot be evidence for Phase B because the opposition was imposed by definition.

Evidence requires a second observable that was not algebraically manufactured from (S), (x_A) or (	heta_A).
That observable must show a stable landmark, sign change, extremum, lag or transfer effect at the predeclared
counterphase location.

## Two non-equivalent meanings of “occurrence in relation”

The phrase currently permits two useful readings. They must not be silently flattened into one.

### A. Vertical occurrence — how Phase A reappears across decimal rungs

```text
adult connection appearance at R8
    -> its changed appearance at R9
    -> its changed appearance at R10
    -> its changed appearance at R11
```

Here (V_r(g)=\Delta_r\theta_A(g)) is the observed movement landmark. A Time-side candidate would be an independently
measured component whose phase relation to this movement remains stable across rungs.

This is the cleaner interpretation if “the larger rung appears” means **the adult wave changing as scale increases**.

### B. Lateral occurrence — where Phase-A events appear along the number line inside a rung

```text
adult Phase-A gate position
    -> candidates released at that phase
    -> ordered positions/gaps where those releases occur
```

This revisits PN3B, but aligns the occurrence record to adult ARA phase before comparing rungs. Absolute window
position would no longer be assumed to carry a common phase on its own.

One direct, non-spectral description is the cumulative occurrence shape for events in adult-phase band (b):

\[
\underbrace{O_{r,b}(u)=2F_{r,b}(u)-1}_{\substack{\text{ARA-scaled cumulative occurrence}\\\text{along normalized number-line position }u}}.
\]

The uniform reference is (2u-1). Any retained deformation
(L_{r,b}(u)=O_{r,b}(u)-(2u-1)) must recur after alignment to (	heta_A); otherwise it is a rung-local fluctuation.

This is the cleaner interpretation if “occurrence” means **where individual connection-release events show up within
the adult identity**.

## Proposed triangulation rule

A candidate Phase B should not be accepted from either route alone. The strong version uses both:

1. **Phase-A landmark:** direct adult connection phase (	heta_A(r,g)).
2. **Vertical relation:** cross-rung movement (V_r(g)).
3. **Lateral relation:** phase-aligned occurrence deformation (L_{r,b}(u)), or another independently declared raw
   occurrence observable.
4. **Geometric prediction:** the Phase-B search region is fixed as the opposite/counterphase direction before the
   next target.
5. **Closure:** vertical and lateral routes must indicate the same orientation or lag within frozen tolerance.
6. **Fresh transfer:** that relation must improve or correctly constrain R12 without using R12 labels in construction.

In Dylan's language: Phase A tells us where we are; its cross-rung movement tells us which way it is facing; occurrence
around it supplies another edge; the intersection points toward the candidate Phase B.

## Non-tautology fences

The test fails as evidence if it does any of the following:

- defines Phase B only as (2-x_A), (-A), (sqrt{1-A^2}) or (	heta_A+pi), then “discovers” the imposed opposite;
- uses future removal stage as a prospective feature rather than an opened-data diagnostic label;
- rotates, flips or shifts each rung separately to maximize agreement;
- searches many occurrence summaries and reports only the most Phase-B-looking result;
- calls any residual “Time” merely because the connection baseline was removed;
- uses R12 to choose the occurrence observable, orientation, tolerance or winning route.

## Safe test sequence

### Stage 1 — opened development geometry

- Use already opened R7-R11 direct candidate/removal records.
- Construct the adult Phase-A reference without Fourier, SVD or NMF.
- Compare vertical and lateral meanings of occurrence explicitly.
- Choose one raw occurrence observable and one orientation.
- Record negative results and rung-specific failures.

### Stage 2 — translation-fidelity check

- Dylan confirms whether “occurrence” meant vertical, lateral or their intersection.
- Freeze the ordinary-language back-translation and mathematical operator.
- Label the proposed opposite as **Time-candidate**, not Time, before evidence.

### Stage 3 — fresh R12 discriminator

- Hash prediction and protocol before target construction.
- Primary question: does the independently measured occurrence relation recur at the Phase-A-predicted counterphase?
- Secondary question: does adding it improve the adult and/or edge path beyond unchanged PN6 and native controls?
- A negative result rejects this particular phase-referenced counterwave representation without reopening PN6.

## Current conclusion

This is a valid correction to the geometry search. PN3B's negative result remains valid for its fixed absolute-position
and future-gate representation, but it does not answer the newly stated question. PN6 has already performed the first
half by ARA-mapping how the adult connection wave appears across rungs. The missing step is a non-tautological,
independent occurrence observable aligned to that adult phase before counterphase is tested.

