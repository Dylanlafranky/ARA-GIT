# PN11 translation-fidelity packet — Phi vertical handover through resonance families

**Claim ID:** `PN11/PHI-VERTICAL-HANDOVER/v1`  
**Frozen:** 21 July 2026, before any PN11 numerical outcome  
**Dylan verdict:** `EXACT ENOUGH TO TEST`, converted under the low-energy rule from Dylan's explicit request to run
the immediately preceding mathematical test: “Can we do the math and test it to see if it is truth?”

## F0 — frozen source

**USER PRIOR — verbatim wording:**

> “I don't think you will like this, but I think it is travelling vertically up the rungs via Phi based on resonance
> and harmonic repeat, maybe as a phase a and phase b. It supports its progress by creating information lock
> structures.”

**Identity/system being measured:** one fundamental prime-child lock and the integer-multiple lineage that repeats
that same distinct child set until the first previously absent prime child joins.

**Ordered poles and direction:** `2` is complete occupation by the existing distinct-child lock; `0` is complete
occupation by repeat/multiplicity content. Moving upward/larger through multiples decreases the old lock share and
increases the repeat share. When a new child is incorporated, the expanded lock is read as a new identity at `2`.

**Scale/rung origin:** a squarefree fundamental full resonance `B` with at least three independently active prime
children. Its repeat multiplier `k` is the vertical/larger coordinate, so the lineage node is `n=kB`.

**Invariant relational claim:** harmonic repetition carries the identity from the locked pole toward the opposite
share; Phi is a preferred handover region for incorporating the next child and closing a larger information lock.

**Permitted decompression:** retain the old lock, repeat echo, exact `0-2` closure, every intermediate multiplier,
the first new-child event, and the reset into the expanded lock. Stratify by the first missing child and inspect the
canonical primorial-prefix ladder as a sensitivity.

**Forbidden substitutions/proxies:**

- do not count the inevitable crossing of any interior landmark as support;
- do not select only the 510 family or only visually attractive examples;
- do not move the Phi value, tolerance, family definition or target interval after seeing the result;
- do not use assigned musical pitch or perceived harmony as evidence;
- do not count the algebraic identity `A+B=2` as evidence that Phi is preferred;
- do not treat several correlated views of the same factorisation as independent confirmations.

**Observable needed:** the old lock coordinate at the first new-child incorporation, its distance from Phi and fixed
nearby rival landmarks, and the transition hazard inside equal-width landmark windows.

**Known ambiguity / competing reading:** the old lock can remain a nested subidentity after the new child joins. This
test uses the first expansion as the rung handover because that is the event explicitly proposed in the accepted
mathematical restatement. A later claim that “vertical travel” means another lineage operator requires a new packet;
it cannot reinterpret PN11 after the result.

**What would count as wrong object:** using arbitrary composite nodes without a fundamental base; treating every
multiple as a new identity; measuring a Phi crossing while ignoring where the child-set expansion occurs; or
flattening the two exact shares into an unlabelled ratio.

## F1 — three-view translation

### Plain restatement — AI RESTATEMENT

A closed group of prime children repeats upward through larger multiples. During those repeats, the old connection
lock occupies less of the whole node and the repeat echo occupies more. The claim is that the first new child tends
to join near the Phi handover, whereupon the enlarged child group becomes a new information lock and starts its own
cycle.

### Mathematical representation — AI TRANSLATION

For a fundamental base `B`, let `P(B)` be its distinct prime factors and let `q(B)` be the smallest prime absent from
`P(B)`. Because every prime below `q` is already in `P(B)`, nodes `kB` for `1<=k<q` introduce no new distinct prime
child. Node `qB` is the first expansion.

At multiplier `k`, define

\[
\underbrace{A_B(k)}_{\substack{\text{old-lock share}\\\text{Phase A}}}
=\frac{2\log B}{\log(kB)},
\qquad
\underbrace{E_B(k)}_{\substack{\text{repeat/echo share}\\\text{Phase B}}}
=\frac{2\log k}{\log(kB)}.
\]

Then `A_B(k)+E_B(k)=2`. The registered handover event coordinate is

\[
X_B=A_B(q(B)).
\]

The ARA Phi landmark is `phi=(1+sqrt(5))/2`; its mirror is `2-phi`. PN11 tests whether expansion coordinates `X_B`
concentrate around `phi` more strongly than equally predeclared nearby alternatives.

### Back-translation — AI TRANSLATION

Start with one fully closed child identity. Move upward by repeating it. At every step, record how much of the new
whole is still explained by the old lock and how much is repetition. When the first genuinely new child arrives,
ask where the old lock sat on its `0-2` diameter immediately before the expanded identity was relabelled as whole.
If Phi is the preferred vertical handover, those expansion readings should gather around Phi rather than around a
nearby rational or the unchanged `2` pole.

## Translation audit

**Assumptions added by the AI:**

- “new information lock” is operationalised as the first addition of a previously absent active prime child;
- vertical progression is integer multiplication of a fundamental base;
- the old-lock share, rather than the reset coordinate of the new lock, is the relevant handover reading;
- statistical preference means outperforming fixed crowded-neighbourhood landmarks on fresh families.

**Information discarded:** assigned musical pitch, the order in which factors are sounded, physical-energy meaning,
and possible couplings between different families at the same integer.

**Alternative mathematical objects:** the Phi point could instead describe the time between locks, the angular phase
of modular children, the ratio between successive fundamental bases, or a lineage that continues through every
nested child addition without resetting. Those are not silently substituted into PN11.

**First possible reversal/flattening point:** calling `A_B` Phase A fixes the old lock at the `2` side. Reversing the
orientation maps the same event to `2-X_B`; both are reported, but the registered comparison is made at
`X_B=phi`, equivalently `2-X_B=2-phi`.

## F3 — critical-field gate

| Field | Match | Note |
|---|---:|---|
| identity | 1 | one resonance family, not an arbitrary number |
| poles | 1 | old lock versus repeat echo |
| direction | 1 | increasing multiplier is upward/larger |
| rung | 1 | fundamental base to first expanded lock |
| observable | 1 | expansion position on the exact factor diameter |
| coupling | 1 | multiplication retains the base and adds repeat content |
| closure | 1 | enlarged child product resets as a new full lock |
| falsifier | 1 | Phi loses to fixed nearby rivals or lacks stable transition concentration |

**Fidelity bookkeeping result:** `1.000`; the score records translation agreement, not probability of truth.

