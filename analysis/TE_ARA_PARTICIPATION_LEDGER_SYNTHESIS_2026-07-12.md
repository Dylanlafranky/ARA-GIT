# TE-ARA participation ledger — plasma result, ENSO use and LLM use

**Prepared:** 12 July 2026  
**Status:** `SYNTHESIS / MX1 DEVELOPMENT EVIDENCE / ENSO AND LLM TESTS PROPOSED`  
**Authorial source:** Dylan La Franchi's TE-ARA clarification, interpreted and mathematically formalised with Codex  
**Canonical correction, 21 July 2026:** TE-ARA is the same ARA geometry viewed as the identity's fixed total-2
allocation. The pure identity contains only Phase A and Phase B; environmental couplings and Other belong to the
real observed account, not the pure identity. The variable quantity formerly named TE-ARA in this synthesis is the
expressed A/B subtotal \(T_{AB}\equiv T_{id}\). Frozen MX1
artifacts retain their historical wording; their numerical results are retyped, not erased. Full correction:
`analysis/TE_ARA_CANONICAL_CORRECTION_2026-07-21.md`.

## Outcome first

TE-ARA is not an additional geometry. It is the **same ARA 0–2 geometry viewed as total allocation**. The pure
identity contains only its two poles:

\[
\underbrace{\mathrm{TE}_{pure}(I)}_{\substack{\text{same ARA geometry}\\\text{pure identity account}}}
=
\underbrace{t_A^{(I)}}_{\text{identity Phase A}}
+
\underbrace{t_B^{(I)}}_{\text{identity Phase B}}
=2.
\]

For identity \(I\) observed inside environment \(\mathcal E\), the same total can be written

\[
\underbrace{\mathrm{TE}_{obs}(I\mid\mathcal E)}_{\substack{\text{identity observed}\\\text{with real couplings}}}
=
\underbrace{t_A+t_B}_{\text{expressed pure A/B pair}}
+
\underbrace{\sum_j c_j}_{\text{named environmental couplings}}
+
\underbrace{t_{\mathrm{Other}}}_{\text{unresolved contextual coupling}}
=2,
\qquad t_c=2p_c,\qquad \sum_c p_c=1.
\]

`Other` is not a third pure pole. It records contextual coupling not yet assigned at the declared boundary and slice.

For example,

\[
t_A=0.25,
\qquad t_B=1.25,
\qquad t_{\mathrm{Other}}=0.50,
\qquad \mathrm{TE}(I)=2.
\]

Plainly: the node is not “1.50 TE-ARA full.” Its complete observed account remains 2; 1.50 is the expressed A/B
pair and 0.50 is contextual coupling. Likewise, if 62% belongs to the expressed identity mode, report
`A/B subtotal T_AB = 1.24; contextual remainder = 0.76; observed TE-ARA total = 2`. ARA position says how the
declared opposed components are mixed; TE-ARA is the same geometry viewed through its total allocation.

A schematic pendulum slice illustrates the distinction:

\[
\underbrace{t_A}_{0.25}
+
\underbrace{t_B}_{1.50}
+
\underbrace{c_g+c_{air}+c_{joint}+t_{Other}}_{0.25}
=2.
\]

The A/B subtotal is `1.75`; the remainder records gravity, air drag, pivot friction and unresolved context. This is
illustrative until those contributions are measured in one common normalization. Their classification depends on
the boundary: gravity is external to a bob-only identity but internal to a pendulum-plus-Earth identity.

The same ARA geometry gives the pair's internal position:

\[
T_{AB}=t_A+t_B=1.75,
\qquad x_{A/B}=2\frac{t_B}{T_{AB}}=\frac{12}{7}\approx1.714,
\qquad T_{context}=2-T_{AB}=0.25.
\]

Thus `1.714` is the release-heavy A/B mixture position, while `0.25` is the contextual coupling remainder.

The first public-data development test found that a variable **expressed A/B allocation** transfers
materially between two views of the same plasma identity, but the current small scalar combination of ARA plus that
allocation does not improve absolute source-magnitude prediction. The fixed TE-ARA total itself cannot be a
predictor because it is always 2. The testable information is in its partition, especially in distributed systems
where several contributors and their interactions jointly form one identity, such as ENSO and LLM representations.

## Coordinates that must remain distinct

| Coordinate | Question answered | Typical scale |
|---|---|---|
| ARA \(x\) | Where is the declared Phase-A/Phase-B mixture around the 1.0 ridge? | 0–2 |
| Pure TE-ARA | Does the identity's own Phase A + Phase B geometry close? | fixed at 2 |
| Observed TE-ARA account | How is the same total expressed after environmental coupling? | fixed at 2 |
| Component/coupling allocation \(t_c\) | How is the observed total divided among A, B, named external couplings and Other? | 0–2 individually; all sum to 2 |
| Total magnitude \(T\) | How much physical or signal activity exists? | native units |
| Rung \(k\) | At what scale is the identity being measured? | wavelength, period or declared octave |
| Phase/path | Where and in which direction is the identity moving? | angle, lag or ordered state |
| Other | How much contextual coupling remains unresolved outside the pure A/B identity? | fraction or native units |

The TE-ARA partition cannot create absolute magnitude from normalized shares. A dimensionally valid magnitude bridge must retain an
independent scale. In the MX1 plasma development test this was \(k_0E_{\mathrm{rms}}\).

### 13 July ridge/freeze clarification

For a closed two-branch identity at equal composition,

\[
p_A=p_B=\frac12,
\qquad
\underbrace{t_A}_{\text{Phase A allocation}}=1,
\qquad
\underbrace{t_B}_{\text{Phase B allocation}}=1,
\qquad
\underbrace{\mathrm{TE}(I)}_{\text{whole ledger}}=2,
\qquad
x_{A/B}=1.
\]

This is the symmetric pure-pair ridge \(1+1=2\), not the required A/B split for every identity. An asymmetric pure
pair such as \(0.50+1.50=2\) is valid; an embedded observation such as \(0.25+1.25+0.50_{context}=2\) is also valid,
but its contextual term is not a third pure pole. The total 2 is not a statement that the system has twice the
physical energy. A symmetric partition is compatible with a static equal pair, a coherent resonant/standing
mode, or incoherent cancellation. TE-ARA records allocation, not motion. A timing-based ARA is undefined without a resolved cycle;
activity/flux, time variation and phase coherence must be retained as separate coordinates.

## Participation across several contributors

For a pure identity embedded among contributors \(i\), environmental relation terms \(ij\), and unresolved Other,
use one declared observed-account denominator. These surrounding terms explain the identity-in-context; they are not
additional poles of its pure A/B geometry:

\[
\underbrace{1}_{\text{all measured participation}}
=
\underbrace{\sum_i p_i}_{\substack{\text{unique individual}\\\text{contributions}}}
+
\underbrace{\sum_{i<j}p_{ij}^{\mathrm{relation}}}_{\substack{\text{participation carried specifically}\\\text{by contributor interactions}}}
+
\underbrace{p_{\mathrm{Other}}}_{\text{unresolved participation}}.
\]

Then

\[
\underbrace{t_i}_{\substack{\text{component }i\\\text{allocation in the parent ledger}}}
=2p_i,
\qquad
\underbrace{t_{ij}}_{\substack{\text{interaction relation}\\\text{allocation in the parent ledger}}}
=2p_{ij}^{\mathrm{relation}}.
\]

The relation allocation is the measurable form of Dylan's \(1+1=3\): contributor A, contributor B, and information
carried specifically by their coupling. It does **not** mean energy appears from nowhere. Individual and interaction
shares must jointly partition the same total so correlated effects are not counted twice.

## Signed direction remains an ARA coordinate

If contributor \(i\) has support/increase and opposition/decrease components,

\[
\underbrace{x_i}_{\substack{\text{ARA direction}\\0=\text{opposes},\ 1=\text{balanced},\ 2=\text{supports}}}
=
2\frac{
\underbrace{W_i^+}_{\text{positive-oriented participation}}
}{
\underbrace{W_i^++W_i^-}_{\text{total unsigned participation}}
},
\qquad
\underbrace{t_i}_{\substack{\text{component allocation}\\\text{in the parent ledger}}}
=
2\frac{W_i^++W_i^-}{W_{\mathrm{total}}}.
\]

Plainly: ARA supplies the direction/mixing; \(t_i\) supplies how much of the parent's fixed total-2 ledger is
allocated to that relation. If component \(i\) is opened as its own identity, its own internal TE-ARA ledger is
renormalized to 2 and can be partitioned again.

---

## MX1 plasma development test

### Exact signed bridge

For positive and negative enclosed source magnitudes,

\[
\underbrace{Q_{\mathrm{net}}}_{\text{signed Gauss source}}
=
\underbrace{T_Q}_{Q_++Q_-}
\underbrace{(x_Q-1)}_{\substack{\text{pair ARA centred}\\\text{on the 1.0 ridge}}},
\qquad
x_Q=2\frac{Q_+}{Q_++Q_-}.
\]

This equality is an exact algebraic embedding. The ARA-specific empirical questions concern identity selection,
participation, local component shape and transferable compression.

### Dataset and contamination status

- Development: Alves/OSIRIS two-stream plasma, DOI `10.5281/zenodo.7968601`; inspected before registration and
  therefore calibration/development only.
- Confirmation: Tang–Wu–Tao two-stream plasma, DOI `10.5281/zenodo.3696310`; record selected and test frozen,
  numerical arrays still unopened.
- Development hashes, confirmation thresholds and interpretation rules:
  `Analysis/electromagnetism/MX1_CONFIRMATION_FREEZE_v1.md`.

### Development results

| Reading | Result | Interpretation |
|---|---:|---|
| Eligible time slices | 299 | coherent mode-5 development region |
| Full Gauss source vs particle source | \(r=0.9971\), NRMSE 0.0767 | simulator/solver consistency and adapter check; not independent ARA evidence |
| Identity-only Gauss vs identity-only particle component | \(r=0.9991\) | declared segmentation is numerically consistent across solver-coupled views; not independent ARA evidence |
| Gauss-source vs particle-source expressed A/B allocation analogue | \(r=0.7987\), MAE 0.0911 on 0–2 | primary non-tautological ARA-specific development result: the component allocation transfers imperfectly but materially; each whole ledger remains 2 |
| Whole periodic-ring pair ARA | median 1.007 | complete neutral cycles coarse-grain near the ridge |
| Five local-cell pair ARA range | 0.790–1.491 | local geometry moves despite global cancellation |
| Local field/particle pair ARA | \(r=0.7706\) | useful local relational agreement |
| Local total unsigned source | \(r=0.9997\) | magnitude is retained |
| Local signed net source | \(r=0.9953\) | pair composition plus magnitude reconstructs direction strongly |

### Same-slice compressed comparison

All models were scored on the same 75 held-late slices with clean bounded component ARAs:

| Model | Direct-source \(R^2\) | Result |
|---|---:|---|
| \(k_0E_{\mathrm{rms}}\) scale only | 0.677 | best |
| Expressed A/B allocation only (historically labelled TE-ARA) | 0.657 | close but no added value |
| ARA only | 0.348 | weaker |
| ARA + expressed A/B allocation | -1.370 | does not transfer within development |
| Matched generic features | -9.199 | also unstable |

### Honest interpretation

The decompressed identity/harmonic crosswalk is strong. The tested scalar ARA + component-allocation magnitude law is
not supported. This does not show that TE-ARA decomposition is useless: it shows that a normalized allocation is not
absolute magnitude and that reducing a
compound waveform to two component ARA scalars loses phase/harmonic/path information.

Raw component ARA values above 2 occurred when a lobe became compound. They are retained as evidence that either
segmentation lost the correct local identity or one scalar ceased to be sufficient; they were not forced back onto
the bounded coordinate.

**MX1 status:** `DEVELOPMENT PARTICIPATION-CROSSWALK POSITIVE / SOLVER IDENTITIES DEMOTED TO INSTRUMENT CHECKS /
SCALAR-MAGNITUDE NARROWING / CONFIRMATION REGISTERED AND SEALED`.

---

## Proposed ENSO use

### Why ENSO is a better TE-ARA target

The plasma development case is dominated by one spatial mode, so its ordinary field scale already contains most
magnitude information. ENSO is a distributed identity produced by several coupled oceanic and atmospheric systems.
The TE-ARA partition may therefore help answer: **how is the fixed total-2 ledger of the present ENSO identity
allocated among each contributor, their couplings and Other?**

### First restrained contributor set

Begin with a small physically declared set rather than every available climate index:

1. NINO3.4 or another declared surface-temperature target;
2. warm-water volume / thermocline reservoir;
3. zonal wind stress or trade-wind forcing;
4. SOI or a declared atmospheric-pressure partner;
5. interaction allocations, particularly reservoir × wind and ocean × atmosphere;
6. Other.

The target, contributor boundary, lag direction, time window and rung must be declared before scoring.

### ENSO participation ledger

\[
\underbrace{1}_{\text{current ENSO signal participation}}
=
\underbrace{p_{\mathrm{surface}}+p_{\mathrm{reservoir}}+p_{\mathrm{wind}}+p_{\mathrm{pressure}}}_{\text{unique shares}}
+
\underbrace{p_{\mathrm{reservoir}\times\mathrm{wind}}+p_{\mathrm{ocean}\times\mathrm{atmosphere}}}_{\text{relation shares}}
+
\underbrace{p_{\mathrm{Other}}}_{\text{unresolved}}.
\]

Initially these are variance/signal-power participation analogues, not literal climate-energy joules. Physical heat,
work or available-potential-energy calibration would be a later and stricter test.

### What would count as useful

- Shares are stable enough to reproduce on held-out decades.
- Participation shifts before declared ENSO regime changes or forecast failures.
- Interaction shares add held-out information beyond individual variables.
- The TE-ARA partition improves attribution, confidence or transition warnings beyond total amplitude and standard regression.
- The method retains Other and does not redistribute unexplained variance merely to force a closed story.

TE-ARA allocation alone is not causal attribution. Lead/lag order, held-out forecasting and removal/intervention
tests are required before calling a contributor a driver.

---

## Proposed LLM use

### Why distributed representations fit TE-ARA

An LLM concept or behaviour is commonly distributed across tokens, features, heads, layers and their interactions.
A thresholded map of only the strongest connection can erase a weak but stable coupling web. The TE-ARA partition
offers a declared way to retain those weak shares while distinguishing them from the target's main identity and from
Other.

### Required measurement declaration

For every LLM reading declare:

1. identity boundary: token prediction, concept, answer behaviour or internal representation;
2. observable: activation, logit contribution, feature attribution or intervention effect;
3. layer/inference time slice;
4. prompt set and coarse-graining;
5. positive/negative pole orientation;
6. direct contributors, interaction terms and Other.

In this domain the TE-ARA partition is a signal/attribution allocation analogue unless an independently justified energy measure
exists.

### LLM participation ledger

For target concept or behaviour \(c\),

\[
\underbrace{1}_{\text{all measured participation in }c}
=
\underbrace{\sum_i p_i(c)}_{\substack{\text{tokens/features/heads/layers}\\\text{unique contributions}}}
+
\underbrace{\sum_{i<j}p_{ij}^{\mathrm{relation}}(c)}_{\substack{\text{contribution present specifically}\\\text{when two representations interact}}}
+
\underbrace{p_{\mathrm{Other}}(c)}_{\text{distributed or unresolved}}.
\]

A weak connection is potentially structural when it is small but stable across paraphrases, layers, random seeds,
models or inference positions. A large connection that disappears under paraphrase may be incidental.

### Information³ connection

\[
\underbrace{\text{representation A}}_{\text{first information}}
+
\underbrace{\text{representation B}}_{\text{second information}}
+
\underbrace{\text{their measured relation}}_{\text{informative relation}}
\longrightarrow
\underbrace{\text{temporary closed identity}}_{\text{concept/behaviour in this inference slice}}.
\]

This is a testable operational form of Information³. The triangle is the smallest closed attribution map, but its
three entries need not be three independent structures: two identities plus their coupling relation can close the
local representation.

### Scientific safeguards

- Attention weight alone is not an explanation or causal contribution.
- Activation magnitude alone does not establish necessity.
- Validate weak links with held-out prompts, paraphrases and different models.
- Ablate, suppress or amplify a proposed contributor and predict the direction of change beforehand.
- Compare against matched generic attribution and random-feature controls.
- Record connections that disappear; do not repair the map after observing the intervention.
- Do not double-count a direct feature share and the same variance again inside an interaction share.

### What would count as useful

- A low-allocation but stable connection predicts a reproducible intervention effect.
- Direct plus relation shares explain held-out behaviour better than strong-edge-only maps.
- Participation shifts across layers in a repeatable order consistent with parsing, integration and output formation.
- The ledger distinguishes concept closure from prompt-specific surface association.

---

## TE-ARA as the decomposition-side inverse of Information³

Dylan's clarified relationship is:

- **Information³ closes upward:** two identities plus their coupling relation form a third local identity.
- **TE-ARA opens downward:** begin with one measured identity, recover its primary two-wave ARA, assign the remaining
  participation to Other, and recursively resolve Other into additional coupled sources.

Write the closure direction as

\[
\underbrace{\mathcal C}_{\substack{\text{Information³}\\\text{closure/synthesis}}}
\left(
\underbrace{A}_{\text{first identity}},
\underbrace{B}_{\text{second identity}},
\underbrace{R_{AB}}_{\text{their coupling relation}}
\right)
=
\underbrace{N}_{\text{closed local identity}}.
\]

The TE-ARA decomposition direction is

\[
\underbrace{\mathcal D_{\mathrm{TE}}}_{\substack{\text{participation}\\\text{decomposition}}}
\left(
\underbrace{N}_{\text{observed identity}}
\right)
=
\left(
\underbrace{A,B,R_{AB}}_{\text{primary two-wave identity}},
\underbrace{O_N}_{\text{Other participation}}
\right).
\]

The accounting is

\[
\underbrace{1}_{\text{all node participation}}
=
\underbrace{p_A+p_B+p_{AB}}_{\substack{\text{primary two-wave identity}\\p_{\mathrm{id}}}}
+
\underbrace{\sum_e p_e}_{\substack{\text{resolved extra}\\\text{coupling edges}}}
+
\underbrace{p_{\mathrm{unresolved}}}_{\text{Other not yet identified}},
\qquad
\underbrace{T_{\mathrm{id}}}_{\substack{\text{primary-identity subtotal}\\\text{inside the parent ledger}}}
=2p_{\mathrm{id}},
\qquad
\underbrace{\mathrm{TE}(N)}_{\text{whole node ledger}}=2.
\]

Each resolved contribution inside Other becomes an **extra edge**, not a third primary pole. That source may then be
treated as its own node and decompressed:

\[
O_N
\longrightarrow
\{N_1,N_2,\ldots,N_m,O_{\mathrm{unresolved}}\},
\qquad
N_j
\stackrel{\mathcal D_{\mathrm{TE}}}{\longrightarrow}
(A_j,B_j,R_j,O_j).
\]

This creates a recursive weighted coupling graph. For edge \(e\):

\[
\underbrace{w_e}_{\substack{\text{edge size}\\\text{coupling share}}}
=
\underbrace{2p_e}_{\text{edge allocation in the parent TE-ARA ledger}},
\qquad
\underbrace{d_e}_{\substack{\text{proposed relational distance}\\\text{not physical distance}}}
=
-\log_2
\left(
\frac{p_e+\epsilon}{p_{\max}+\epsilon}
\right).
\]

The strongest resolved edge has the largest share and smallest displayed distance. Progressively weaker shares appear
as smaller or more distant edges. The logarithmic distance is a **proposed visual/rung coordinate**, not an established
physical law. Physical distance, causal delay and scale separation must remain separate unless data connect them.

ARA supplies each edge's signed direction or pole mixture; its component allocation supplies edge
thickness/participation; rung supplies scale; phase/lag supplies ordered motion. The parent allocations sum to 2.
Opening an edge as a child identity gives it a fresh internal ledger that also sums to 2. This is the compact
recursive graph Dylan intended.

### Identifiability fence

TE-ARA decomposition is conceptually the opposite-direction tool to Information³, but it is not automatically a unique mathematical
inverse. Several hidden-source decompositions may reproduce the same observed node. Nearby-source searches,
independent measurements, held-out transfer and interventions are needed to decide which proposed extra edges are
real. Stop recursion when Other falls below a predeclared tolerance, data cannot distinguish candidate sources, or the
next boundary/rung is not independently measurable.

---

## What the visible MX1 separation means

**Corrected notation:** the curves historically called “TE-ARA analogues” below are variable expressed A/B
allocations \(T_{AB}\equiv T_{id}\). They are not whole TE-ARA totals; each complete observed account remains 2.

The development visual contains three participation curves:

1. field expressed A/B allocation analogue;
2. Gauss-predicted source expressed A/B allocation analogue;
3. independently measured particle-source expressed A/B allocation analogue.

They share a 0–2 participation scale but are not the same observable. Two separate gaps must be distinguished.

### Gap 1 — field to Gauss-source projection

\[
\underbrace{\Delta_{\mathcal G}}_{\substack{\text{field-to-source}\\\text{projection gap}}}
=
\underbrace{T_{AB,E}}_{\text{field expressed A/B allocation}}
-
\underbrace{T_{AB,\rho,G}}_{\text{Gauss-predicted source allocation}}.
\]

In Fourier space,

\[
\widehat\rho(k)=ik\widehat E(k),
\]

so Gauss rotates phase by a quarter turn and weights short spatial scales more strongly. The two historical
component-allocation formulae are

\[
T_{AB,E}
=2\frac{\sum_{k\in H}|\widehat E_k|^2}{\sum_{k\ne0}|\widehat E_k|^2},
\qquad
T_{AB,\rho,G}
=2\frac{\sum_{k\in H}k^2|\widehat E_k|^2}{\sum_{k\ne0}k^2|\widehat E_k|^2}.
\]

The curves therefore need not touch. On 299 eligible development slices,
\(\Delta_{\mathcal G}\) had mean 0.250, median 0.223 and range 0.093–0.385. It grew with field amplitude
(\(r=0.727\)) and fundamental-mode concentration (\(r=0.802\)). This separation is primarily a deterministic
derivative-weighting effect: high-\(k\) Other structure is amplified on the source side. The standard mathematical
object between these views is the operator \(ik\), not yet a newly detected physical node.

### Gap 2 — predicted source to particle source

\[
\underbrace{\Delta_{\mathrm P}}_{\substack{\text{Gauss prediction to}\\\text{independent particle reading}}}
=
\underbrace{T_{AB,\rho,G}}_{\text{field-predicted source allocation}}
-
\underbrace{T_{AB,\rho,F}}_{\text{particle-source allocation}}.
\]

This is the empirically interesting gap because it compares two data views. It had mean 0.091, median 0.020 and range
0.001–0.409. It declined strongly with time (\(r=-0.805\)), field amplitude (\(r=-0.829\)) and source activity
(\(r=-0.818\)); it increased with particle-side Other (\(r=0.860\)).

A restrained interpretation is that the early particle distribution contains more unresolved/non-identity structure,
then becomes increasingly organised into the field-defined harmonic identity as the instability develops. This could
be an ARA **handover/identity-formation marker**, but the present data do not distinguish physical organisation from
low-amplitude particle/deposition noise.

### Physical handover test required

To promote either gap from projection/residual to physical handover, a new development test must compare it against
independent energy exchange:

\[
\underbrace{J(x,t)E(x,t)}_{\substack{\text{field-particle}\\\text{power transfer}}},
\qquad
\underbrace{\frac{dU_E}{dt}}_{\text{field-energy change}},
\qquad
\underbrace{\frac{dK_{\mathrm{particle}}}{dt}}_{\text{particle kinetic-energy change}}.
\]

The phase-space archive permits reconstruction of current \(J\) and relativistic kinetic energy. Local verification
found \(v=u/\sqrt{1+u^2}\) to \(1.5\times10^{-8}\), confirming a uniform normalised momentum grid \(u\).

An unregistered feasibility check found:

- \(dU_E/dt\) vs \(-\int J E\,dx\): \(r=0.9839\);
- \(dK_e/dt\) vs \(\int J E\,dx\): \(r=0.9831\);
- total \(U_E+K_e\) relative range: 0.00101;
- instantaneous gap-closing rate vs particle-to-field power: \(r=-0.091\), a raw null;
- gap state vs field energy: \(r=-0.725\).

Thus the simple live-power-meter interpretation is not supported in development. The remaining hypothesis is that
the gap is a cumulative identity-formation/coherence state. That must beat amplitude, SNR and spectral-Other controls
on untouched data. Full protocol: `Analysis/electromagnetism/MX2_HANDOVER_ENERGY_PROTOCOL_DRAFT.md`. MX2 does not
modify the already frozen MX1 confirmation protocol.

### Credibility assessment

MX1 adds credibility to ARA as:

- a coherent signed-balance coordinate;
- a scale-aware identity/Other decomposition;
- a local-versus-coarse-grained relational map;
- a reproducible crosswalk that can be compared with independent particle data.

It does not by itself validate:

- ARA as universal/fractal geometry;
- the projection gap as a physical handover substance or node;
- a new replacement for Gauss/Fourier physics;
- the failed scalar ARA + component-allocation magnitude law.

### Known phenomenon versus potentially new diagnostic

In standard plasma language, the broad “identity formation” phenomenon is known: a two-stream instability grows
coherent electrostatic modes, bunches/traps particles, develops phase-space holes or vortices, and may approach
BGK-like nonlinear structures. Field–particle correlation is also an established diagnostic for velocity-resolved
energy transfer in counter-streaming instabilities.

The ARA-added candidate is not the discovery that particles organise. It is the proposal that

\[
\underbrace{C_{\mathrm{id}}(t)}_{\substack{\text{candidate 0--1}\\\text{identity-closure index}}}
=
1-\frac{
\left|
\underbrace{T_{AB,\rho,G}(t)
-
T_{AB,\rho,F}(t)}_{\Delta_{\mathrm P}(t)}
\right|
}{2}
\]

provides a compact, scale-resolved order parameter for how completely two independently observed appearances share
one declared identity. This index is **proposed**, not validated or established as novel.

It becomes scientifically substantial only if it:

1. remains stable when particle count/noise and numerical deposition change;
2. detects trapping/coherent-structure onset consistently against conventional diagnostics;
3. adds held-out information beyond field amplitude, spectral coherence, particle Other and field–particle
   correlation;
4. transfers without redefining the identity family;
5. generalises beyond this plasma example to other genuinely distributed identities.

If it merely follows amplitude/SNR or known spectral coherence, it is a useful ARA re-expression of established
physics, not a new physical result. A comprehensive novelty claim would require a dedicated literature review beyond
the current targeted search.

The decisive proposed test is a deliberately seeded, noise-controlled convergence family spanning particle counts,
random seeds and a continuum reference, with trapping/phase-space-vortex onset labelled independently. See
`Analysis/electromagnetism/MX3_IDENTITY_CLOSURE_CONVERGENCE_TEST_DRAFT.md`.

An MX3a development drill has now inspected the full existing single-noise archive. Coherent trapped/vortical
phase-space organisation is visually clear, and \(C_{\mathrm{id}}\) co-moves strongly with mutual information
\(r=0.8428\), velocity-bin phase coherence \(r=0.7612\) and approximate trapping \(r=0.7519\). It also follows
field amplitude \(r=0.8293\). Across 80 pairs matched within 1% field RMS, closure has essentially no post-minus-pre
separation \(-0.00080\) while approximate trapping increases \(+0.02622\). The present index is therefore a useful
organisation-associated coordinate but not yet a distinct identity order parameter. It is now defined only after a
coherent-mode eligibility gate; raw near-zero agreement before that gate is not identity evidence. Full particle-count,
seed and continuum convergence remains required. See
`Analysis/electromagnetism/MX3A_EXISTING_DATA_REPORT.md`.

Later MX3b--MX3d drilling separated ridge direction, an adjacent pressure state and parent-to-daughter harmonic
inheritance. The strongest result is MX3d: frozen parent \(k=5\) precedes daughter \(k=10\) by 19 field slices and 31
particle slices; phase closure rises from 0.2873 baseline to 0.9848 during sub-threshold formation; field/particle
bicoherence is 0.8376/0.8334 at the 97.48th/94.96th control percentiles; and the daughter persists with field/particle
TE correlation 0.9991. Six of eight strict gates pass. This is development support for a persistent nonlinear
harmonic daughter with a separate participation state, while circular-shift locality, particle-threshold, noise/seed
transfer and universal/fractal interpretation remain open. See
`Analysis/electromagnetism/MX3D_DAUGHTER_ECHO_REPORT.md`.

**Exploratory-chain disclosure (13 July audit):** MX1 through MX3g are one adaptive development chain on the same
already inspected Alves/OSIRIS archive. Later questions were informed by earlier plots, results and conversation.
Their gates and nulls make the chain auditable, but the stages are not independent confirmations and must not be
counted as separate replications. Independent noise/seed, resolution and archive transfer remain necessary.

MX3e extends the same construction one generation: \(k=10+k=10\rightarrow20\). The k20 field/particle mode follows
k10 by 63/57 slices, has phase inheritance 0.8481 with random-phase \(p=0.0010\), persists 199 slices at mean field
power fraction 0.001739, and has field/particle TE correlation 0.9988. All eight development criteria pass. Competing
sum-20 routes, especially 9+11, show that the physical structure is a nonlinear coupling web rather than a unique
binary genealogy. This is unusually clean development support for recursive daughter identities, while noise/seed
transfer and a quantitative fractal scale law remain untested. See
`Analysis/electromagnetism/MX3E_GRANDDAUGHTER_REPORT.md`.

Within this harmonic system the rung map itself is exact and requires no fitted crosswalk:
\(r(k)=\log_2(k/k_0)\), giving \(r(5)=0\), \(r(10)=1\), and \(r(20)=2\). Mixed modes occupy non-integer positions,
for example \(r(15)=\log_2 3\). This identifies ARA's proposed logarithmic rung with established harmonic doubling
locally; it does not establish the same identification across non-Fourier domains.

Repository-wide qualification: ARA already has empirical support in non-harmonic-index applications, especially
prospective 3/3 coupled-pendulum rung dominance, replicated 13/17-subject heart-morphology lift, the held-out
nasal-ENSO relation-class result and CGM convergent validation. What remains unestablished is one frozen exact
operator transferring across those domains, not the existence of any cross-domain evidence.

The same plasma descent was predeclared through k40 and k80. k40 remains a weak jointly recovered web identity
(6/8 gates; field/particle TE correlation 0.9667), while exact 20+20 self-coupling is not dominant. k80 is field-only
at 3.2 samples per wavelength and k160 exceeds Nyquist. This locates the current archive's numerical observability
floor between k40 and k80; it does not establish an ARA singularity flip.

Prospective MX1 transfer and an independent MX2 cumulative/local energy-exchange association would materially raise
the evidential level. The instantaneous MX2 association is already a development null.

---

## Current conclusion

TE-ARA presently has three different evidential standings:

1. **Defined within ARA:** TE-ARA is the same ARA geometry viewed as total allocation; the pure A/B pair closes to 2,
   while the real observed account can assign part of that total to environmental coupling and Other.
2. **Development-supported in MX1:** a variable expressed A/B allocation inside that account survives a Gauss
   field-to-particle crosswalk, while local pair geometry is recoverable beneath global cancellation.
3. **Open cross-domain hypothesis:** the TE-ARA partition will add the most value in distributed, multi-coupled
   identities such as ENSO and LLM representations.

The central lesson from MX1 is:

> TE-ARA is ARA itself viewed as a fixed total-2 allocation. The pure identity is Phase A plus Phase B; environmental
> couplings and Other explain its real observed expression but are not extra pure poles. Its allocations vary; its
> whole does not.
