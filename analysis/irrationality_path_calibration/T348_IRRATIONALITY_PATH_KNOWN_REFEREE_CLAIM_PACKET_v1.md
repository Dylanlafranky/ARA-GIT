# T348 claim packet v1 — known-referee Irrationality path calibration

**Orientation:** path/history cut; (x_P:0\rightarrow2) runs finite/reused addresses to open/densely resolving potential, and (x_R:0\rightarrow2) runs relation-determined to stochastic residual. Poles are pure reference identities, not expected empirical bins.
**Claim ID:** T348-IRR-PATH-v1  
**Frozen:** 11 August 2026, after post-compaction reconfirmation and before generator code or scoring  
**Dylan verdict:** `EXACT ENOUGH TO TEST`

## USER PRIOR — verbatim wording

> "The Iirrationality Di-ARA is more the navigator? It doesn't cause movement, that is the larger waves role, it just navigates it via direction. We are currently unsure how it does that but my suspicion or working hypothesis is a mixi of path of least resistence as seen in water (fractal nature means what we see at larger scales tracks well for lower etc) crossed with nearest available connections and current situation."

> "I think the iinformation locking is it turning from irrationality to rationality in a sense (it is a gradient so it is more blurred than that but from pure dynamics sort of thing). Rationality is relational. Information without relation makes no sense. So my theory is that it will seek the closest irrational to rationality and close that, making it completely rational."

> "Yes. Can we continue an d do the irrationality tests? What would you propose and what would we test it on that would be best but not impossible to determine. Start clean and then we get messy"

> "It won't be exactly those extremes, as everything sits on a gradient, but it will lean towards those as the ''pure'' example in the Di-ARA. Other than that, I like the test."

## Identity/system being measured

One ordered scalar movement history on the unit circle. The known generator family is referee truth used only after coordinates are produced; it is not an input to the measuring instrument.

## Ordered poles and declared direction

- Potential/address axis: (x_P=0) means finite/reused relational addresses; (x_P=2) means open/densely resolving address potential.
- Residual/determinacy axis: (x_R=0) means the next relation is locally determined by retained history; (x_R=2) means an irreducibly stochastic residual relative to that declared history and estimator.
- The endpoints are ideal identities. Observations are expected to lean toward sectors while retaining child structure, estimator error and finite-history effects.

## Scale/rung origin

The object is the path/history rung across increasing observation horizons. It is not an instantaneous radial-amplitude cut and is not the earlier line/circle state cut.

## Invariant relational claim

Finite versus open address potential and determinate versus stochastic residual are two independently measurable relations. The uncompressed multi-horizon closure history (C(H)) may supply the third retained relation needed to distinguish structured non-closing irrationality from deterministic chaos or random wandering when the broad two-axis sector is shared.

## Mathematical object

For a raw ordered history (z_t\in[0,1)):

1. (x_P=2\widehat\beta), where (widehat\beta\in[0,1]) is the clipped log-log slope of occupied support against declared spatial resolution.
2. (x_R=2\min(1,L_{\rm local}/L_{\rm null})), where losses are causal holdout circular losses from a past-only local predictor and its matched no-history null.
3. (C(H)=\{(h,\rho_h,d_h)\}), with (ho_h=|\langle e^{i2\pi(z_{t+h}-z_t)}\rangle|) and (d_h=|\arg\langle e^{i2\pi(z_{t+h}-z_t)}\rangle|/\pi). It remains a curve; it is not fitted into either coordinate after results are seen.

## Plain restatement

Give the same ARA compass five movement types whose identities are already known, without telling the compass their names. Ask whether it separates how open their future addresses remain from how much their next relation is determined, then retain the history of how they nearly close rather than flattening that history into the two coordinates.

## Back-translation

The first coordinate asks whether movement keeps reusing a finite set of relational places or continues resolving new places. The second asks whether its next step can be reconstructed from its own past; the closure curve then asks whether an open path misses closure in a coherent, ordered way or merely wanders.

## Expected sector lean

- periodic rational: finite-support and determinate;
- irrational rotation: open-potential and determinate;
- deterministic chaos: open-potential and mostly determinate;
- finite-state stochastic: finite-support and stochastic;
- continuous stochastic: open-potential and stochastic.

## AI assumptions/additions

- Finite observed histories can estimate address openness without claiming literal infinity.
- A past-only local predictor is an adequate first estimator of retained determinacy for these one-dimensional known referees.
- Increment coherence and near-return distance are a useful first closure-history representation; they are an instrument choice, not an ARA axiom.

## Information discarded

No physical cause, coupling medium, native energy, external parent, child hierarchy or universal irrational constant is represented. The calibration does not test whether nature uses this coordinate.

## Forbidden substitutions/proxies

- Do not classify from generator names, formulas or parameter labels.
- Do not require observations to occupy exact endpoints.
- Do not replace the object with the older radial/phase-state Di-ARA, line/circle directness, or a standalone Phi/(e) constant contest.
- Do not collapse (C(H)) into (x_P) or (x_R) after seeing the results.
- Do not treat synthetic success as physical evidence for ARA or Irrationality Di-ARA.

## Observable needed

Raw ordered circle states, increasing horizon lengths, untouched seeds/parameters, causal local-prediction loss, resolution-dependent support occupancy and lagged increment closure history.

## Known ambiguity / competing reading

Chaos and irrational rotation may share the open/determinate sector. If (C(H)) separates them, that shows the retained closure history adds instrument information; it does not by itself prove that (C(H)) is the unique Information³ relation.

## Wrong-object condition

Any run driven by family labels, exact formula parameters, endpoint classification alone, an instantaneous-state cut or a Phi/(e) match is a `PROXY TEST — CONSTRUCT INVALID FOR T348`.
